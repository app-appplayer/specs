name: SplashConfig
definition:
  {
    "type": "object",
    "properties": {
      "image": {
        "$ref": "#/$defs/AssetRef"
      },
      "backgroundColor": {
        "$ref": "#/$defs/ColorHex"
      },
      "duration": {
        "type": "number",
        "minimum": 0
      }
    },
    "additionalProperties": false
  }
