{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specs.makemind.ai/mcp_ui_dsl/1.4/widgets.schema.json",
  "title": "MCP UI DSL 1.4 — Widget Registry (Phase 1)",
  "$defs": {
    "EdgeInsets": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "object",
          "properties": {
            "value": {
              "type": "number"
            },
            "unit": {
              "type": "string"
            }
          },
          "required": [
            "value"
          ]
        },
        {
          "type": "object",
          "properties": {
            "all": {
              "$ref": "#/$defs/Dimension"
            },
            "horizontal": {
              "$ref": "#/$defs/Dimension"
            },
            "vertical": {
              "$ref": "#/$defs/Dimension"
            },
            "top": {
              "$ref": "#/$defs/Dimension"
            },
            "right": {
              "$ref": "#/$defs/Dimension"
            },
            "bottom": {
              "$ref": "#/$defs/Dimension"
            },
            "left": {
              "$ref": "#/$defs/Dimension"
            }
          }
        },
        {
          "$ref": "#/$defs/Binding"
        }
      ],
      "description": "Space around or inside a box. Four spellings, all accepted wherever insets\nare expected:\n\n- **Number** — one value applied to every edge.\n- **`{value, unit}`** — the legacy v1.0 dimension object; `unit` is\n  informational and the runtime treats every inset as logical pixels.\n- **Edge map** — `{all}` for every edge, `{horizontal, vertical}` for the\n  two axes, or `{top, right, bottom, left}` for individual edges. Each entry\n  is a `Dimension`, so one edge may be bound while its neighbours are\n  literal: `{ \"left\": \"{{sidebar.width}}\", \"top\": 8 }`.\n- **Binding** — resolves to any of the above at render time.\n\nA bare word is not an inset: the runtime resolves it to null and the widget\nfalls back to no padding, so validators reject it rather than let it render\nas a silent zero."
    },
    "Binding": {
      "type": "string",
      "pattern": "^\\{\\{.*\\}\\}$",
      "description": "Binding-expression string of the form `\"{{...}}\"`. Authors may\nsubstitute any primitive value with a binding so the runtime\nresolves the value at render time. Used as an alternative branch\nin primitives that otherwise expect a specific structural shape\n(AssetRef, Dimension, BorderRadius, Alignment, …)."
    },
    "Origin": {
      "type": "object",
      "properties": {
        "connection": {
          "oneOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$"
            }
          ],
          "description": "Outbound MCP client connection id, or a binding to one."
        }
      },
      "required": [
        "connection"
      ],
      "additionalProperties": false,
      "description": "Who serves a definition. Deliberately open so future sources need no new\nsyntax — today one shape is defined; `{ \"bundle\": ... }` / `{ \"agent\": ... }`\nand similar can be added without changing any document that uses it.\n\n`connection` names a live connection in the host's outbound MCP client\nregistry, addressed by the id used to open it. The DSL does not define how\nconnections are made — that is a host capability (`06_Runtime_Contract.md`\n§6.11.1); it defines only how a definition is addressed once one exists.\n\nA binding is the normal value here: an application opens its connections in\n`lifecycle.onInit` and keeps the ids in state.\n\nRuntimes MUST reject an Origin whose sole key they do not recognise rather\nthan falling back to the current origin — a permissive reading would render a\ndifferent server's UI under the requested one's identity (`07_Security.md`\n§7.10.1)."
    },
    "DefinitionSource": {
      "anyOf": [
        {
          "type": "string",
          "pattern": "^\\{\\{.*\\}\\}$",
          "description": "Binding to a definition held in state (since v1.4)."
        },
        {
          "type": "string",
          "description": "Resource URI on the current origin, e.g. `ui://pages/main`."
        },
        {
          "type": "object",
          "description": "Qualified reference to a definition on another origin (since v1.4).",
          "properties": {
            "$ref": {
              "type": "string",
              "description": "Resource URI to read from the named origin."
            },
            "from": {
              "$ref": "#/$defs/Origin"
            }
          },
          "required": [
            "$ref"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "description": "Inline PageDefinition or WidgetDefinition."
        }
      ],
      "description": "Where a definition comes from. Four forms: an inline definition, a resource\nURI on the current origin, a qualified `{ $ref, from }` reference to another\norigin, or a binding to a definition already held in state.\n\nOmitting `from` means the current origin, so every pre-1.4 document keeps its\nexact meaning. Naming an origin makes that origin ambient for the resolved\nsubtree — its tool calls, resource reads, state, storage and permissions all\nscope to it. See `01_Core_Concepts.md` §1.9 and `07_Security.md` §7.10.\n\nAccepted wherever `RouteValue` is accepted (§1.2.1) and as the `source` of the\n`view` widget (§2.13.1)."
    },
    "Color": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6}|[0-9a-fA-F]{8})$",
          "description": "Hex — `#RGB`, `#RRGGBB`, or `#AARRGGBB` (alpha first)."
        },
        {
          "description": "Material 3 scheme slot (§5.3) — adapts to light / dark mode.",
          "enum": [
            "primary",
            "onPrimary",
            "primaryContainer",
            "onPrimaryContainer",
            "secondary",
            "onSecondary",
            "secondaryContainer",
            "onSecondaryContainer",
            "tertiary",
            "onTertiary",
            "tertiaryContainer",
            "onTertiaryContainer",
            "error",
            "onError",
            "errorContainer",
            "onErrorContainer",
            "surface",
            "onSurface",
            "onSurfaceVariant",
            "surfaceTint",
            "surfaceBright",
            "surfaceDim",
            "surfaceContainerLowest",
            "surfaceContainerLow",
            "surfaceContainer",
            "surfaceContainerHigh",
            "surfaceContainerHighest",
            "outline",
            "outlineVariant",
            "inverseSurface",
            "onInverseSurface",
            "inverseOnSurface",
            "background",
            "onBackground",
            "surfaceVariant",
            "inversePrimary",
            "scrim",
            "shadow",
            "success",
            "onSuccess",
            "warning",
            "onWarning",
            "info",
            "onInfo",
            "divider",
            "foreground",
            "textOnPrimary",
            "textOnSecondary",
            "textOnBackground",
            "textOnSurface",
            "textOnError"
          ],
          "x-legacy": [
            "divider",
            "foreground",
            "textOnPrimary",
            "textOnSecondary",
            "textOnBackground",
            "textOnSurface",
            "textOnError"
          ],
          "x-legacy-note": "Pre-M3 slot names (§17.3). Accepted so documents written before §5.3.1's roles keep loading and painting; §5.3.1 lists what to write now."
        },
        {
          "type": "string",
          "pattern": "^([Rr][Ee][Dd]|[Bb][Ll][Uu][Ee]|[Gg][Rr][Ee][Ee][Nn]|[Yy][Ee][Ll][Ll][Oo][Ww]|[Oo][Rr][Aa][Nn][Gg][Ee]|[Pp][Uu][Rr][Pp][Ll][Ee]|[Bb][Ll][Aa][Cc][Kk]|[Ww][Hh][Ii][Tt][Ee]|[Gg][Rr][Ee][Yy]|[Gg][Rr][Aa][Yy])$",
          "description": "CSS basic color name. Spelled as a character class per\nletter because JSON Schema regexes are ECMA-262, which has no\ninline `(?i:)` flag — writing one makes the whole branch\nunconstrained instead of case-insensitive, and it accepted every\nstring."
        },
        {
          "type": "string",
          "pattern": "^[Rr][Gg][Bb][Aa]? *\\( *[0-9]{1,3} *, *[0-9]{1,3} *, *[0-9]{1,3} *(, *([01]|[01]?[.][0-9]+) *)?\\) *$",
          "description": "Functional notation — `rgb(r, g, b)` or `rgba(r, g, b, a)`,\nchannels 0-255 and alpha 0-1. §5.3.4 rates it SHOULD, below hex:\nit carries no alpha in the three-channel form and does not adapt\nto light / dark mode the way a scheme slot does."
        },
        {
          "$ref": "#/$defs/Binding"
        }
      ],
      "description": "A color value. Three spellings, all accepted wherever a color is\nexpected:\n\n- **Hex** — `#RGB`, `#RRGGBB`, or `#AARRGGBB`. The 8-digit form puts\n  alpha **first**, matching the runtime's parse order.\n- **Theme scheme slot** — a bare Material 3 role name from §5.3.1\n  (`primary`, `onSurfaceVariant`, `error`, …), the three semantic pairs\n  this spec adds (`success`, `warning`, `info` and their `on*`), and the\n  four legacy spellings §5.3.1 keeps (`background`, `onBackground`,\n  `surfaceVariant`, `inverseOnSurface`).\n  Resolved against the active theme, so it follows light / dark mode.\n  This is the preferred form: it is the only one that adapts.\n- **CSS basic name** — the ten colors a document can name without a\n  theme (`red`, `blue`, `green`, `yellow`, `orange`, `purple`,\n  `black`, `white`, `grey`/`gray`). Matched case-insensitively.\n- **Functional** — `rgb(r, g, b)` / `rgba(r, g, b, a)`. §5.3.4 rates\n  this SHOULD where hex is MUST; it is accepted so that a theme\n  written in that notation validates, but it adapts to neither mode\n  nor palette.\n\nBindings are accepted in all three positions.\n\nA bare word that is neither a scheme slot nor a basic name is **not**\na color: the runtime resolves it to null and the widget falls back to\nits default. Validators reject it rather than let it render as a\nsilent default."
    },
    "BorderRadius": {
      "oneOf": [
        {
          "type": "number",
          "minimum": 0
        },
        {
          "type": "object",
          "properties": {
            "topStart": {
              "type": "number",
              "minimum": 0
            },
            "topEnd": {
              "type": "number",
              "minimum": 0
            },
            "bottomStart": {
              "type": "number",
              "minimum": 0
            },
            "bottomEnd": {
              "type": "number",
              "minimum": 0
            },
            "all": {
              "type": "number",
              "minimum": 0
            }
          },
          "additionalProperties": false
        },
        {
          "$ref": "#/$defs/Binding"
        }
      ],
      "description": "Per-corner radius value. A bare number applies uniformly to all\nfour corners; the object form lets each corner take an independent\nradius. `all` is a shorthand for setting every corner at once and\nis overridden by individual corner keys.\n\nCorner names are **directional** (RTL-aware) per Material 3:\n\n- `topStart` — top corner on the leading edge (top-left in LTR,\n  top-right in RTL).\n- `topEnd` — top corner on the trailing edge.\n- `bottomStart` / `bottomEnd` — same convention on the bottom row.\n\nThis is the cross-schema canonical for any \"per-corner radius\"\nshape — referenced by `theme.Shape` (M3 shape token sizes) and by\nthe `BoxDecoration.borderRadius` widget property."
    },
    "AssetRef": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^([A-Za-z][A-Za-z0-9+.-]*:|assets/)"
        },
        {
          "type": "object",
          "properties": {
            "uri": {
              "type": "string",
              "description": "Resource uri read through MCP `resources/read` on the resolved origin."
            },
            "origin": {
              "$ref": "#/$defs/Origin"
            }
          },
          "required": [
            "uri"
          ],
          "additionalProperties": false
        },
        {
          "$ref": "#/$defs/Binding"
        }
      ],
      "description": "Reference to an asset (image, font, audio, video, SVG, …).\n\nAn asset reference takes one of three shapes: a **scheme-prefixed\nstring**, an **object** naming a resource and the origin that serves\nit, or a **binding** that resolves to either at render time.\n\n## Known schemes\n\nA runtime dispatches the right loader by prefix:\n\n- `bundle://<path>` — asset packaged inside the active bundle. Inside\n  an embedded subtree this is the *embedded* document's bundle, not\n  the embedder's (`06_Runtime_Contract.md` §6.11.3).\n- `https://<url>` / `http://<url>` — fetched over the network and\n  cached on first render.\n- `data:<mime>;base64,<payload>` (or `data:<mime>,<urlencoded>`) —\n  inline bytes carried in the DSL itself. The only form that needs no\n  I/O capability at all, and so the one a constrained host can always\n  render.\n- `assets/<path>` — Flutter-style local asset path declared in the\n  consumer app's pubspec asset list (used by host integrations that\n  ship icon SVGs alongside the runtime).\n- `client://<channel>/<id>` — resource served by the client / host\n  process via an MCP client-extension channel (`08_Client_Extensions.md`\n  §8.3). This is how a document reaches the *host's* own files —\n  `client://file/…`, `client://workspace/…` — rather than the server's.\n\n**This list is open, not closed.** The pattern admits any RFC 3986\nscheme so a host may serve forms this document does not name, exactly\nas `Origin` admits sources beyond `connection`. A runtime that meets a\nscheme it does not implement MUST fail visibly per §6.12 rather than\nreject the document — an author naming a scheme this runtime lacks has\nwritten a valid document that this runtime cannot fully render, which\nis not the same as an invalid document.\n\n## Object form — an asset served by an origin\n\n```json\n{ \"uri\": \"file:///reports/2026-q2.pdf\", \"origin\": { \"connection\": \"{{ deviceId }}\" } }\n```\n\n`uri` is read through MCP `resources/read` on the named origin. No new\nscheme is minted for \"the server's own asset\": MCP already reads an\narbitrary resource uri and returns text or base64 `blob`, so a server\npublishes assets the same way it publishes anything else.\n\n`origin` is the `Origin` primitive, so addressing an asset and\naddressing a definition use one vocabulary. Omitted, the asset resolves\nagainst the **ambient origin** — the origin of the definition that\ndeclares it (§6.11.3), never the embedder's.\n\nThe object form is what lets an asset stay *out* of application state.\nA server with room can publish a resource uri and let the runtime pull\nthe bytes; a server without room can inline the same asset as a `data:`\nstring. Both are valid, and which one a server chooses is its own\ndecision — the DSL does not force a transport on it.\n\n## Support is declared, not assumed\n\nRuntimes declare which schemes they resolve (§6.12, §18.9). Nothing\nhere requires a runtime to implement all of them; it requires a runtime\nto be honest about which it does, and to route the rest to the\nconsumer's declared fallback instead of a blank box or a placeholder\nbearing an implementation detail.\n\nUsed cross-schema by `app` (splash · publisher logos · font assets),\n`page`/widgets (image · icon · avatar · lottieAnimation ·\nBackgroundImage · mediaPlayer · rive · lightbox), and `theme` (font\nasset registration). Lives under `configs/_primitive/` for that reason."
    },
    "Dimension": {
      "oneOf": [
        {
          "type": "number"
        },
        {
          "type": "object",
          "properties": {
            "value": {
              "oneOf": [
                {
                  "type": "number"
                },
                {
                  "$ref": "#/$defs/Binding"
                }
              ]
            },
            "unit": {
              "type": "string"
            }
          },
          "required": [
            "value"
          ],
          "additionalProperties": false
        },
        {
          "$ref": "#/$defs/Binding"
        }
      ],
      "description": "Numeric size — accepts a plain `number` (logical pixels) OR the\nlegacy MCP UI DSL v1.0 `{value, unit}` object form. The `unit`\nfield is informational; the runtime treats every dimension as\nlogical pixels. `value` may itself be a binding: a bar whose width\ncomes from data writes `{\"value\": \"{{item.ratio}}\", \"unit\": \"percent\"}`,\nand that is the whole point of the object form."
    },
    "Alignment": {
      "oneOf": [
        {
          "enum": [
            "topStart",
            "topLeft",
            "topCenter",
            "topEnd",
            "topRight",
            "centerStart",
            "centerLeft",
            "center",
            "centerEnd",
            "centerRight",
            "bottomStart",
            "bottomLeft",
            "bottomCenter",
            "bottomEnd",
            "bottomRight"
          ]
        },
        {
          "type": "object",
          "properties": {
            "x": {
              "type": "number",
              "minimum": -1,
              "maximum": 1
            },
            "y": {
              "type": "number",
              "minimum": -1,
              "maximum": 1
            }
          },
          "required": [
            "x",
            "y"
          ],
          "additionalProperties": false
        },
        {
          "$ref": "#/$defs/Binding"
        }
      ],
      "description": "2D alignment within a rectangle. Token names are **directional**\n(RTL-aware) per Material 3:\n\n- `topStart` — top corner on the leading edge (top-left in LTR,\n  top-right in RTL).\n- `topEnd` — top corner on the trailing edge.\n- `centerStart` / `centerEnd`, `bottomStart` / `bottomEnd` — same\n  convention on the middle / bottom rows.\n- `topCenter`, `center`, `bottomCenter` — neutral on the cross\n  axis.\n\nObject form `{x, y}` is a numeric pair where each axis runs from\n-1 (start) through 0 (center) to 1 (end). The numeric form\nsupports any point inside or outside the rectangle.\n\nCross-schema canonical for any \"2D rectangle alignment\" use —\nchild placement (`box.alignment`, `align.alignment`,\n`stack.alignment`), gradient endpoints (`Gradient.begin/end`),\nimage fit (`BackgroundImage.alignment`)."
    },
    "AnimationCurve": {
      "enum": [
        "linear",
        "easeIn",
        "easeOut",
        "easeInOut",
        "standard",
        "standardAccelerate",
        "standardDecelerate",
        "emphasized",
        "emphasizedAccelerate",
        "emphasizedDecelerate",
        "bounceIn",
        "bounceOut"
      ],
      "description": "Easing curve applied to an animation's progress. Eleven canonical\nvalues cover the M3-recommended palette plus the CSS-familiar\nbasics:\n\n- **CSS-familiar** — `linear`, `easeIn`, `easeOut`, `easeInOut`.\n- **M3 standard** — `standard`, `standardAccelerate`,\n  `standardDecelerate`. Used for everyday transitions; minimal\n  overshoot, clean cubic.\n- **M3 emphasized** — `emphasized`, `emphasizedAccelerate`,\n  `emphasizedDecelerate`. Used for hero / page transitions; more\n  dramatic acceleration profile.\n- **Bounce** — `bounceIn`, `bounceOut`. For attention / alert\n  micro-interactions.\n\nCross-schema canonical for `animatedContainer.curve`,\n`animatedOpacity.curve`, RouteTransition, scrollAnimated easing,\nand any future implicit-animation widget. Matches the M3 motion\nspec's named tokens 1:1 so theme-level motion overrides stay\nvendor-neutral."
    },
    "Gradient": {
      "type": "object",
      "properties": {
        "type": {
          "enum": [
            "linear",
            "radial",
            "sweep"
          ]
        },
        "colors": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Color"
          },
          "minItems": 2
        },
        "stops": {
          "type": "array",
          "items": {
            "type": "number",
            "minimum": 0,
            "maximum": 1
          }
        },
        "begin": {
          "$ref": "#/$defs/Alignment"
        },
        "end": {
          "$ref": "#/$defs/Alignment"
        },
        "center": {
          "$ref": "#/$defs/Alignment"
        },
        "radius": {
          "type": "number",
          "minimum": 0
        },
        "startAngle": {
          "type": "number"
        },
        "endAngle": {
          "type": "number"
        },
        "tileMode": {
          "enum": [
            "clamp",
            "repeated",
            "mirror"
          ],
          "default": "clamp"
        }
      },
      "required": [
        "type",
        "colors"
      ],
      "additionalProperties": false,
      "description": "Multi-stop gradient fill. Selected by `type`:\n\n- `linear` — straight axis from `begin` (default `centerLeft`) to\n  `end` (default `centerRight`).\n- `radial` — circular gradient out from `center` (default `center`)\n  to `radius` (default `0.5`, expressed as a fraction of the\n  bounding rectangle's longest side).\n- `sweep` — angular gradient around `center`, swept from\n  `startAngle` (default `0`) to `endAngle` (default `2π`) in\n  radians.\n\n`colors` lists at least two color values; `stops` (optional) places\neach color at a position in `[0, 1]`. `tileMode` controls behaviour\npast the gradient extents."
    },
    "BackgroundImage": {
      "type": "object",
      "properties": {
        "image": {
          "$ref": "#/$defs/AssetRef"
        },
        "fit": {
          "enum": [
            "fill",
            "contain",
            "cover",
            "fitWidth",
            "fitHeight",
            "none",
            "scaleDown"
          ],
          "default": "cover"
        },
        "alignment": {
          "$ref": "#/$defs/Alignment"
        },
        "opacity": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "colorFilter": {
          "type": "object",
          "properties": {
            "color": {
              "$ref": "#/$defs/Color"
            },
            "blendMode": {
              "enum": [
                "srcIn",
                "color",
                "screen",
                "overlay",
                "multiply"
              ],
              "default": "srcIn",
              "description": "Curated subset of blend modes covering the common tinting use cases (`srcIn` for solid tint, `color` for hue swap, `screen`/`overlay`/`multiply` for atmosphere)."
            }
          },
          "required": [
            "color"
          ],
          "additionalProperties": false
        },
        "repeat": {
          "enum": [
            "noRepeat",
            "repeat",
            "repeatX",
            "repeatY"
          ],
          "default": "noRepeat"
        }
      },
      "required": [
        "image"
      ],
      "additionalProperties": false,
      "description": "Image fill behind a box's content. `image` accepts the same forms as\nthe `image` widget's `src` (`bundle://`, `https?://`, `data:`,\n`client://`). `fit` and `alignment` mirror the `image` widget's\nsemantics. `opacity` applies an alpha multiplier; `colorFilter`\ncomposites a tint via the named blend mode. `repeat` controls\ntiling behaviour outside the image's natural bounds."
    },
    "RouteTransition": {
      "type": "object",
      "properties": {
        "style": {
          "enum": [
            "slide",
            "fade",
            "scale",
            "cube",
            "sharedAxis",
            "fadeThrough"
          ]
        },
        "duration": {
          "$ref": "#/$defs/Dimension",
          "description": "Total transition duration in milliseconds. Defaults to the M3 motion-system value for the chosen style."
        },
        "curve": {
          "$ref": "#/$defs/AnimationCurve",
          "description": "Easing curve for the transition's progress. Defaults to `emphasized`."
        },
        "axis": {
          "enum": [
            "horizontal",
            "vertical"
          ],
          "description": "Direction for `slide` / `sharedAxis`. Defaults to `horizontal`."
        },
        "reverse": {
          "type": "boolean",
          "default": false,
          "description": "When true, run the transition in reverse direction (for back navigation)."
        }
      },
      "required": [
        "style"
      ],
      "additionalProperties": false,
      "description": "Per-route page transition. Declared on a `RouteValue` so every\n`navigate` to that route uses the same transition, OR overridden\ninline on a `navigate` action. Six canonical styles:\n\n- `slide` — incoming page slides over the outgoing along the\n  direction (M3 default for forward navigation).\n- `fade` — cross-fade between pages at the same position.\n- `scale` — outgoing scales out, incoming scales in.\n- `cube` — perspective-rotate as if turning a 3D cube; needs a\n  perspective-aware compositor (falls back to `slide` when\n  unavailable).\n- `sharedAxis` — coordinated slide+fade across an axis (M3 motion\n  pattern for sibling-level navigation).\n- `fadeThrough` — outgoing fades out while incoming fades in\n  after a brief zero-opacity bridge (M3 motion pattern for\n  top-level navigation)."
    },
    "IconRef": {
      "$comment": "anyOf, not oneOf: the name branch is an unconstrained string, so a binding expression matches both it and the Binding branch. `oneOf` reads that legitimate value as a violation — the same reason the Widget union uses anyOf.",
      "anyOf": [
        {
          "type": "string",
          "description": "Icon name, or an AssetRef string (scheme-prefixed, or an `assets/` path)."
        },
        {
          "type": "object",
          "properties": {
            "codepoint": {
              "type": "integer"
            },
            "fontFamily": {
              "type": "string"
            },
            "fontPackage": {
              "type": "string"
            }
          },
          "required": [
            "codepoint"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "uri": {
              "type": "string",
              "description": "Resource uri read through MCP `resources/read` on the resolved origin."
            },
            "origin": {
              "$ref": "#/$defs/Origin"
            }
          },
          "required": [
            "uri"
          ],
          "additionalProperties": false
        },
        {
          "$ref": "#/$defs/Binding"
        }
      ],
      "description": "Reference to an icon. Three forms, all accepted anywhere an icon is\ntaken — the `icon` widget and every widget with an icon slot\n(`button.icon`, `iconButton.icon`, `floatingActionButton.icon`,\n`popupMenuButton.icon`, `rating.icon`, `offlineFallback.icon`,\n`permissionPrompt.icon`, …):\n\n1. **Name** — `\"home\"`, `\"folder_open\"`, … resolved through the\n   runtime's built-in icon table. Zero-cost, and the default reading of\n   a bare string.\n2. **Codepoint** — `{codepoint, fontFamily?, fontPackage?}` for glyphs\n   the name table does not carry.\n3. **`AssetRef`** — any form that primitive accepts (network URL,\n   `bundle://`, `assets/`, `data:image/svg+xml;…`, `client://`, or the\n   `{uri, origin?}` object). Which of those a runtime resolves is\n   declared, not assumed (`06_Runtime_Contract.md` §6.12).\n\nA bare string carrying a known scheme prefix (or an `assets/` path) is\nan asset; every other bare string is a **name**. That rule is what lets\nthe named form stay the zero-ceremony default while the same slot also\naccepts a server-served glyph.\n\nThis primitive exists so the rule is stated once. Before it, `icon`\ndocumented three forms while the eight other icon slots were typed as\nbare strings — so the same value was legal in one slot and undeclared\nin the next, and a codepoint object could not be written at all outside\nthe `icon` widget."
    },
    "NavigationStyle": {
      "type": "object",
      "properties": {
        "backgroundColor": {
          "$ref": "#/$defs/Color"
        },
        "backgroundImage": {
          "$ref": "#/$defs/BackgroundImage"
        },
        "indicatorColor": {
          "$ref": "#/$defs/Color",
          "description": "Selection indicator pill color (rail / drawer / tabs)."
        },
        "indicatorShape": {
          "$ref": "#/$defs/BorderRadius",
          "description": "Selection indicator corner radius. Defaults to a fully-rounded pill."
        },
        "dividerColor": {
          "$ref": "#/$defs/Color",
          "description": "Drawer-only — color of dividers between item groups."
        },
        "dividerThickness": {
          "$ref": "#/$defs/Dimension"
        },
        "dividerIndent": {
          "$ref": "#/$defs/Dimension",
          "description": "Drawer-only — leading inset before each divider."
        },
        "labelStyle": {
          "$ref": "#/$defs/TextStyle",
          "description": "Default text style for item labels (selected and unselected). Per-item override via NavItem.style.labelStyle."
        },
        "iconStyle": {
          "type": "object",
          "properties": {
            "color": {
              "$ref": "#/$defs/Color"
            },
            "size": {
              "$ref": "#/$defs/Dimension"
            }
          },
          "additionalProperties": false,
          "description": "Default icon color and size."
        },
        "selectedColor": {
          "$ref": "#/$defs/Color",
          "description": "Foreground color (icon + label) for the selected item."
        },
        "unselectedColor": {
          "$ref": "#/$defs/Color",
          "description": "Foreground color for non-selected items."
        },
        "elevation": {
          "$ref": "#/$defs/Dimension"
        }
      },
      "additionalProperties": false,
      "description": "Visual styling for a navigation surface (drawer / rail / tabs /\nbottomBar). Every field is optional — omitted fields fall back to\nthe active theme's component defaults. Used by\n`NavigationConfig.style` (whole surface) and `NavItem.style`\n(per-item override).\n\nBackgrounds compose: `backgroundColor` paints first, then\n`backgroundImage` paints over it."
    },
    "TextStyle": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^\\{\\{.*\\}\\}$",
          "description": "Binding expression that resolves to a TextStyle object — typically `{{theme.typography.<role>}}`."
        },
        {
          "type": "object",
          "properties": {
            "fontFamily": {
              "oneOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              ]
            },
            "fontSize": {
              "$ref": "#/$defs/Dimension"
            },
            "fontWeight": {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^[1-9]00$",
                  "description": "CSS numeric spelling (`\"700\"`). Documents written against CSS habits use it, and the runtime reads it the same as the number."
                },
                {
                  "enum": [
                    "thin",
                    "extraLight",
                    "light",
                    "normal",
                    "medium",
                    "semiBold",
                    "bold",
                    "extraBold",
                    "black",
                    "regular",
                    "w100",
                    "w200",
                    "w300",
                    "w400",
                    "w500",
                    "w600",
                    "w700",
                    "w800",
                    "w900"
                  ]
                },
                {
                  "$ref": "#/$defs/Binding"
                }
              ]
            },
            "fontStyle": {
              "oneOf": [
                {
                  "enum": [
                    "normal",
                    "italic"
                  ]
                },
                {
                  "$ref": "#/$defs/Binding"
                }
              ]
            },
            "color": {
              "type": "string"
            },
            "backgroundColor": {
              "type": "string"
            },
            "letterSpacing": {
              "$ref": "#/$defs/Dimension"
            },
            "wordSpacing": {
              "$ref": "#/$defs/Dimension"
            },
            "height": {
              "$ref": "#/$defs/Dimension",
              "description": "Line-height multiplier — `height * fontSize` is the per-line vertical advance."
            },
            "decoration": {
              "oneOf": [
                {
                  "enum": [
                    "none",
                    "underline",
                    "overline",
                    "lineThrough"
                  ]
                },
                {
                  "$ref": "#/$defs/Binding"
                }
              ]
            },
            "decorationColor": {
              "type": "string"
            },
            "decorationStyle": {
              "oneOf": [
                {
                  "enum": [
                    "solid",
                    "double",
                    "dotted",
                    "dashed",
                    "wavy"
                  ]
                },
                {
                  "$ref": "#/$defs/Binding"
                }
              ]
            },
            "decorationThickness": {
              "$ref": "#/$defs/Dimension"
            },
            "fontFeatures": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "OpenType feature tags (`smcp`, `lnum`, `tnum`, `liga`, `dlig`, …)."
            },
            "shadows": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "color": {
                    "type": "string"
                  },
                  "offset": {
                    "type": "object",
                    "properties": {
                      "dx": {
                        "type": "number"
                      },
                      "dy": {
                        "type": "number"
                      },
                      "x": {
                        "type": "number"
                      },
                      "y": {
                        "type": "number"
                      }
                    },
                    "additionalProperties": false,
                    "description": "`{dx, dy}` (canonical) or `{x, y}` (legacy alias) offset."
                  },
                  "blurRadius": {
                    "$ref": "#/$defs/Dimension"
                  }
                },
                "required": [
                  "color"
                ],
                "additionalProperties": false
              }
            },
            "shader": {
              "$ref": "#/$defs/Gradient",
              "description": "Gradient/shader fill applied to the rendered text via a shader mask. When set, `color` is ignored."
            }
          },
          "additionalProperties": true
        }
      ],
      "description": "Inline text style applied per `text` widget instance. Layered on\ntop of an M3 typography role when `variant` is set, or used\nstandalone. Mirrors the theme `TextStyle` token shape (fontFamily,\nfontSize, fontWeight, lineHeight, letterSpacing, fontFeatureSettings)\nand adds the per-instance fields a content app needs: color,\ndecoration, shader (gradient fill), and shadow stack.\n\nThe object stays open. Documents in the field carry keys this style\nnever had — `textAlign` inside `style` is the common one, written by\nhand before it moved onto the widget — and the runtime simply ignores\nthem. Rejecting them here would not correct anything: validation runs\nwhen the document loads, so a stray key inside `style` would stop the\nwhole page instead of being ignored the way the runtime ignores it."
    },
    "Marker": {
      "type": "object",
      "properties": {
        "latitude": {
          "type": "number"
        },
        "longitude": {
          "type": "number"
        },
        "lat": {
          "type": "number"
        },
        "lng": {
          "type": "number"
        },
        "label": {
          "type": "string"
        },
        "color": {
          "$ref": "#/$defs/Color"
        },
        "icon": {
          "$ref": "#/$defs/IconRef"
        }
      },
      "additionalProperties": true,
      "description": "One point marked on a map.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "BorderSide": {
      "type": "object",
      "properties": {
        "color": {
          "$ref": "#/$defs/Color"
        },
        "width": {
          "type": "number",
          "minimum": 0
        },
        "style": {
          "enum": [
            "solid",
            "none"
          ],
          "default": "solid"
        }
      },
      "additionalProperties": false,
      "description": "One side of a box border."
    },
    "DropCap": {
      "type": "object",
      "properties": {
        "lines": {
          "type": "integer",
          "minimum": 2,
          "maximum": 6,
          "default": 3,
          "description": "Number of body-text lines the cap spans vertically."
        },
        "glyph": {
          "type": "string",
          "description": "Override character. Default: first character of the text widget's `text`."
        },
        "style": {
          "$ref": "#/$defs/TextStyle",
          "description": "Cap-specific style overrides, layered on top of the parent widget's style. `fontSize` is computed from `lines × parent.fontSize` when omitted."
        }
      },
      "additionalProperties": false,
      "description": "Typographic drop-cap ornament for long-form text (book chapters,\nmagazine articles). The first character of the text widget's content\nis rendered enlarged in the top-left corner; the surrounding text\nflows in a left-indented column for `lines` lines and then continues\nfull width below the cap. `glyph` overrides the cap character (e.g.\nrendering a stylised dingbat instead of the literal first letter).\n`style` layers on top of the parent text widget's resolved style for\ncap-specific overrides (typically a different `fontFamily` and\nlarger `fontWeight`)."
    },
    "Overlay": {
      "type": "object",
      "properties": {
        "points": {
          "type": "array"
        },
        "color": {
          "$ref": "#/$defs/Color"
        },
        "type": {
          "type": "string"
        },
        "width": {
          "type": "number"
        }
      },
      "additionalProperties": true,
      "description": "A shape drawn over a map — a path or a filled area.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "Step": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string"
        },
        "titleText": {
          "type": "string"
        },
        "subtitle": {
          "type": "string"
        },
        "content": {
          "$ref": "#/$defs/Widget"
        },
        "state": {
          "type": "string"
        },
        "isActive": {
          "type": "boolean"
        }
      },
      "additionalProperties": true,
      "description": "One step of a stepper.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "TimelineItem": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string"
        },
        "subtitle": {
          "type": "string"
        },
        "time": {
          "type": "string"
        },
        "icon": {
          "$ref": "#/$defs/IconRef"
        },
        "color": {
          "$ref": "#/$defs/Color"
        }
      },
      "additionalProperties": true,
      "description": "One entry on a timeline.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "TableRow": {
      "type": "object",
      "properties": {
        "cells": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          }
        }
      },
      "additionalProperties": true,
      "description": "One row of a `table`. `cells` carries the widgets laid out across the row's\ncolumns — `table` is a layout table, not a data-bound one (§10.3), so a row\nnames its widgets rather than its values.\n\nNothing here is required and nothing is bounded, deliberately. `cells: []`\ncarried a `minItems: 1` for one revision — left behind when the narrowing\nthis file describes was reverted — and it did exactly what the paragraph\nbelow says not to do: refused the document at load.\n\nNothing here is required, deliberately. A row with no `cells` — `{}`, or the\ncolumn-keyed shape that belongs to `dataTable` (`{ \"name\": \"Flutter\" }`) —\ncannot be laid out, and the temptation is to refuse it in the schema. That\nrefusal happens at load, so it does not deprecate the old document, it stops\nthe whole document opening (§1.7.5). The runtime drops the rows it cannot\nlay out and draws the rest; the shape stays accepted."
    },
    "Sliver": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "type": {
              "const": "sliverAppBar"
            },
            "title": {
              "$ref": "#/$defs/Widget"
            },
            "expandedHeight": {
              "$ref": "#/$defs/Dimension"
            },
            "flexibleSpace": {
              "$ref": "#/$defs/Widget"
            },
            "background": {
              "$ref": "#/$defs/Widget"
            },
            "pinned": {
              "type": "boolean",
              "default": false
            },
            "floating": {
              "type": "boolean",
              "default": false
            },
            "snap": {
              "type": "boolean",
              "default": false
            },
            "stretch": {
              "type": "boolean",
              "default": false
            },
            "elevation": {
              "$ref": "#/$defs/Dimension"
            },
            "backgroundColor": {
              "$ref": "#/$defs/Color"
            },
            "foregroundColor": {
              "$ref": "#/$defs/Color"
            }
          },
          "required": [
            "type"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "const": "sliverPersistentHeader"
            },
            "minExtent": {
              "$ref": "#/$defs/Dimension"
            },
            "maxExtent": {
              "$ref": "#/$defs/Dimension"
            },
            "child": {
              "$ref": "#/$defs/Widget"
            },
            "pinned": {
              "type": "boolean",
              "default": false
            },
            "floating": {
              "type": "boolean",
              "default": false
            }
          },
          "required": [
            "type",
            "minExtent",
            "maxExtent",
            "child"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "const": "sliverList"
            },
            "items": {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$"
                }
              ]
            },
            "itemTemplate": {
              "$ref": "#/$defs/Widget"
            },
            "children": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            }
          },
          "required": [
            "type"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "const": "sliverGrid"
            },
            "items": {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$"
                }
              ]
            },
            "itemTemplate": {
              "$ref": "#/$defs/Widget"
            },
            "children": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            "columns": {
              "type": "number",
              "minimum": 1
            },
            "mainAxisSpacing": {
              "type": "number",
              "minimum": 0
            },
            "crossAxisSpacing": {
              "type": "number",
              "minimum": 0
            },
            "itemAspectRatio": {
              "type": "number",
              "exclusiveMinimum": 0
            }
          },
          "required": [
            "type",
            "columns"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "type": {
              "const": "sliverFixedExtentList"
            },
            "itemExtent": {
              "$ref": "#/$defs/Dimension"
            },
            "items": {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$"
                }
              ]
            },
            "itemTemplate": {
              "$ref": "#/$defs/Widget"
            },
            "children": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            }
          },
          "required": [
            "type",
            "itemExtent"
          ],
          "additionalProperties": false
        }
      ],
      "description": "Scrollable region inside `scrollView.slivers`. Discriminated by\n`type`:\n\n- **sliverAppBar** — collapsing parallax masthead at the top of a\n  scroll view. Expands to `expandedHeight` then shrinks to a\n  fixed bar as the user scrolls. `flexibleSpace` paints the hero\n  area; `pinned`/`floating` control whether the bar sticks.\n  `backgroundColor` is the colour of the **bar itself**, which is\n  what the reader sees once the hero has faded out — without it a\n  `title` colour chosen against the hero becomes unreadable at the\n  moment the bar collapses, and the author has no way to say\n  otherwise. `foregroundColor` sets the title and icon colour for\n  the same reason.\n- **sliverPersistentHeader** — section header that stays visible\n  while its section's items scroll. `minExtent`/`maxExtent` set\n  the size envelope; the runtime morphs the header within it.\n- **sliverList** — vertical list with variable-height items.\n  Same `items` + `itemTemplate` contract as the `list` widget: an inline\n  array or a binding, either one. It accepted only the binding string for a\n  revision, so the ordinary inline form — legal in `list` and `grid` — was\n  refused at load in slivers alone.\n- **sliverGrid** — 2D grid with the same `items` / `columns`\n  contract as `grid`.\n- **sliverFixedExtentList** — list whose items all share the\n  same scroll-axis extent (uniform row height). Faster than\n  `sliverList` because the runtime can skip per-item measurement.\n\nEach shape carries the type discriminator on `type` and is only\nvalid inside a `scrollView.slivers` array. The runtime rejects\nthese shapes at the top level."
    },
    "Tab": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string"
        },
        "text": {
          "type": "string"
        },
        "icon": {
          "$ref": "#/$defs/IconRef"
        },
        "iconMargin": {
          "$ref": "#/$defs/EdgeInsets"
        },
        "height": {
          "$ref": "#/$defs/Dimension"
        }
      },
      "additionalProperties": true,
      "description": "One tab of a tab bar.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping.\n\nA tab that names none of `label` / `text` / `icon` shows nothing, and that\nis accepted: it was briefly required here, and requiring it rejects the\ndocument at load rather than drawing an empty tab (§1.7.5). The runtime\ndraws the empty tab."
    },
    "Span": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "text": {
              "type": "string"
            },
            "style": {
              "$ref": "#/$defs/TextStyle"
            },
            "onTap": {
              "$ref": "#/$defs/Action"
            },
            "children": {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Span"
              }
            }
          },
          "required": [
            "text"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "widget": {
              "$ref": "#/$defs/Widget"
            },
            "alignment": {
              "enum": [
                "top",
                "middle",
                "bottom",
                "aboveBaseline",
                "belowBaseline",
                "baseline"
              ],
              "default": "bottom"
            },
            "baseline": {
              "enum": [
                "alphabetic",
                "ideographic"
              ]
            }
          },
          "required": [
            "widget"
          ],
          "additionalProperties": false
        }
      ],
      "description": "Inline span in a `richText` composition. Discriminated by which\nidentifying field is present:\n\n- `text` (string) → **TextSpan** — a styled run of text. Optional\n  `style: TextStyle`, `onTap: Action`, and nested `children:\n  Span[]` for further inline structure.\n- `widget` (Widget) → **WidgetSpan** — an inline-widget placeholder\n  (icon, image, custom badge) that the rich-text layout treats as\n  a single character. `alignment` and `baseline` control its\n  vertical placement relative to the surrounding line.\n\nThe two shapes are mutually exclusive — a span MUST NOT carry both\n`text` and `widget`."
    },
    "Column": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "field": {
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "sortable": {
          "type": "boolean"
        },
        "align": {
          "type": "string"
        },
        "width": {
          "type": "number"
        }
      },
      "additionalProperties": true,
      "description": "One column of a data table.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "BoxSpacing": {
      "anyOf": [
        {
          "type": "string",
          "description": "M3 spacing token resolved through `theme.spacing.<token>`. Any other string resolves to no inset and is reported at runtime."
        },
        {
          "type": "object",
          "properties": {
            "token": {
              "type": "string"
            }
          },
          "required": [
            "token"
          ]
        },
        {
          "$ref": "#/$defs/EdgeInsets"
        }
      ],
      "description": "An inset on `box`, which additionally accepts an **M3 spacing token**.\n\nEvery other inset slot in the DSL is a plain `EdgeInsets`. `box` is the one\nthat also resolves a token through `theme.spacing.<token>` to a uniform\ninset, in two spellings:\n\n- **Token** — `\"md\"`. The nine standard slots are `xxs` / `xs` / `sm` / `md`\n  / `lg` / `xl` / `2xl` / `3xl` / `4xl`, and a theme may declare its own\n  (`05_Theme.md` §5.5), so the form is a bare word rather than a closed set.\n- **`{token}` object** — `{\"token\": \"md\"}`, the same lookup written as an\n  object. Kept because it composes with the responsive override map.\n- Everything `EdgeInsets` accepts — number, `{value, unit}`, the edge map,\n  and a binding.\n\n**The string branch is deliberately open.** A value that was never a token\n(`\"16px\"`, `\"1rem\"`) resolves to no inset, and catching it here would be\nbetter than catching it on screen — but `box.padding` has admitted any\nstring since 1.4, and a runtime validates documents *at load*, so\nnarrowing this pattern would stop an already-published bundle from opening\nrather than merely warning its author. The unresolved value is reported by\nthe runtime instead, once per distinct value. Tightening it needs a\nrelease allowed to break bundles; until then the report is the answer.\n\n`margin` takes the same spellings as `padding`. It did not until the 1.4.1\nfollow-up — the widget has always resolved both through one helper, so the\nnarrower declaration only made the token unauthorable, never unrenderable."
    },
    "BoxShadow": {
      "type": "object",
      "properties": {
        "color": {
          "$ref": "#/$defs/Color"
        },
        "offset": {
          "type": "object",
          "properties": {
            "dx": {
              "type": "number"
            },
            "dy": {
              "type": "number"
            }
          },
          "required": [
            "dx",
            "dy"
          ],
          "additionalProperties": false
        },
        "offsetX": {
          "type": "number",
          "description": "Legacy flat offset; use `offset.dx`."
        },
        "offsetY": {
          "type": "number",
          "description": "Legacy flat offset; use `offset.dy`."
        },
        "blurRadius": {
          "type": "number",
          "minimum": 0
        },
        "blur": {
          "type": "number",
          "minimum": 0,
          "description": "Legacy alias of `blurRadius`."
        },
        "spreadRadius": {
          "type": "number"
        },
        "spread": {
          "type": "number",
          "description": "Legacy alias of `spreadRadius`."
        }
      },
      "required": [
        "color"
      ],
      "additionalProperties": false,
      "description": "A drop shadow painted beneath the box. Multiple shadows compose via\n`decoration.boxShadow: array<BoxShadow>` and stack from the first\n(closest to the box) outward.\n\nThe offset has two spellings: `offset: {dx, dy}` (canonical) and the\nflat `offsetX` / `offsetY` pair kept from v1.0. Likewise `blurRadius`\nand `spreadRadius` each accept a short legacy name (`blur`, `spread`).\nCanonical wins when both are present."
    },
    "MenuItem": {
      "type": "object",
      "properties": {
        "value": {},
        "label": {
          "type": "string"
        },
        "icon": {
          "$ref": "#/$defs/IconRef"
        },
        "enabled": {
          "type": "boolean"
        },
        "child": {
          "$ref": "#/$defs/Widget"
        }
      },
      "additionalProperties": true,
      "description": "One entry in a popup menu.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "ExitButtonConfig": {
      "type": "object",
      "properties": {
        "icon": {
          "$ref": "#/$defs/IconRef"
        },
        "tooltip": {
          "type": "string"
        },
        "color": {
          "$ref": "#/$defs/Color"
        }
      },
      "additionalProperties": true,
      "description": "Override for the host-inserted close button.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "BoxBorder": {
      "anyOf": [
        {
          "$ref": "#/$defs/BorderSide"
        },
        {
          "type": "object",
          "properties": {
            "top": {
              "oneOf": [
                {
                  "$ref": "#/$defs/BorderSide"
                },
                {
                  "type": "boolean"
                }
              ]
            },
            "bottom": {
              "oneOf": [
                {
                  "$ref": "#/$defs/BorderSide"
                },
                {
                  "type": "boolean"
                }
              ]
            },
            "left": {
              "oneOf": [
                {
                  "$ref": "#/$defs/BorderSide"
                },
                {
                  "type": "boolean"
                }
              ]
            },
            "right": {
              "oneOf": [
                {
                  "$ref": "#/$defs/BorderSide"
                },
                {
                  "type": "boolean"
                }
              ]
            },
            "all": {
              "$ref": "#/$defs/BorderSide"
            },
            "color": {
              "$ref": "#/$defs/Color"
            },
            "width": {
              "type": "number",
              "minimum": 0
            },
            "style": {
              "enum": [
                "solid",
                "none"
              ]
            }
          },
          "additionalProperties": false
        }
      ],
      "description": "Border around a box. Either a single uniform side applied to all four\nedges, or per-side overrides. Per-side keys (`top` / `bottom` /\n`left` / `right`) take precedence over `all`.\n\nA per-side key may also be `true`, which draws that edge with the side\ndescribed by the sibling `color` / `width` / `style` keys — the shorthand a\ndocument reaches for when only one edge is wanted."
    },
    "Option": {
      "type": "object",
      "properties": {
        "value": {},
        "label": {
          "type": "string"
        },
        "binding": {
          "type": "string"
        },
        "icon": {
          "$ref": "#/$defs/IconRef"
        },
        "enabled": {
          "type": "boolean"
        }
      },
      "required": [
        "value"
      ],
      "additionalProperties": true,
      "description": "One entry in a selectable list.\n\n`value` is required. Without it the runtime falls back to an empty string,\nso two such entries carry the same value and selecting one selects both —\nthe widget renders, reports nothing, and behaves wrongly. `label` falls back\nto `value`, so the reverse substitution does not exist.\n\nOther keys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "DrawerItem": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "icon": {
          "$ref": "#/$defs/IconRef"
        },
        "route": {
          "type": "string"
        },
        "value": {}
      },
      "additionalProperties": true,
      "description": "One entry in a drawer.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "Point": {
      "type": "object",
      "properties": {
        "x": {},
        "y": {
          "type": "number"
        },
        "label": {
          "type": "string"
        },
        "value": {
          "type": "number"
        },
        "color": {
          "$ref": "#/$defs/Color"
        }
      },
      "additionalProperties": true,
      "description": "One plotted point. `{x, y}` for a series, `{label, value}` for a category.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "SnackBarAction": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string"
        },
        "onPressed": {
          "$ref": "#/$defs/Action"
        },
        "onTap": {
          "$ref": "#/$defs/Action"
        },
        "textColor": {
          "$ref": "#/$defs/Color"
        }
      },
      "additionalProperties": true,
      "description": "The action offered alongside a snack bar message.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "BoxDecoration": {
      "type": "object",
      "properties": {
        "color": {
          "$ref": "#/$defs/Color"
        },
        "gradient": {
          "$ref": "#/$defs/Gradient"
        },
        "image": {
          "$ref": "#/$defs/BackgroundImage"
        },
        "border": {
          "$ref": "#/$defs/BoxBorder"
        },
        "borderRadius": {
          "$ref": "#/$defs/BorderRadius"
        },
        "boxShadow": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/BoxShadow"
          }
        },
        "shape": {
          "enum": [
            "rectangle",
            "circle"
          ],
          "default": "rectangle"
        },
        "backdropBlur": {
          "type": "number",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "description": "Visual decoration for a box. `color` and `gradient` are mutually\nexclusive — when both are present `gradient` wins. `image` paints\nbeneath the gradient/color (background image layer). `border`,\n`borderRadius`, and `boxShadow` apply on top of the fill. `shape`\ntoggles between rectangle (default) and circle (uses the box's\nshorter side as diameter; ignores `borderRadius`). `backdropBlur`\napplies a Gaussian blur to whatever is behind the box (for frosted-\nglass overlays); the value is the blur sigma."
    },
    "Segment": {
      "type": "object",
      "properties": {
        "from": {
          "type": "number"
        },
        "to": {
          "type": "number"
        },
        "color": {
          "$ref": "#/$defs/Color"
        },
        "label": {
          "type": "string"
        }
      },
      "additionalProperties": true,
      "description": "One coloured band of a gauge scale.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "BannerAction": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string"
        },
        "click": {
          "$ref": "#/$defs/Action"
        },
        "onTap": {
          "$ref": "#/$defs/Action"
        }
      },
      "additionalProperties": true,
      "description": "One action offered on a banner.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "NavItem": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "icon": {
          "$ref": "#/$defs/IconRef"
        },
        "activeIcon": {
          "$ref": "#/$defs/IconRef"
        },
        "backgroundColor": {
          "$ref": "#/$defs/Color"
        },
        "tooltip": {
          "type": "string"
        },
        "route": {
          "type": "string"
        }
      },
      "additionalProperties": true,
      "description": "One destination in a navigation surface.\n\nKeys the runtime reads are declared with their types; a document may carry\nfurther keys, which the runtime ignores rather than refusing — an item shape\nthat rejected an unknown key would break every document that carries one for\nits own bookkeeping."
    },
    "accessibleWrapper": {
      "type": "object",
      "description": "Wraps a child with accessibility annotations (ARIA-style). See [`13_Accessibility.md`](13_Accessibility.md) for the full property set.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "accessibleWrapper"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "announceNavigation": {
          "description": "Announce navigation changes through the screen reader.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Announce navigation changes through the screen reader."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "announceOnChange": {
          "description": "Announce the watched value whenever it changes.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Announce the watched value whenever it changes."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "autoFocus": {
          "description": "Move focus into this subtree when it appears.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Move focus into this subtree when it appears."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "focusGroup": {
          "type": "string",
          "description": "Name of the focus group this subtree belongs to. Traversal moves through\none group before leaving it, so the name is what ties siblings together.\n"
        },
        "focusOrder": {
          "description": "Position of this subtree in the focus order; lower comes first.",
          "anyOf": [
            {
              "type": "number",
              "description": "Position of this subtree in the focus order; lower comes first."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "liveRegion": {
          "description": "Announce updates inside the subtree without moving focus to it, at the\nnamed urgency — `polite` waits for a pause, `assertive` interrupts,\n`status` and `alert` carry the matching ARIA roles.\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "polite",
                "assertive",
                "status",
                "alert"
              ],
              "description": "Announce updates inside the subtree without moving focus to it, at the\nnamed urgency — `polite` waits for a pause, `assertive` interrupts,\n`status` and `alert` carry the matching ARIA roles.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "navigationMessage": {
          "type": "string",
          "description": "Message announced when navigation reaches this subtree."
        },
        "watchPath": {
          "type": "string",
          "description": "State path whose changes are announced (with `announceOnChange`)."
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "accessibility": {
          "description": "`{ label, hint, role, live }` — mirrors §13.",
          "anyOf": [
            {
              "type": "object",
              "description": "`{ label, hint, role, live }` — mirrors §13.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "accordion": {
      "type": "object",
      "description": "Sections that expand and collapse. `children` are the panels; each panel\ndeclares its own `title` (or a `header` widget) and body.\n\nComposing this from `conditional` + `inkWell` renders correctly and loses\ntwo things the author cannot add back: the expand/collapse transition, and\nthe accessibility state a screen reader announces (expanded / collapsed,\nand which control owns which region). A collapsed section that is merely\nabsent from the tree reads to assistive technology as content that does\nnot exist rather than content that is hidden.\n\n`collapsible` is an alias for the single-section case — one panel with\n`allowMultiple` irrelevant. It is the same widget, not a smaller one.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "accordion",
            "collapsible"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "panels": {
          "description": "`{ id, title?, header?, content, expanded?, enabled? }` entries. `header` (a Widget) wins over `title` when both are set.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "description": "object",
                "anyOf": [
                  {
                    "type": "object",
                    "description": "object",
                    "additionalProperties": true
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "allowMultiple": {
          "description": "Allow more than one panel open at once. False collapses the previously open panel.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Allow more than one panel open at once. False collapses the previously open panel."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "expandedIds": {
          "description": "Two-way bound list of open panel ids. Binding it makes expansion state addressable from actions.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "bordered": {
          "description": "Draw dividers between panels.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Draw dividers between panels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "icon": {
          "description": "Expand indicator. Rotates on toggle.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Expand indicator. Rotates on toggle."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onChange": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "panels"
      ],
      "additionalProperties": true
    },
    "alertDialog": {
      "type": "object",
      "description": "Modal alert with an optional title, an optional body (string or widget),\nand a list of labeled action buttons. Dispatched via the `dialog` action\n(`{ \"type\": \"dialog\", \"dialog\": { \"type\": \"alertDialog\", ... } }`). The\nruntime renders it as a `Dialog` surface scoped to the current navigator\n(not the root navigator), so tapping an action pops only the dialog.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "alertDialog",
            "alert",
            "confirmDialog"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "insetPadding": {
          "description": "Space between the dialog and the screen edges.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space between the dialog and the screen edges."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "scrollable": {
          "description": "Let the body scroll when it does not fit.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Let the body scroll when it does not fit."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "shadowColor": {
          "description": "Colour of the drop shadow cast by the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the drop shadow cast by the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "Outline of the surface, as a shape descriptor.",
          "anyOf": [
            {
              "type": "object",
              "description": "Outline of the surface, as a shape descriptor.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "surfaceTintColor": {
          "description": "Elevation tint applied over the surface colour (M3).",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Elevation tint applied over the surface colour (M3)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "title": {
          "type": "string",
          "description": "Dialog title shown at the top."
        },
        "content": {
          "description": "Dialog body. When a string, rendered as a single `Text`. When a widget, rendered as-is.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/Widget"
            }
          ]
        },
        "dismissible": {
          "description": "Whether tapping outside the dialog closes it.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether tapping outside the dialog closes it."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onClose": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "actions": {
          "description": "Action buttons rendered in the dialog footer. Each entry:\n  - `label` (required): button text.\n  - `variant`: one of `text` / `elevated` / `outlined` / `filled`; default `text`.\n  - `primary`: highlights the button as the default action.\n  - `onTap`: the action fired on press. The runtime pops the dialog\n    BEFORE executing `onTap`, so handlers that trigger state changes or\n    navigation do not race with the dialog dismissal.\n",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "description": "object{ label: string, variant: string, primary: boolean, onTap: Action }",
                "anyOf": [
                  {
                    "type": "object",
                    "description": "object{ label: string, variant: string, primary: boolean, onTap: Action }",
                    "additionalProperties": true
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "alignment": {
          "description": "Where the dialog sits in the screen.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Where the dialog sits in the screen."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow depth of the dialog surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth of the dialog surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "align": {
      "type": "object",
      "description": "Aligns a single child at a specified alignment.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "align"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "heightFactor": {
          "description": "Size to this multiple of the child height.",
          "anyOf": [
            {
              "type": "number",
              "description": "Size to this multiple of the child height."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "widthFactor": {
          "description": "Size to this multiple of the child width.",
          "anyOf": [
            {
              "type": "number",
              "description": "Size to this multiple of the child width."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "alignment": {
          "description": "Alignment of the child within this widget's bounds. Defaults to `center` when omitted.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Alignment of the child within this widget's bounds. Defaults to `center` when omitted."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "animatedAlign": {
      "type": "object",
      "description": "Implicitly animates a child's `alignment` within its bounds. Use\nfor slide-from-edge / settle-to-position micro-interactions.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "animatedAlign"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "alignment": {
          "description": "Target alignment. Changes are tweened.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Target alignment. Changes are tweened."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "duration": {
          "description": "Animation duration in milliseconds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Animation duration in milliseconds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "curve": {
          "description": "Easing curve.",
          "anyOf": [
            {
              "$ref": "#/$defs/AnimationCurve",
              "description": "Easing curve."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onEnd": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type",
        "alignment"
      ],
      "additionalProperties": true
    },
    "animatedContainer": {
      "type": "object",
      "description": "Container that implicitly animates property changes. Whenever any\nof the size/decoration/alignment/padding fields change between\nbuilds the runtime tweens between the old and new values over\n`duration` using `curve`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "animatedContainer"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "foregroundDecoration": {
          "description": "Decoration painted over the child.",
          "anyOf": [
            {
              "$ref": "#/$defs/BoxDecoration",
              "description": "Decoration painted over the child."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "transform": {
          "description": "4×4 matrix applied to the box, in row-major order.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "string",
                    "pattern": "^\\{\\{.*\\}\\}$",
                    "description": "Binding expression."
                  },
                  {
                    "type": "object",
                    "properties": {
                      "value": {
                        "type": "number"
                      },
                      "unit": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "value"
                    ],
                    "description": "Dimension object: `{value, unit}`."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "transformAlignment": {
          "description": "Origin the transform is applied about.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Origin the transform is applied about."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "constraints": {
          "description": "Minimum / maximum width and height the box may take.",
          "anyOf": [
            {
              "type": "object",
              "description": "Minimum / maximum width and height the box may take.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "duration": {
          "description": "Animation duration in milliseconds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Animation duration in milliseconds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "curve": {
          "description": "Easing curve.",
          "anyOf": [
            {
              "$ref": "#/$defs/AnimationCurve",
              "description": "Easing curve."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Animated width.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Animated width."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "height": {
          "description": "Animated height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Animated height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "padding": {
          "description": "Animated inner padding.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Animated inner padding."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "margin": {
          "description": "Animated outer margin.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Animated outer margin."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "alignment": {
          "description": "Animated child alignment.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Animated child alignment."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "decoration": {
          "description": "Animated decoration (color/gradient/border/shadow/image).",
          "anyOf": [
            {
              "$ref": "#/$defs/BoxDecoration",
              "description": "Animated decoration (color/gradient/border/shadow/image)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onEnd": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "animatedDefaultTextStyle": {
      "type": "object",
      "description": "Implicitly animates the default `TextStyle` applied to descendant\n`text` / `richText` widgets. Each style field (color, fontSize,\nfontWeight, letterSpacing, lineHeight, …) tweens independently.\nUse for emphasis transitions (e.g., headline grows + bolds when\nselected).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "animatedDefaultTextStyle"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "style": {
          "description": "Target text style. Changes are tweened.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Target text style. Changes are tweened."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "duration": {
          "description": "Animation duration in milliseconds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Animation duration in milliseconds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "curve": {
          "description": "Easing curve.",
          "anyOf": [
            {
              "$ref": "#/$defs/AnimationCurve",
              "description": "Easing curve."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onEnd": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type",
        "style"
      ],
      "additionalProperties": true
    },
    "animatedOpacity": {
      "type": "object",
      "description": "Implicitly animates the child's `opacity` whenever it changes.\nUse for fade-in / fade-out micro-interactions without explicit\nanimation controllers.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "animatedOpacity"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "opacity": {
          "description": "Target opacity in `[0, 1]`. Changes are tweened.",
          "anyOf": [
            {
              "type": "number",
              "description": "Target opacity in `[0, 1]`. Changes are tweened."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "duration": {
          "description": "Animation duration in milliseconds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Animation duration in milliseconds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "curve": {
          "description": "Easing curve.",
          "anyOf": [
            {
              "$ref": "#/$defs/AnimationCurve",
              "description": "Easing curve."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onEnd": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type",
        "opacity"
      ],
      "additionalProperties": true
    },
    "animatedPositioned": {
      "type": "object",
      "description": "Implicitly animates a child's edge offsets inside a `stack`. Same\ncontract as `positioned` (top/right/bottom/left/width/height) but\nchanges are tweened. Only valid as a `stack` child.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "animatedPositioned"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "top": {
          "description": "Distance from the parent stack's top edge.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Distance from the parent stack's top edge."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "right": {
          "description": "Distance from the parent stack's right edge.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Distance from the parent stack's right edge."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "bottom": {
          "description": "Distance from the parent stack's bottom edge.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Distance from the parent stack's bottom edge."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "left": {
          "description": "Distance from the parent stack's left edge.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Distance from the parent stack's left edge."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Animated width.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Animated width."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "height": {
          "description": "Animated height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Animated height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "duration": {
          "description": "Animation duration in milliseconds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Animation duration in milliseconds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "curve": {
          "description": "Easing curve.",
          "anyOf": [
            {
              "$ref": "#/$defs/AnimationCurve",
              "description": "Easing curve."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onEnd": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "aspectRatio": {
      "type": "object",
      "description": "Sizes the child to a specific aspect ratio.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "aspectRatio"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "aspectRatio": {
          "description": "Width-to-height ratio (e.g., `1.5` for 3:2). Defaults to `1.0` (square) when omitted.",
          "anyOf": [
            {
              "type": "number",
              "default": 1.0,
              "description": "Width-to-height ratio (e.g., `1.5` for 3:2). Defaults to `1.0` (square) when omitted."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "avatar": {
      "type": "object",
      "description": "Circular widget for user images or initials.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "avatar"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "src": {
          "description": "Image source. Any `AssetRef` form; if absent or unresolvable, falls back to `label`.",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Image source. Any `AssetRef` form; if absent or unresolvable, falls back to `label`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Text label (typically initials)."
        },
        "text": {
          "type": "string",
          "description": "Legacy alias of `label`."
        },
        "size": {
          "description": "Diameter in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "default": 40,
              "description": "Diameter in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "color": {
          "description": "Background color when showing label.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Background color when showing label."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Background color when showing label."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundImage": {
          "description": "Image drawn behind the initials or icon.",
          "anyOf": [
            {
              "$ref": "#/$defs/BackgroundImage",
              "description": "Image drawn behind the initials or icon."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "foregroundColor": {
          "description": "Bar color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Bar color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "icon": {
          "description": "Icon shown. Any `IconRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Icon shown. Any `IconRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "radius": {
          "description": "Radius of the circle, in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "description": "Radius of the circle, in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "badge": {
      "type": "object",
      "description": "Small status indicator typically anchored to another widget.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "badge"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "isLabelVisible": {
          "description": "Whether the badge label is drawn at all.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Whether the badge label is drawn at all."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "smallSize": {
          "description": "Render the compact form — a smaller dot and label. The dimensions are\nthe M3 badge sizes, so this is a form switch, not a measurement.\n",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Render the compact form — a smaller dot and label. The dimensions are\nthe M3 badge sizes, so this is a form switch, not a measurement.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "offset": {
          "description": "Displacement from the anchor, as `{ dx, dy }`.",
          "anyOf": [
            {
              "type": "object",
              "description": "Displacement from the anchor, as `{ dx, dy }`.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Text content of the badge."
        },
        "color": {
          "description": "Badge background color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Badge background color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "alignment": {
          "description": "Where the badge sits against the child it decorates.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Where the badge sits against the child it decorates."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "textColor": {
          "description": "Default text color",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Default text color"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "banner": {
      "type": "object",
      "description": "Persistent message banner at the top of a section or page.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "banner"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "message": {
          "type": "string",
          "description": "Banner text."
        },
        "severity": {
          "description": "`info`, `success`, `warning`, `error`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "info",
                "success",
                "warning",
                "error"
              ],
              "default": "info",
              "description": "`info`, `success`, `warning`, `error`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "actions": {
          "description": "Action buttons `{ label, onTap }`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/BannerAction"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "onClose": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "message"
      ],
      "additionalProperties": true
    },
    "barcode": {
      "type": "object",
      "description": "Renders a 1-D barcode for `value`.\n\nLike `qrCode`, the bar pattern is computed rather than laid out. Unlike it,\neach `format` constrains what `value` may contain — an EAN-13 payload is\nthirteen digits with a check digit, not arbitrary text.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "barcode"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "value": {
          "description": "Encoded payload. MUST satisfy the chosen `format`; a runtime reports a violation rather than rendering an unscannable image.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "format": {
          "description": "Symbology. `code128` accepts the widest payload and is the safe default.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "code128",
                "code39",
                "ean13",
                "ean8",
                "upcA",
                "upcE",
                "itf",
                "codabar"
              ],
              "default": "code128",
              "description": "Symbology. `code128` accepts the widest payload and is the safe default."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "width": {
          "description": "Rendered width in logical pixels. Omitted means intrinsic — the natural width for the module count, which is what stays scannable.",
          "anyOf": [
            {
              "type": "number",
              "description": "Rendered width in logical pixels. Omitted means intrinsic — the natural width for the module count, which is what stays scannable."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Bar height.",
          "anyOf": [
            {
              "type": "number",
              "default": 80,
              "description": "Bar height."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "displayValue": {
          "description": "Print the payload under the bars.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Print the payload under the bars."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "foregroundColor": {
          "description": "Bar color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Bar color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Background color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Background color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "value"
      ],
      "additionalProperties": true
    },
    "baseline": {
      "type": "object",
      "description": "Aligns a child by its baseline.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "baseline"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "baseline": {
          "description": "Distance from the top to the child's baseline.",
          "anyOf": [
            {
              "type": "number",
              "description": "Distance from the top to the child's baseline."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "baselineType": {
          "type": "string",
          "default": "alphabetic",
          "description": "`alphabetic` or `ideographic`."
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type",
        "baseline"
      ],
      "additionalProperties": true
    },
    "bottomNavigation": {
      "type": "object",
      "description": "Bottom navigation bar. Each item's text field is `label`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "bottomNavigation",
            "bottomNav",
            "bottomnavigationbar"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "fixedColor": {
          "description": "Colour of the selected item when the bar is in fixed mode.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the selected item when the bar is in fixed mode."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "selectedFontSize": {
          "description": "Label size for the selected item.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Label size for the selected item."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "selectedIconTheme": {
          "description": "Icon size / colour for the selected item.",
          "anyOf": [
            {
              "type": "object",
              "description": "Icon size / colour for the selected item.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "selectedItemColor": {
          "description": "Colour of the selected item.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the selected item."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "selectedLabelStyle": {
          "description": "Text style for the selected label.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Text style for the selected label."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "showSelectedLabels": {
          "description": "Whether the selected item shows its label.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Whether the selected item shows its label."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showUnselectedLabels": {
          "description": "Whether unselected items show their labels.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Whether unselected items show their labels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "unselectedFontSize": {
          "description": "Label size for the items that are not selected.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Label size for the items that are not selected."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "unselectedIconTheme": {
          "description": "Icon size / colour for the items that are not selected.",
          "anyOf": [
            {
              "type": "object",
              "description": "Icon size / colour for the items that are not selected.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "unselectedItemColor": {
          "description": "Colour of the items that are not selected.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the items that are not selected."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "unselectedLabelStyle": {
          "description": "Text style for the labels that are not selected.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Text style for the labels that are not selected."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "enableFeedback": {
          "description": "Play the platform tap sound / haptic on activation.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Play the platform tap sound / haptic on activation."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "iconSize": {
          "description": "Rendered size of the icon.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Rendered size of the icon."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "selectedIndex": {
          "description": "Currently selected index. Defaults to 0 when omitted.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "items": {
          "description": "`{ icon, label, route? }` entries.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/NavItem"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow depth of the bar.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth of the bar."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "currentIndex": {
          "description": "Measured from real documents (2026-08-04): a zero-based tab index, or a binding to one — not a length.",
          "anyOf": [
            {
              "type": "number",
              "description": "Measured from real documents (2026-08-04): a zero-based tab index, or a binding to one — not a length."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        }
      },
      "required": [
        "type",
        "items"
      ],
      "additionalProperties": true
    },
    "bottomSheet": {
      "type": "object",
      "description": "Modal bottom sheet with swipeable handle.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "bottomSheet"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "dragHandleColor": {
          "description": "Colour of the drag handle.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the drag handle."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "dragHandleSize": {
          "description": "Size of the drag handle, as `{ width, height }`.",
          "anyOf": [
            {
              "type": "object",
              "description": "Size of the drag handle, as `{ width, height }`.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onClosing": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "showDragHandle": {
          "description": "Draw the drag handle at the top of the sheet.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Draw the drag handle at the top of the sheet."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "constraints": {
          "description": "Minimum / maximum width and height the box may take.",
          "anyOf": [
            {
              "type": "object",
              "description": "Minimum / maximum width and height the box may take.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shadowColor": {
          "description": "Colour of the drop shadow cast by the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the drop shadow cast by the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "isDismissible": {
          "description": "Allow dismiss by tapping scrim.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Allow dismiss by tapping scrim."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "enableDrag": {
          "description": "Allow drag to dismiss.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Allow drag to dismiss."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "backgroundColor": {
          "description": "Sheet background.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Sheet background."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "`{ type: \"rounded\", radius: { top: 16 } }`.",
          "anyOf": [
            {
              "type": "object",
              "description": "`{ type: \"rounded\", radius: { top: 16 } }`.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onClose": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow depth of the sheet surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth of the sheet surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "box": {
      "type": "object",
      "description": "Generic single-child container that applies size, padding, margin, and a\ndecoration (background color, border, shadow, gradient, borderRadius). Most\ncommonly used as a styled wrapper around any child widget.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "box",
            "container",
            "constrained",
            "decoratedBox",
            "constrainedBox"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "constraints": {
          "description": "Minimum / maximum width and height the box may take.",
          "anyOf": [
            {
              "type": "object",
              "description": "Minimum / maximum width and height the box may take.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Fixed width — a number of logical pixels, or a `{value, unit}` object. When omitted, the box shrinks to its child's width.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed width — a number of logical pixels, or a `{value, unit}` object. When omitted, the box shrinks to its child's width."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "height": {
          "description": "Fixed height — a number of logical pixels, or a `{value, unit}` object. When omitted, the box shrinks to its child's height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed height — a number of logical pixels, or a `{value, unit}` object. When omitted, the box shrinks to its child's height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "minWidth": {
          "description": "Minimum width constraint. Honored independently of `width`; a child wider than `minWidth` is allowed.",
          "anyOf": [
            {
              "type": "number",
              "description": "Minimum width constraint. Honored independently of `width`; a child wider than `minWidth` is allowed."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "maxWidth": {
          "description": "Maximum width constraint. Caps the child's width regardless of intrinsic size.",
          "anyOf": [
            {
              "type": "number",
              "description": "Maximum width constraint. Caps the child's width regardless of intrinsic size."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "minHeight": {
          "description": "Minimum height constraint.",
          "anyOf": [
            {
              "type": "number",
              "description": "Minimum height constraint."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "maxHeight": {
          "description": "Maximum height constraint.",
          "anyOf": [
            {
              "type": "number",
              "description": "Maximum height constraint."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "padding": {
          "description": "Space inside the box, between the border and the child. Accepts an\nM3 spacing token shorthand (`xxs` / `xs` / `sm` / `md` / `lg` /\n`xl` / `2xl` / `3xl` / `4xl`, or any custom slot in\n`theme.spacing`) — resolved through `theme.spacing.<token>` to a\nuniform inset — the `{token}` object form, or any EdgeInsets spelling.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/BoxSpacing",
              "description": "Space inside the box, between the border and the child. Accepts an\nM3 spacing token shorthand (`xxs` / `xs` / `sm` / `md` / `lg` /\n`xl` / `2xl` / `3xl` / `4xl`, or any custom slot in\n`theme.spacing`) — resolved through `theme.spacing.<token>` to a\nuniform inset — the `{token}` object form, or any EdgeInsets spelling.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "margin": {
          "description": "Space outside the box, between the box and its parent. Takes the same\nspellings as `padding`, token included — the widget resolves both\nthrough one helper, so declaring less here only made the token\nunauthorable, never unrenderable.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/BoxSpacing",
              "description": "Space outside the box, between the box and its parent. Takes the same\nspellings as `padding`, token included — the widget resolves both\nthrough one helper, so declaring less here only made the token\nunauthorable, never unrenderable.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "alignment": {
          "description": "Alignment of the child inside the box when the box is larger\nthan the child. See the `Alignment` primitive for the canonical\n9-token directional set and the `{x, y}` numeric form.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Alignment of the child inside the box when the box is larger\nthan the child. See the `Alignment` primitive for the canonical\n9-token directional set and the `{x, y}` numeric form.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "color": {
          "description": "Solid background color. Shorthand for `decoration.color`; mutually exclusive with `decoration`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Solid background color. Shorthand for `decoration.color`; mutually exclusive with `decoration`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Solid background color. Shorthand for `decoration.color`; mutually exclusive with `decoration`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "decoration": {
          "description": "Visual decoration. Mutually exclusive with the top-level `color`\nshorthand — when both are present `decoration` wins. See the\n`BoxDecoration` $def for the full property set: `color`,\n`gradient`, `image`, `border`, `borderRadius`, `boxShadow`,\n`shape`, `backdropBlur`.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/BoxDecoration",
              "description": "Visual decoration. Mutually exclusive with the top-level `color`\nshorthand — when both are present `decoration` wins. See the\n`BoxDecoration` $def for the full property set: `color`,\n`gradient`, `image`, `border`, `borderRadius`, `boxShadow`,\n`shape`, `backdropBlur`.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "breadcrumb": {
      "type": "object",
      "description": "Trail of ancestor locations ending at the current one. Composable from\n`linear` + `text` + navigate actions, but composing it loses the landmark\nrole assistive technology uses to skip to it, and every author re-decides\nwhere the separator goes and whether the last item is a link (it is not).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "breadcrumb"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "items": {
          "description": "`{ label, route?, params?, icon? }` in ancestor-first order. The final entry is the current location and MUST NOT be rendered as a link even if it carries a `route`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "description": "object",
                "anyOf": [
                  {
                    "type": "object",
                    "description": "object",
                    "additionalProperties": true
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "separator": {
          "type": "string",
          "default": "/",
          "description": "Glyph between entries. Decorative — hidden from assistive technology."
        },
        "maxItems": {
          "description": "Collapse the middle of a long trail behind an overflow control, keeping the first and last.",
          "anyOf": [
            {
              "type": "number",
              "description": "Collapse the middle of a long trail behind an overflow control, keeping the first and last."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "onClick": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "items"
      ],
      "additionalProperties": true
    },
    "button": {
      "type": "object",
      "description": "Interactive button. The canonical label field is `label`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "button"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "fullWidth": {
          "description": "Stretch the button to the available width.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Stretch the button to the available width."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "iconPosition": {
          "type": "string",
          "description": "Which side the icon sits on: `start` or `end`."
        },
        "ariaLabel": {
          "type": "string",
          "description": "Label announced by a screen reader in place of the content."
        },
        "borderWidth": {
          "description": "Thickness of the outline.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Thickness of the outline."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "size": {
          "description": "Rendered size: the named steps `small` / `medium` / `large`, which\nthe runtime's padding, icon size and spinner size are written\nagainst. It was declared as a `Dimension` here — a number, which the\nbutton reads as no size at all and falls back to `medium`, while the\nnamed steps it does honour were rejected before the document loaded.\nA number is still accepted so that documents carrying one keep\nloading; the button renders at `medium`.\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "small",
                "medium",
                "large"
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            },
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Button text."
        },
        "text": {
          "type": "string",
          "description": "Legacy alias of `label`."
        },
        "variant": {
          "description": "`elevated`, `filled`, `outlined`, `text`, `icon`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "elevated",
                "filled",
                "outlined",
                "text",
                "icon"
              ],
              "default": "elevated",
              "description": "`elevated`, `filled`, `outlined`, `text`, `icon`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "style": {
          "description": "Legacy alias of `variant`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "elevated",
                "filled",
                "outlined",
                "text",
                "icon"
              ],
              "default": "elevated",
              "description": "`elevated`, `filled`, `outlined`, `text`, `icon`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "elevation": {
          "description": "Shadow elevation. Accepts either a numeric dp value or an M3\nelevation token shorthand (`level0` … `level5`) that resolves\nthrough `theme.elevation.<token>.shadow`. Honored only by the\n`elevated` variant; ignored by `filled`/`outlined`/`text`/`icon`.\n",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "icon": {
          "description": "Optional leading icon. Any `IconRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Optional leading icon. Any `IconRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "enabled": {
          "description": "Whether the button is interactive.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the button is interactive."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onDoubleTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onLongPress": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "borderColor": {
          "description": "Pad border",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Pad border"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "foregroundColor": {
          "description": "Bar color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Bar color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "loading": {
          "description": "Show a progress indicator in place of the label and refuse taps.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Show a progress indicator in place of the label and refuse taps."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onSubmit": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "submit": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "calendar": {
      "type": "object",
      "description": "Calendar view for date selection and event display.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "calendar"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "eventColor": {
          "description": "Colour of the marker drawn on a day that has events.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the marker drawn on a day that has events."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "firstDayOfWeek": {
          "description": "Weekday the grid starts on, `0` = Sunday.",
          "anyOf": [
            {
              "type": "number",
              "description": "Weekday the grid starts on, `0` = Sunday."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "onMonthChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "primaryColor": {
          "description": "Colour of the selection and header accents.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the selection and header accents."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "showHeader": {
          "description": "Show the month header with its navigation controls.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Show the month header with its navigation controls."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showWeekNumbers": {
          "description": "Show the ISO week number beside each row.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Show the ISO week number beside each row."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "todayColor": {
          "description": "Colour marking the current day.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour marking the current day."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "height": {
          "description": "Fixed height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "selectedDate": {
          "description": "Currently selected date (ISO 8601 string, or binding to a string).",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "events": {
          "description": "Array of `{ date, title, color? }`. Declared `binding` before — a\ncalendar could only take its events through a state path, never as the\nlist the description names.\n",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "description": "object",
                "anyOf": [
                  {
                    "type": "object",
                    "description": "object",
                    "additionalProperties": true
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "firstDate": {
          "type": "string",
          "description": "Earliest selectable date (ISO 8601)."
        },
        "lastDate": {
          "type": "string",
          "description": "Latest selectable date (ISO 8601)."
        },
        "view": {
          "description": "Calendar display granularity.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "month",
                "week",
                "day"
              ],
              "default": "month",
              "description": "Calendar display granularity."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "selectedColor": {
          "description": "Foreground color (icon + label) for the selected item.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Foreground color (icon + label) for the selected item."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "canvas": {
      "type": "object",
      "description": "General-purpose vector drawing via an ordered command array. All numeric and color properties support binding expressions, enabling data-driven graphics (progress rings, custom indicators, composable chart primitives).\nbackgroundColor:\n  type: Color\n  description: \"Legacy alias of `color`.\"",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "canvas"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "width": {
          "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "card": {
      "type": "object",
      "description": "Elevated single-child container.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "card"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "semanticContainer": {
          "description": "Treat the card and its contents as one node in the accessibility tree.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Treat the card and its contents as one node in the accessibility tree."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "shadowColor": {
          "description": "Colour of the drop shadow cast by the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the drop shadow cast by the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "surfaceTintColor": {
          "description": "Elevation tint applied over the surface colour (M3).",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Elevation tint applied over the surface colour (M3)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow elevation. Accepts either a numeric dp value or an M3\nelevation token shorthand (`level0` … `level5`) which resolves\nthrough `theme.elevation.<token>.shadow`.\n",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "margin": {
          "description": "Outer margin.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Outer margin."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "Shape of the card surface. M3 shape token shorthand (`extraSmall` /\n`small` / `medium` / `large` / `extraLarge` / `full` / `none`)\nresolves through `theme.shape.<token>`. Object form\n`{ type: \"rounded\", radius: 12 }` is also accepted.\n",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "object",
              "additionalProperties": true
            }
          ]
        },
        "color": {
          "description": "Card surface color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Card surface color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Card surface color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "carousel": {
      "type": "object",
      "description": "Horizontally scrolling browser with optional partial-viewport\nframing (cover-flow / album-shelf style). Distinct from `pageView`\n(which always snaps a full viewport per page) — `carousel` lets\nauthors set `viewportFraction` < 1 so adjacent items peek in,\nsignalling \"more to scroll\". Used for content shelves (book\ncovers, album browsers, magazine front pages).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "carousel"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "items": {
          "description": "Array source. Required when `children` is omitted.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "itemTemplate": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "description": "Static slides. Mutually exclusive with `items` + `itemTemplate`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "scrollDirection": {
          "description": "Scroll axis.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "horizontal",
                "vertical"
              ],
              "default": "horizontal",
              "description": "Scroll axis."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "viewportFraction": {
          "description": "Each slide's width as a fraction of the carousel width.\n`1.0` = one item fills the viewport (matches `pageView`);\n`0.85` leaves both neighbours peeking; `0.6` shows three items.\n",
          "anyOf": [
            {
              "type": "number",
              "default": 1.0,
              "description": "Each slide's width as a fraction of the carousel width.\n`1.0` = one item fills the viewport (matches `pageView`);\n`0.85` leaves both neighbours peeking; `0.6` shows three items.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "loop": {
          "description": "When true the list wraps around — last → first → last.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "When true the list wraps around — last → first → last."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "autoPlay": {
          "description": "When set, advances one item every `autoPlay` ms. Authors\ntypically pair with `loop: true`.\n",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "initialIndex": {
          "description": "Index of the slide rendered first.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Index of the slide rendered first."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "transition": {
          "description": "Per-slide transition. `slide` is the default linear scroll;\n`fade` cross-fades between slides at the same position;\n`coverflow` rotates and depth-shifts neighbours; `depth`\nscales/translates Z-axis. `coverflow`/`depth` need a\nperspective-aware compositor — runtimes without it fall\nback to `slide`.\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "slide",
                "fade",
                "coverflow",
                "depth"
              ],
              "default": "slide",
              "description": "Per-slide transition. `slide` is the default linear scroll;\n`fade` cross-fades between slides at the same position;\n`coverflow` rotates and depth-shifts neighbours; `depth`\nscales/translates Z-axis. `coverflow`/`depth` need a\nperspective-aware compositor — runtimes without it fall\nback to `slide`.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "indicatorPosition": {
          "description": "Page indicator dot bar.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "bottom",
                "top",
                "none"
              ],
              "default": "bottom",
              "description": "Page indicator dot bar."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "onPageChanged": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "center": {
      "type": "object",
      "description": "Centers a single child within available space.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "center"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "heightFactor": {
          "description": "Size to this multiple of the child height.",
          "anyOf": [
            {
              "type": "number",
              "description": "Size to this multiple of the child height."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "widthFactor": {
          "description": "Size to this multiple of the child width.",
          "anyOf": [
            {
              "type": "number",
              "description": "Size to this multiple of the child width."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "chart": {
      "type": "object",
      "description": "Data visualization widget with multiple chart types.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "chart"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "colors": {
          "description": "Series colours, applied in order.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Color"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "primaryColor": {
          "description": "Colour of the first series when `colors` is not given.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the first series when `colors` is not given."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "showGrid": {
          "description": "Draw the background grid.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Draw the background grid."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showLabels": {
          "description": "Draw the axis labels.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Draw the axis labels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showLegend": {
          "description": "Draw the series legend.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Draw the series legend."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "labelColor": {
          "description": "Colour applied to the label text.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour applied to the label text."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "chartType": {
          "description": "Chart visualization style.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "line",
                "bar",
                "pie",
                "donut",
                "scatter",
                "area",
                "radar",
                "polar",
                "bubble"
              ],
              "description": "Chart visualization style."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "data": {
          "description": "Canonical `{ labels: string[], datasets: Dataset[] }`, or legacy `[{label, value}]` array form.",
          "anyOf": [
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "options": {
          "description": "Rendering options",
          "anyOf": [
            {
              "type": "object",
              "description": "Rendering options",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Fixed width (logical pixels)",
          "anyOf": [
            {
              "type": "number",
              "description": "Fixed width (logical pixels)"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Fixed height (logical pixels)",
          "anyOf": [
            {
              "type": "number",
              "description": "Fixed height (logical pixels)"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "gridColor": {
          "description": "Grid color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Grid color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "title": {
          "type": "string",
          "description": "Dialog title shown at the top."
        }
      },
      "required": [
        "type",
        "chartType",
        "data"
      ],
      "additionalProperties": true
    },
    "checkbox": {
      "type": "object",
      "description": "Boolean checkbox. Shared rows per §2.6.0; `value` is `boolean`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "checkbox"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Optional label."
        },
        "tristate": {
          "description": "Allow the third, indeterminate state. With `tristate: true` the value\ncycles unchecked → checked → indeterminate, and `null` is a value the\nbinding carries. A parent row summarising children that are partly\nselected has no other way to say so — `false` would claim none are.\n",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Allow the third, indeterminate state. With `tristate: true` the value\ncycles unchecked → checked → indeterminate, and `null` is a value the\nbinding carries. A parent row summarising children that are partly\nselected has no other way to say so — `false` would claim none are.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "checkboxGroup": {
      "type": "object",
      "description": "Multi-selection checkbox group. Shared rows per §2.6.0; `binding` holds an `array` of selected values (top-level, not per-option).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "checkboxGroup"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        },
        "options": {
          "description": "`{ value, label }` entries.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "items": {
          "description": "Legacy alias of `options`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "orientation": {
          "type": "string",
          "default": "vertical",
          "description": "`vertical` or `horizontal`."
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "direction": {
          "type": "string",
          "description": "`horizontal` or `vertical`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "chip": {
      "type": "object",
      "description": "Compact element representing an attribute, action, or filter.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "chip",
            "tag"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "deleteIcon": {
          "description": "Icon for the delete control.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Icon for the delete control."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onDeleted": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "side": {
          "description": "Outline drawn around the chip, as `{ color, width }`.",
          "anyOf": [
            {
              "type": "object",
              "description": "Outline drawn around the chip, as `{ color, width }`.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "padding": {
          "description": "Space inside the surface, around its content.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space inside the surface, around its content."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shadowColor": {
          "description": "Colour of the drop shadow cast by the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the drop shadow cast by the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "Outline of the surface, as a shape descriptor.",
          "anyOf": [
            {
              "type": "object",
              "description": "Outline of the surface, as a shape descriptor.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Display text."
        },
        "avatar": {
          "$ref": "#/$defs/Widget"
        },
        "selected": {
          "description": "Selected state.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Selected state."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "variant": {
          "type": "string",
          "default": "filled",
          "description": "`filled` or `outlined`."
        },
        "onDelete": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow depth of the chip.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth of the chip."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "labelStyle": {
          "description": "Default text style for item labels (selected and unselected). Per-item override via NavItem.style.labelStyle.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Default text style for item labels (selected and unselected). Per-item override via NavItem.style.labelStyle."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onPressed": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "label"
      ],
      "additionalProperties": true
    },
    "clipOval": {
      "type": "object",
      "description": "Clips a child to an oval.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "clipOval"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "clipRRect": {
      "type": "object",
      "description": "Clips a child to a rounded rectangle.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "clipRRect"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "borderRadius": {
          "description": "Corner radius — uniform number or directional `{topStart, topEnd,\nbottomStart, bottomEnd, all}` per the `BorderRadius` primitive.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/BorderRadius",
              "description": "Corner radius — uniform number or directional `{topStart, topEnd,\nbottomStart, bottomEnd, all}` per the `BorderRadius` primitive.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "codeEditor": {
      "type": "object",
      "description": "Syntax-highlighted code editor.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "codeEditor",
            "code"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "lineNumberColor": {
          "description": "Colour of the line-number gutter.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the line-number gutter."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "copyable": {
          "description": "Offer a copy-to-clipboard control. Useful with `readOnly`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Offer a copy-to-clipboard control. Useful with `readOnly`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "expandAll": {
          "description": "Expand every collapsible region on load. Chiefly for structured languages such as JSON.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Expand every collapsible region on load. Chiefly for structured languages such as JSON."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "code": {
          "description": "Code content — literal string or binding. Required unless `binding` supplies the content.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "language": {
          "description": "Syntax highlighting language.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "plaintext",
                "javascript",
                "typescript",
                "dart",
                "python",
                "java",
                "kotlin",
                "swift",
                "go",
                "rust",
                "c",
                "cpp",
                "csharp",
                "ruby",
                "php",
                "sql",
                "json",
                "yaml",
                "xml",
                "html",
                "css",
                "markdown",
                "shell"
              ],
              "default": "plaintext",
              "description": "Syntax highlighting language."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "theme": {
          "description": "Editor color theme.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "vsLight",
                "vsDark",
                "monokai",
                "solarizedLight",
                "solarizedDark",
                "github",
                "dracula",
                "light",
                "dark"
              ],
              "default": "vsDark",
              "description": "Editor color theme."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "readOnly": {
          "description": "Disable editing (syntax highlighting still applies)",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Disable editing (syntax highlighting still applies)"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showLineNumbers": {
          "description": "Show gutter line numbers",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Show gutter line numbers"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "fontSize": {
          "description": "Font size (logical px)",
          "anyOf": [
            {
              "type": "number",
              "description": "Font size (logical px)"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "lineHeight": {
          "description": "Line height multiplier",
          "anyOf": [
            {
              "type": "number",
              "description": "Line height multiplier"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "tabSize": {
          "description": "Tab width in spaces",
          "anyOf": [
            {
              "type": "number",
              "default": 2,
              "description": "Tab width in spaces"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "width": {
          "description": "Widget width",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget width"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Widget height",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget height"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "backgroundColor": {
          "description": "Override theme background",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Override theme background"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "textColor": {
          "description": "Override theme default text color",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Override theme default text color"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "colorPicker": {
      "type": "object",
      "description": "Color selection. Shared rows per §2.6.0; `value` is a hex color string.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "colorPicker"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        },
        "showAlpha": {
          "description": "Enable alpha channel.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Enable alpha channel."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showLabel": {
          "description": "Show hex label.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Show hex label."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "pickerType": {
          "description": "`wheel` (a continuous hue strip), `palette` (preset swatches), or\n`both`. The default is `palette` — what every picker has drawn since it\nshipped. The registry said `wheel` while no implementation did that, and\nchanging the default would change screens that never named it.\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "wheel",
                "palette",
                "both"
              ],
              "default": "palette",
              "description": "`wheel` (a continuous hue strip), `palette` (preset swatches), or\n`both`. The default is `palette` — what every picker has drawn since it\nshipped. The registry said `wheel` while no implementation did that, and\nchanging the default would change screens that never named it.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "enableHistory": {
          "description": "Show recent colors.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Show recent colors."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "combobox": {
      "type": "object",
      "description": "Text entry with suggestions. Shared rows per §2.6.0; `binding` holds the\n**typed string**, not an option id.\n\nThe defining property is that a value outside `options` is legal — that\nis what separates it from `select` / `multiSelect`, where the option list\nis the domain. Composing it from `textInput` + a list loses focus\nownership: the list must not steal focus from the field, arrow keys must\nmove a highlight without moving the caret, and Escape must close the list\nwithout clearing the text. Those are one widget's job.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "combobox",
            "autocomplete"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        },
        "options": {
          "description": "Suggestions. Optional — a combobox with none is a plain text field until `onSearch` supplies them.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "items": {
          "description": "Legacy alias of `options`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "allowCustom": {
          "description": "Accept a value not present in `options`. Setting it false makes this a searchable `select`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Accept a value not present in `options`. Setting it false makes this a searchable `select`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onSearch": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "minChars": {
          "description": "Characters typed before suggestions are requested or shown.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Characters typed before suggestions are requested or shown."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "debounceMs": {
          "description": "Idle time before `onSearch` fires.",
          "anyOf": [
            {
              "type": "number",
              "default": 250,
              "description": "Idle time before `onSearch` fires."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "placeholder": {
          "type": "string",
          "description": "Placeholder text."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "conditional": {
      "type": "object",
      "description": "Renders different branches based on an expression. Two forms are supported: then/else and multi-branch (`switch`/`cases`).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "conditional"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "condition": {
          "description": "Boolean literal or boolean-producing expression (then/else form).",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "then": {
          "$ref": "#/$defs/Widget"
        },
        "child": {
          "$ref": "#/$defs/Widget",
          "description": "Legacy alias of `then`."
        },
        "widget": {
          "$ref": "#/$defs/Widget",
          "description": "Legacy alias of `then`."
        },
        "else": {
          "$ref": "#/$defs/Widget"
        },
        "switch": {
          "type": "string",
          "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
          "description": "Expression whose value is compared against each case (switch form)."
        },
        "cases": {
          "description": "List of `{ value, child }` entries (switch form).",
          "anyOf": [
            {
              "type": "array",
              "description": "List of `{ value, child }` entries (switch form)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "default": {
          "$ref": "#/$defs/Widget"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "contextMenu": {
      "type": "object",
      "description": "Menu raised by secondary activation on its `child` — right-click, long\npress, or the platform's context key.\n\nThis cannot be composed: the gesture that raises it is platform-specific\n(and on touch competes with scroll and text selection), and the menu must\nappear at the pointer rather than against the child's box. A\n`gestureDetector` wrapping a `popupMenuButton` gets neither.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "contextMenu"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "items": {
          "description": "`{ key, label, icon?, enabled?, divider?, children? }`. `divider: true` renders a separator and carries no action.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "description": "object",
                "anyOf": [
                  {
                    "type": "object",
                    "description": "object",
                    "additionalProperties": true
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "enabled": {
          "description": "When false the child renders normally and the gesture falls through to the platform.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "When false the child renders normally and the gesture falls through to the platform."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        },
        "onSelect": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "items"
      ],
      "additionalProperties": true
    },
    "customDialog": {
      "type": "object",
      "description": "Dialog with arbitrary widget content.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "customDialog",
            "modal",
            "dialog"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "actions": {
          "description": "Action row along the bottom of the dialog.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "insetPadding": {
          "description": "Space between the dialog and the screen edges.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space between the dialog and the screen edges."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "shadowColor": {
          "description": "Colour of the drop shadow cast by the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the drop shadow cast by the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "Outline of the surface, as a shape descriptor.",
          "anyOf": [
            {
              "type": "object",
              "description": "Outline of the surface, as a shape descriptor.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "surfaceTintColor": {
          "description": "Elevation tint applied over the surface colour (M3).",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Elevation tint applied over the surface colour (M3)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "dismissible": {
          "description": "Whether tapping outside dismisses.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether tapping outside dismisses."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onClose": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "alignment": {
          "description": "Where the dialog sits in the screen.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Where the dialog sits in the screen."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "content": {
          "type": "string",
          "description": "Body text. Use `children` when the body is more than a string."
        },
        "elevation": {
          "description": "Shadow depth of the dialog surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth of the dialog surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "title": {
          "type": "string",
          "description": "Dialog title shown at the top."
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "dashboard": {
      "type": "object",
      "description": "Renders a compact dashboard tile — the widget equivalent of the `ApplicationDefinition.dashboard` field (see [`11_Bundle_Metadata.md`](11_Bundle_Metadata.md) §11). Used to embed a dashboard-mode tile inside a page (launcher / app grid scenarios).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "dashboard"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "content": {
          "$ref": "#/$defs/Widget"
        },
        "refreshInterval": {
          "description": "Auto-refresh period in milliseconds. Omit for static tiles.",
          "anyOf": [
            {
              "type": "number",
              "description": "Auto-refresh period in milliseconds. Omit for static tiles."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "onTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "dataTable": {
      "type": "object",
      "description": "Material-style sortable, selectable data table bound to a row array.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "dataTable",
            "dataGrid"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "editable": {
          "description": "Allow in-place cell editing. Edits report through `onCellEdit`; the widget does not mutate `data` on its own.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Allow in-place cell editing. Edits report through `onCellEdit`; the widget does not mutate `data` on its own."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "filterable": {
          "description": "Offer per-column filter controls.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Offer per-column filter controls."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "resizableColumns": {
          "description": "Allow dragging column edges to resize.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Allow dragging column edges to resize."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "virtualScroll": {
          "description": "Render only visible rows. Requires a fixed `rowHeight` — a virtualised list cannot measure rows it has not built.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Render only visible rows. Requires a fixed `rowHeight` — a virtualised list cannot measure rows it has not built."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "rowHeight": {
          "description": "Fixed row height in logical pixels. Required when `virtualScroll` is true.",
          "anyOf": [
            {
              "type": "number",
              "description": "Fixed row height in logical pixels. Required when `virtualScroll` is true."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "columns": {
          "description": "Column definitions",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Column"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "rows": {
          "description": "Array of row objects",
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "description": "object",
                    "anyOf": [
                      {
                        "type": "object",
                        "description": "object",
                        "additionalProperties": true
                      },
                      {
                        "$ref": "#/$defs/Binding",
                        "description": "Binding expression resolving to this value."
                      }
                    ]
                  }
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression resolving to an array."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "selectable": {
          "description": "Whether rows are selectable",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Whether rows are selectable"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "sortColumn": {
          "type": "string",
          "description": "Current sort column key. Declared `binding` before, which accepted\n`\"{{sortColumn}}\"` and rejected the key itself — a table sorted by a\nfixed column could not say so.\n"
        },
        "sortAscending": {
          "description": "Current sort direction. `true` is ascending. Declared `binding` before,\nso writing the direction as the boolean the runtime reads was rejected\nat load.\n",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Current sort direction. `true` is ascending. Declared `binding` before,\nso writing the direction as the boolean the runtime reads was rejected\nat load.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onSort": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onCellEdit": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onRowTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "columns",
        "rows"
      ],
      "additionalProperties": true
    },
    "dateField": {
      "type": "object",
      "description": "Date input with calendar/text entry modes. Shared rows per §2.6.0; `value` is an ISO date string.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "dateField"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "errorText": {
          "type": "string",
          "description": "Error message shown under the field. Overrides validation output."
        },
        "label": {
          "type": "string",
          "description": "Field label."
        },
        "format": {
          "type": "string",
          "default": "yyyy-MM-dd",
          "description": "Display format."
        },
        "firstDate": {
          "type": "string",
          "description": "Earliest allowed date (ISO)."
        },
        "lastDate": {
          "type": "string",
          "description": "Latest allowed date (ISO)."
        },
        "mode": {
          "description": "`calendar`, `input`, `both`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "calendar",
                "input",
                "both"
              ],
              "default": "calendar",
              "description": "`calendar`, `input`, `both`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "locale": {
          "type": "string",
          "description": "Locale identifier."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "datePicker": {
      "type": "object",
      "description": "Standalone date picker surface. Shared rows per §2.6.0; `value` is an ISO date string.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "datePicker"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "initialDate": {
          "type": "string",
          "description": "Date the picker opens on when the bound value is empty (ISO 8601)."
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        },
        "firstDate": {
          "type": "string",
          "description": "Earliest allowed date."
        },
        "lastDate": {
          "type": "string",
          "description": "Latest allowed date."
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "dateFormat": {
          "type": "string",
          "description": "Display pattern for the date portion. Display only — the bound value stays ISO-8601."
        },
        "icon": {
          "description": "Icon shown. Any `IconRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Icon shown. Any `IconRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "variant": {
          "description": "`elevated`, `filled`, `outlined`, `text`, `icon`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "elevated",
                "filled",
                "outlined",
                "text",
                "icon"
              ],
              "description": "`elevated`, `filled`, `outlined`, `text`, `icon`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "dateRangePicker": {
      "type": "object",
      "description": "Date range selection. Exception to §2.6.0: instead of a single `binding`, the range is bound via **two separate properties** — `startDate` and `endDate` — each independently following §2.6.0 two-way semantics (each accepts a literal ISO string, a `{{path}}` expression for read-only display, or a plain state-path string for two-way binding). `firstDate`/`lastDate` remain one-way constraints on the allowed range.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "dateRangePicker"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the picker accepts user input.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the picker accepts user input."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "errorText": {
          "type": "string",
          "description": "Error message shown under the field. Overrides validation output."
        },
        "startDate": {
          "type": "string",
          "description": "State path bound two-way to the range's start (ISO date)."
        },
        "endDate": {
          "type": "string",
          "description": "State path bound two-way to the range's end (ISO date)."
        },
        "label": {
          "type": "string",
          "description": "Field label."
        },
        "firstDate": {
          "type": "string",
          "description": "Earliest allowed date (constraint, one-way)."
        },
        "lastDate": {
          "type": "string",
          "description": "Latest allowed date (constraint, one-way)."
        },
        "format": {
          "type": "string",
          "default": "yyyy-MM-dd",
          "description": "Display format."
        },
        "locale": {
          "type": "string",
          "description": "Locale identifier."
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "dateTimePicker": {
      "type": "object",
      "description": "Single instant — date and time chosen together. Shared rows per §2.6.0;\n`binding` holds one ISO-8601 timestamp.\n\nNot `datePicker` + `timePicker` side by side: those bind two values the\nauthor must then recombine, and the recombination is where time zones get\nlost. One widget, one instant, one binding.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "dateTimePicker"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        },
        "min": {
          "type": "string",
          "description": "Earliest selectable instant (ISO-8601)."
        },
        "max": {
          "type": "string",
          "description": "Latest selectable instant (ISO-8601)."
        },
        "dateFormat": {
          "type": "string",
          "description": "Display pattern for the date portion. Display only — the bound value stays ISO-8601."
        },
        "timeFormat": {
          "type": "string",
          "description": "Display pattern for the time portion. Display only."
        },
        "minuteInterval": {
          "description": "Granularity of the minute wheel.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Granularity of the minute wheel."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "timeZone": {
          "type": "string",
          "description": "IANA zone the picker presents in. Absent means the host's zone. The bound value carries its offset either way — a timestamp without one is not an instant."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "decoration": {
      "type": "object",
      "description": "Wraps a child with a decoration box (color, gradient, border, shadow,\nimage, blur) without otherwise affecting layout. The full\n`BoxDecoration` may be supplied via `decoration:`, or any of its\nfields may appear flat at the top level for ergonomic shorthand.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "decoration"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "position": {
          "type": "string",
          "description": "Where the decoration sits relative to the child: `background` or `foreground`."
        },
        "decoration": {
          "description": "Full decoration object. Alternatively, the constituent fields\n(`color`, `gradient`, `image`, `border`, `borderRadius`,\n`boxShadow`, `shape`, `backdropBlur`) may appear flat at the top\nlevel. Flat keys override matching `decoration.<field>` entries.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/BoxDecoration",
              "description": "Full decoration object. Alternatively, the constituent fields\n(`color`, `gradient`, `image`, `border`, `borderRadius`,\n`boxShadow`, `shape`, `backdropBlur`) may appear flat at the top\nlevel. Flat keys override matching `decoration.<field>` entries.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "color": {
          "description": "Flat shorthand for `decoration.color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Flat shorthand for `decoration.color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "borderRadius": {
          "description": "Flat shorthand for `decoration.borderRadius`.",
          "anyOf": [
            {
              "$ref": "#/$defs/BorderRadius",
              "description": "Flat shorthand for `decoration.borderRadius`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "border": {
          "description": "Flat shorthand for `decoration.border`.",
          "anyOf": [
            {
              "$ref": "#/$defs/BoxBorder",
              "description": "Flat shorthand for `decoration.border`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "gradient": {
          "description": "Flat shorthand for `decoration.gradient`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Gradient",
              "description": "Flat shorthand for `decoration.gradient`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "image": {
          "description": "Flat shorthand for `decoration.image`.",
          "anyOf": [
            {
              "$ref": "#/$defs/BackgroundImage",
              "description": "Flat shorthand for `decoration.image`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "boxShadow": {
          "description": "Flat shorthand for `decoration.boxShadow`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/BoxShadow"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "shape": {
          "description": "Flat shorthand for `decoration.shape`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "rectangle",
                "circle"
              ],
              "default": "rectangle",
              "description": "Flat shorthand for `decoration.shape`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "backdropBlur": {
          "description": "Flat shorthand for `decoration.backdropBlur` (Gaussian blur sigma).",
          "anyOf": [
            {
              "type": "number",
              "description": "Flat shorthand for `decoration.backdropBlur` (Gaussian blur sigma)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "description": "Multiple widgets; runtime wraps them in a linear column.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "diffViewer": {
      "type": "object",
      "description": "Side-by-side or unified comparison of two texts.\n\nSeparate from `codeEditor` rather than a mode on it, because the input is\ntwo documents: a `mode` flag on a single-value widget would leave one of\nthe two with nowhere to bind. Read-only by design — editing a diff means\nediting one side, which is `codeEditor`'s job.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "diffViewer"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "oldValue": {
          "description": "Left / base text.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "newValue": {
          "description": "Right / changed text.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "splitView": {
          "description": "Two columns when true, unified single column when false.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Two columns when true, unified single column when false."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "language": {
          "type": "string",
          "description": "Syntax highlighting language, same vocabulary as `codeEditor.language`."
        },
        "showLineNumbers": {
          "description": "Gutter line numbers.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Gutter line numbers."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "contextLines": {
          "description": "Unchanged lines kept around each change. Omitted shows the whole document.",
          "anyOf": [
            {
              "type": "number",
              "description": "Unchanged lines kept around each change. Omitted shows the whole document."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "highlightLines": {
          "description": "Extra emphasis on specific new-side lines.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "string",
                    "pattern": "^\\{\\{.*\\}\\}$",
                    "description": "Binding expression."
                  },
                  {
                    "type": "object",
                    "properties": {
                      "value": {
                        "type": "number"
                      },
                      "unit": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "value"
                    ],
                    "description": "Dimension object: `{value, unit}`."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        }
      },
      "required": [
        "type",
        "oldValue",
        "newValue"
      ],
      "additionalProperties": true
    },
    "divider": {
      "type": "object",
      "description": "Horizontal separator.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "divider"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "vertical": {
          "description": "Draw the rule vertically instead of horizontally.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Draw the rule vertically instead of horizontally."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "height": {
          "description": "Fixed height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "thickness": {
          "description": "Line thickness.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Line thickness."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "color": {
          "description": "Line color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Line color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "indent": {
          "description": "Leading indent.",
          "anyOf": [
            {
              "type": "number",
              "description": "Leading indent."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "endIndent": {
          "description": "Trailing indent.",
          "anyOf": [
            {
              "type": "number",
              "description": "Trailing indent."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "dragTarget": {
      "type": "object",
      "description": "Drop area that accepts dragged data.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "dragTarget"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "canDrop": {
          "type": "string",
          "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
          "description": "Expression controlling acceptance."
        },
        "builder": {
          "$ref": "#/$defs/Widget"
        },
        "child": {
          "$ref": "#/$defs/Widget",
          "description": "Legacy alias of `builder`."
        },
        "children": {
          "description": "Multiple widgets; runtime wraps them in a linear column. Mutually exclusive with `builder`/`child`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "onDrop": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onDragEnter": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onDragLeave": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "draggable": {
      "type": "object",
      "description": "Drag source widget that produces drag data and feedback visuals.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "draggable"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "affinity": {
          "type": "string",
          "description": "Axis this drag claims before competing gestures: `horizontal`, `vertical`."
        },
        "axis": {
          "type": "string",
          "description": "Axis the drag is restricted to."
        },
        "dragAnchorStrategy": {
          "type": "string",
          "description": "Where the dragged feedback sits relative to the pointer: `child` or `pointer`."
        },
        "onDragCompleted": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onDragEnd": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onDragStarted": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onDraggableCanceled": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "data": {
          "description": "Payload emitted on drop.",
          "anyOf": [
            {
              "description": "any",
              "anyOf": [
                {
                  "description": "any"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "feedback": {
          "$ref": "#/$defs/Widget"
        },
        "childWhenDragging": {
          "$ref": "#/$defs/Widget"
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type",
        "data"
      ],
      "additionalProperties": true
    },
    "drawer": {
      "type": "object",
      "description": "Side navigation drawer. Each item's text field is `label`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "drawer"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "semanticLabel": {
          "type": "string",
          "description": "Label announced by a screen reader in place of the content."
        },
        "shadowColor": {
          "description": "Colour of the drop shadow cast by the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the drop shadow cast by the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "Outline of the surface, as a shape descriptor.",
          "anyOf": [
            {
              "type": "object",
              "description": "Outline of the surface, as a shape descriptor.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "surfaceTintColor": {
          "description": "Elevation tint applied over the surface colour (M3).",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Elevation tint applied over the surface colour (M3)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "items": {
          "description": "`{ icon, label, route? }` entries. Required when `children` is omitted.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/DrawerItem"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "children": {
          "description": "Custom drawer body widgets. Mutually exclusive with `items`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "header": {
          "$ref": "#/$defs/Widget"
        },
        "onSelect": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onClose": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow depth of the drawer surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth of the drawer surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "errorBoundary": {
      "type": "object",
      "description": "Catches render and build errors thrown by descendants and displays a fallback UI. See [`06_Runtime_Contract.md`](06_Runtime_Contract.md) for the exact recovery lifecycle.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "errorBoundary"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "fallback": {
          "$ref": "#/$defs/Widget"
        },
        "onError": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "errorRecovery": {
      "type": "object",
      "description": "Retry-oriented variant of `errorBoundary`. The `handlers` map may route specific error types to distinct fallbacks.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "errorRecovery"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "description": "Multiple protected widgets; runtime wraps them in a linear column.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "fallback": {
          "$ref": "#/$defs/Widget"
        },
        "handlers": {
          "description": "Map of error-type → fallback Widget.",
          "anyOf": [
            {
              "type": "object",
              "description": "Map of error-type → fallback Widget.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onError": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "showDetails": {
          "description": "Whether to render stack/error detail.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Whether to render stack/error detail."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "expanded": {
      "type": "object",
      "description": "Flex child that expands to fill available space inside a `linear` parent.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "expanded"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "flex": {
          "description": "Flex weight.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Flex weight."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "fileExplorer": {
      "type": "object",
      "description": "File and directory browser.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "fileExplorer"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "iconColor": {
          "description": "Colour applied to the icon.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour applied to the icon."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "items": {
          "description": "Hierarchical `{ name, path, type, children? }` tree. Required when `rootPath`/`files`/`directories` legacy props are omitted.",
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "description": "object",
                    "anyOf": [
                      {
                        "type": "object",
                        "description": "object",
                        "additionalProperties": true
                      },
                      {
                        "$ref": "#/$defs/Binding",
                        "description": "Binding expression resolving to this value."
                      }
                    ]
                  }
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression resolving to an array."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "rootPath": {
          "type": "string",
          "description": "Legacy: root path string; pairs with flat `files` / `directories`."
        },
        "files": {
          "description": "Legacy: flat file list (alternative to hierarchical `items`).",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "directories": {
          "description": "Legacy: flat directory list (alternative to hierarchical `items`).",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "showIcons": {
          "description": "Show file / folder icons",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Show file / folder icons"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showHidden": {
          "description": "Show entries whose name starts with `.`",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Show entries whose name starts with `.`"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "expandAll": {
          "description": "Expand all folders by default",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Expand all folders by default"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "width": {
          "description": "Widget width",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget width"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Widget height",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget height"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "selectedColor": {
          "description": "Background color for selected entry",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Background color for selected entry"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onSelect": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onOpen": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "fileInput": {
      "type": "object",
      "description": "Lets the user hand the application a file they picked themselves.\n\nFollows the shared input contract (spec §2.6.0): with `binding` set, the\nruntime writes the selection to that state path. The value written is a\nlist of file descriptors — see `binding` below.\n\n**Why this is Core and `client.selectFile` is not.** Reading a path the\ndocument names (`client.readFile`, `client.listFiles` — spec §8.2) reaches\ninto the host's filesystem, so it belongs to the Client Profile behind a\n`file.read` grant (spec §8.4). Receiving a file the *user chose in a picker* is the\nopposite: the choosing is the consent, and the app learns nothing it was\nnot handed. It is the same trust level as `signature` (also Core), which\nalready accepts user-drawn input. A browser-rendered app that cannot claim\nthe Client Profile still must be able to accept an upload.\n\nA runtime MUST NOT use this widget as a path to unpicked files: only what\nthe picker returned is readable, and `path` MAY be absent (it is on web).\nTransport is not this widget's concern — once the selection is in state,\nsending it is an ordinary `tool` action.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "fileInput"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path the selection is written to. Receives an array of\n`{name, size, mimeType, bytes?, path?}` — `bytes` is a base64\n`data:` URI usable directly as an `AssetRef`, `path` is present only\nwhere the host has a filesystem. Empty array when the user cancels.\n"
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the picker can be opened.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the picker can be opened."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "dragDrop": {
          "description": "Accept files dropped onto the control where the host supports it. Never the only affordance — the picker button stays.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Accept files dropped onto the control where the host supports it. Never the only affordance — the picker button stays."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "maxFiles": {
          "description": "Ceiling on selection count. Ignored unless `multiple` is true.",
          "anyOf": [
            {
              "type": "number",
              "description": "Ceiling on selection count. Ignored unless `multiple` is true."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "preview": {
          "description": "Render thumbnails for image selections, using each descriptor's `bytes` as an `AssetRef`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Render thumbnails for image selections, using each descriptor's `bytes` as an `AssetRef`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "crop": {
          "description": "Offer a crop step for image selections before the value lands in state.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Offer a crop step for image selections before the value lands in state."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "aspectRatio": {
          "description": "Crop ratio (width / height). Ignored unless `crop` is true.",
          "anyOf": [
            {
              "type": "number",
              "description": "Crop ratio (width / height). Ignored unless `crop` is true."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "accept": {
          "description": "Acceptable types as MIME patterns (`image/*`, `application/pdf`) or\nextensions (`.csv`). A hint to the picker, not a guarantee — a\nruntime MUST re-check `mimeType` before relying on it.\n",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "multiple": {
          "description": "Allow selecting more than one file.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Allow selecting more than one file."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "maxBytes": {
          "description": "Per-file size ceiling. Files above it are rejected before entering\nstate and MUST surface through `onError`, not silently vanish.\n",
          "anyOf": [
            {
              "type": "number",
              "description": "Per-file size ceiling. Files above it are rejected before entering\nstate and MUST surface through `onError`, not silently vanish.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Text on the picker trigger."
        },
        "onError": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "fittedBox": {
      "type": "object",
      "description": "Scales and aligns its child to fit available space.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "fittedBox"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "fit": {
          "description": "`cover`, `contain`, `fill`, `scaleDown`, `none`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "cover",
                "contain",
                "fill",
                "scaleDown",
                "none"
              ],
              "default": "contain",
              "description": "`cover`, `contain`, `fill`, `scaleDown`, `none`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "alignment": {
          "description": "Alignment within the fitted bounds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Alignment within the fitted bounds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "flexible": {
      "type": "object",
      "description": "Flex child that may occupy up to its natural size.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "flexible"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "flex": {
          "description": "Flex weight.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Flex weight."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "fit": {
          "type": "string",
          "default": "loose",
          "description": "`\"loose\"` or `\"tight\"`."
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "floatingActionButton": {
      "type": "object",
      "description": "Floating action button (FAB).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "floatingActionButton"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ],
          "description": "Legacy alias of `onTap`."
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "disabledElevation": {
          "description": "Shadow depth while disabled.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth while disabled."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "focusElevation": {
          "description": "Shadow depth while focused.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth while focused."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "heroTag": {
          "type": "string",
          "description": "Tag used to animate the button between routes; distinct tags let two coexist."
        },
        "highlightElevation": {
          "description": "Shadow depth while held down.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth while held down."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "hoverElevation": {
          "description": "Shadow depth while hovered.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth while hovered."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "isExtended": {
          "description": "Render the extended form, with a label beside the icon.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Render the extended form, with a label beside the icon."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "materialTapTargetSize": {
          "type": "string",
          "description": "Minimum tap target: `padded` (48dp) or `shrinkWrap`."
        },
        "mini": {
          "description": "Render the smaller button size.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Render the smaller button size."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "autofocus": {
          "description": "Take focus when the surface first appears.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Take focus when the surface first appears."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "focusColor": {
          "description": "Overlay while the surface holds focus.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay while the surface holds focus."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "hoverColor": {
          "description": "Overlay while a pointer rests on the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay while a pointer rests on the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "Outline of the surface, as a shape descriptor.",
          "anyOf": [
            {
              "type": "object",
              "description": "Outline of the surface, as a shape descriptor.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "splashColor": {
          "description": "Colour of the ripple that expands from the touch point.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the ripple that expands from the touch point."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "icon": {
          "description": "Icon shown. Any `IconRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Icon shown. Any `IconRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Extended FAB label."
        },
        "onTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onPressed": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ],
          "description": "Legacy alias of `onTap`."
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow depth at rest.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth at rest."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "foregroundColor": {
          "description": "Bar color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Bar color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onLongPress": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "flow": {
      "type": "object",
      "description": "Lightweight flowing layout that places each child along the main axis with fixed `spacing`, falling back to a new run when space is exhausted. A simpler alternative to `wrap` (§2.4.11) for spacer-style chains.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "flow"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "children": {
          "description": "Flow children.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "direction": {
          "type": "string",
          "default": "horizontal",
          "description": "`horizontal` or `vertical`."
        },
        "spacing": {
          "description": "Gap between children in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "default": 8,
              "description": "Gap between children in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "alignment": {
          "description": "`start`, `center`, `end`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "start",
                "center",
                "end"
              ],
              "default": "start",
              "description": "`start`, `center`, `end`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        }
      },
      "required": [
        "type",
        "children"
      ],
      "additionalProperties": true
    },
    "form": {
      "type": "object",
      "description": "Container that manages validation state for its child inputs.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "form"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "children": {
          "description": "Form field widgets.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "showErrorsOn": {
          "description": "`submit`, `change`, `blur`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "submit",
                "change",
                "blur"
              ],
              "default": "submit",
              "description": "`submit`, `change`, `blur`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "onSubmit": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "children"
      ],
      "additionalProperties": true
    },
    "fractionallySized": {
      "type": "object",
      "description": "Sizes the child as a fraction of the parent.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "fractionallySized"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "widthFactor": {
          "description": "Width fraction in `0.0..1.0`.",
          "anyOf": [
            {
              "type": "number",
              "description": "Width fraction in `0.0..1.0`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "heightFactor": {
          "description": "Height fraction in `0.0..1.0`.",
          "anyOf": [
            {
              "type": "number",
              "description": "Height fraction in `0.0..1.0`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "alignment": {
          "description": "Where the sized child sits inside the available space.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Where the sized child sits inside the available space."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "gantt": {
      "type": "object",
      "description": "Tasks on a time axis, with dependencies and progress.\n\nWhat makes this a widget rather than a composition is the axis. Bars are\npositioned by *time*, not by index, so the row and the header must share\none scale, stay aligned through zoom and horizontal scroll, and place a\ntask whose span is shorter than a pixel without dropping it. Composing it\nfrom `grid` gives rows that drift from the header as soon as either\nscrolls.\n\nDependencies are drawn, not enforced: the widget renders the arrows and\nreports an edit, and whether a move is legal is the server's answer.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "gantt"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "tasks": {
          "description": "`{ id, label, start, end, progress?, dependsOn?, group?, color? }`. `start`/`end` are ISO-8601. `dependsOn` holds task ids.",
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "description": "object",
                    "anyOf": [
                      {
                        "type": "object",
                        "description": "object",
                        "additionalProperties": true
                      },
                      {
                        "$ref": "#/$defs/Binding",
                        "description": "Binding expression resolving to this value."
                      }
                    ]
                  }
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression resolving to an array."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "viewMode": {
          "description": "Axis granularity.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "hour",
                "day",
                "week",
                "month",
                "quarter",
                "year"
              ],
              "default": "day",
              "description": "Axis granularity."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "range": {
          "description": "`{ start, end }` window shown. Omitted fits all tasks.",
          "anyOf": [
            {
              "type": "object",
              "description": "`{ start, end }` window shown. Omitted fits all tasks.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "editable": {
          "description": "Allow dragging bars to move them and dragging edges to reschedule.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Allow dragging bars to move them and dragging edges to reschedule."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showProgress": {
          "description": "Render each task's `progress` as a fill.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Render each task's `progress` as a fill."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showDependencies": {
          "description": "Draw dependency arrows.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Draw dependency arrows."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "todayMarker": {
          "description": "Mark the current instant on the axis.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Mark the current instant on the axis."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "rowHeight": {
          "description": "Task row height in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "description": "Task row height in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "onTaskChange": {
          "$ref": "#/$defs/Action"
        },
        "onTaskClick": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "tasks"
      ],
      "additionalProperties": true
    },
    "gauge": {
      "type": "object",
      "description": "Radial gauge for a value within a range.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "gauge",
            "meter"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "value": {
          "description": "Current value",
          "anyOf": [
            {
              "type": "number",
              "description": "Current value"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "min": {
          "description": "Range minimum",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Range minimum"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "max": {
          "description": "Range maximum",
          "anyOf": [
            {
              "type": "number",
              "default": 100,
              "description": "Range maximum"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "segments": {
          "description": "Color segments with `from`, `to`, `color`",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Segment"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "size": {
          "description": "Diameter (logical px)",
          "anyOf": [
            {
              "type": "number",
              "default": 200,
              "description": "Diameter (logical px)"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "strokeWidth": {
          "description": "Arc thickness in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "default": 10,
              "description": "Arc thickness in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "backgroundColor": {
          "description": "Track color",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Track color"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "valueColor": {
          "description": "Value arc color (when no segments)",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Value arc color (when no segments)"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "showLabel": {
          "description": "Show numeric label",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Show numeric label"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "labelFormat": {
          "type": "string",
          "default": "{value}%",
          "description": "Label pattern; `{value}` is replaced by the reading. The default carries\nthe per-cent sign because that is what every gauge written so far has\nbeen drawing — a gauge of something else writes its own pattern.\n"
        },
        "startAngle": {
          "description": "Start angle in degrees, measured the way the painter does: `-220` with a\nsweep of `260` is the open-bottom dial these gauges have always drawn.\n",
          "anyOf": [
            {
              "type": "number",
              "default": -220,
              "description": "Start angle in degrees, measured the way the painter does: `-220` with a\nsweep of `260` is the open-bottom dial these gauges have always drawn.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "sweepAngle": {
          "description": "Sweep span in degrees.",
          "anyOf": [
            {
              "type": "number",
              "default": 260,
              "description": "Sweep span in degrees."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        }
      },
      "required": [
        "type",
        "value"
      ],
      "additionalProperties": true
    },
    "gestureDetector": {
      "type": "object",
      "description": "Detects touch and pointer gestures on its child.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "gestureDetector"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "onScaleUpdate": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "onTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onDoubleTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onLongPress": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onPanStart": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onPanUpdate": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onPanEnd": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "graph": {
      "type": "object",
      "description": "Time-series / numeric data graph (line, bar, area, scatter) drawn on a single set of axes. For node/edge topology visualization use `networkGraph` (§10.13).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "graph"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "labelColor": {
          "description": "Colour applied to the label text.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour applied to the label text."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "data": {
          "description": "Points as `{ x, y }` or `{ label, value }`, or bare numbers when the\nx axis is just the position in the list — which is what a sparkline\nfed from a rolling buffer carries.\n",
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/Point"
                      },
                      {
                        "$ref": "#/$defs/Binding",
                        "description": "Binding expression resolving to this value."
                      }
                    ]
                  }
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression resolving to an array."
                }
              ]
            },
            {
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "number"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\{\\{.*\\}\\}$",
                        "description": "Binding expression."
                      },
                      {
                        "type": "object",
                        "properties": {
                          "value": {
                            "type": "number"
                          },
                          "unit": {
                            "type": "string"
                          }
                        },
                        "required": [
                          "value"
                        ],
                        "description": "Dimension object: `{value, unit}`."
                      }
                    ]
                  }
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression resolving to an array."
                }
              ]
            }
          ]
        },
        "chartType": {
          "description": "Chart style. Legacy alias `type` is retained for backward compatibility but avoid it — it collides with the widget-type discriminator.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "line",
                "bar",
                "area",
                "scatter"
              ],
              "default": "line",
              "description": "Chart style. Legacy alias `type` is retained for backward compatibility but avoid it — it collides with the widget-type discriminator."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "width": {
          "description": "Render width in logical px.",
          "anyOf": [
            {
              "type": "number",
              "default": 300,
              "description": "Render width in logical px."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Render height.",
          "anyOf": [
            {
              "type": "number",
              "default": 200,
              "description": "Render height."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "showGrid": {
          "description": "Draw background grid.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Draw background grid."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showLabels": {
          "description": "Draw axis labels.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Draw axis labels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "lineColor": {
          "description": "Line / bar color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Line / bar color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "fillColor": {
          "description": "Area fill color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Area fill color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "gridColor": {
          "description": "Grid color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Grid color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "strokeWidth": {
          "description": "Line stroke thickness.",
          "anyOf": [
            {
              "type": "number",
              "default": 2,
              "description": "Line stroke thickness."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        }
      },
      "required": [
        "type",
        "data"
      ],
      "additionalProperties": true
    },
    "grid": {
      "type": "object",
      "description": "Scrollable two-dimensional collection.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "grid",
            "gridview"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "mainAxisExtent": {
          "description": "Fixed extent of each row along the scroll axis.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed extent of each row along the scroll axis."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "maxCrossAxisExtent": {
          "description": "Widest a cell may be before the grid adds a column.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Widest a cell may be before the grid adds a column."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "padding": {
          "description": "Space inside the surface, around its content.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space inside the surface, around its content."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "physics": {
          "type": "string",
          "description": "Scroll response: `bouncing`, `clamping`, `never`, `always`."
        },
        "shrinkWrap": {
          "description": "Size to the content instead of filling the viewport.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Size to the content instead of filling the viewport."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "spacing": {
          "description": "Gap between adjacent items.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Gap between adjacent items."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "items": {
          "description": "Array source. Required when `children` is omitted.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "itemTemplate": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "description": "Static grid cells. Mutually exclusive with `items` + `itemTemplate`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "columns": {
          "description": "Column count. The object form is a responsive override keyed by form factor (§14.1.1) — `{compact, medium, expanded, large, extraLarge, default}`.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "crossAxisCount": {
          "description": "Legacy alias of `columns`.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "rowGap": {
          "description": "Gap between rows.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Gap between rows."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "columnGap": {
          "description": "Gap between columns.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Gap between columns."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "itemAspectRatio": {
          "description": "Fixed aspect ratio for each item.",
          "anyOf": [
            {
              "type": "number",
              "description": "Fixed aspect ratio for each item."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "reverse": {
          "description": "When true, run the transition in reverse direction (for back navigation).",
          "anyOf": [
            {
              "type": "boolean",
              "description": "When true, run the transition in reverse direction (for back navigation)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "scrollDirection": {
          "type": "string",
          "description": "Scroll axis."
        },
        "template": {
          "description": "Template name to instantiate.",
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": true
            },
            {
              "type": "string"
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "headerBar": {
      "type": "object",
      "description": "Application header / toolbar at the top of a page.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "headerBar",
            "appbar"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "automaticallyImplyLeading": {
          "description": "Insert a back / drawer control when the route provides one.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Insert a back / drawer control when the route provides one."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "bottomHeight": {
          "description": "Height reserved for the bottom area (tabs, search, …).",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Height reserved for the bottom area (tabs, search, …)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "bottomOpacity": {
          "description": "Opacity of the bottom area, `0.0`–`1.0`.",
          "anyOf": [
            {
              "type": "number",
              "description": "Opacity of the bottom area, `0.0`–`1.0`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "flexibleSpace": {
          "$ref": "#/$defs/Widget"
        },
        "toolbarHeight": {
          "description": "Height of the bar itself, excluding any bottom area.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Height of the bar itself, excluding any bottom area."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "toolbarOpacity": {
          "description": "Opacity of the toolbar contents, `0.0`–`1.0`.",
          "anyOf": [
            {
              "type": "number",
              "description": "Opacity of the toolbar contents, `0.0`–`1.0`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "shadowColor": {
          "description": "Colour of the drop shadow cast by the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the drop shadow cast by the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "Outline of the surface, as a shape descriptor.",
          "anyOf": [
            {
              "type": "object",
              "description": "Outline of the surface, as a shape descriptor.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "title": {
          "description": "Header title.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/Widget"
            }
          ]
        },
        "leading": {
          "$ref": "#/$defs/Widget"
        },
        "actions": {
          "description": "Trailing action widgets.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "exitButton": {
          "description": "Override the host-inserted close button on the root route; see below.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "$ref": "#/$defs/ExitButtonConfig"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            }
          ]
        },
        "backgroundColor": {
          "description": "Header background.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Header background."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow elevation.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Shadow elevation."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "centerTitle": {
          "description": "Whether to center the title.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Whether to center the title."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "bottom": {
          "description": "Distance from the parent stack's bottom edge.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Distance from the parent stack's bottom edge."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "foregroundColor": {
          "description": "Bar color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Bar color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "heatmap": {
      "type": "object",
      "description": "Two-dimensional heatmap visualization.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "heatmap"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "cellGap": {
          "description": "Gap between adjacent cells.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Gap between adjacent cells."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "colorScheme": {
          "type": "string",
          "description": "Named colour ramp used between `minValue` and `maxValue`."
        },
        "columns": {
          "description": "Number of columns the values are laid out in.",
          "anyOf": [
            {
              "type": "number",
              "description": "Number of columns the values are laid out in."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "maxValue": {
          "description": "Value mapped to the high end of the colour range. Optional — when omitted the range is taken from the data, so a heatmap always renders as a scale rather than one flat block.",
          "anyOf": [
            {
              "type": "number",
              "description": "Value mapped to the high end of the colour range. Optional — when omitted the range is taken from the data, so a heatmap always renders as a scale rather than one flat block."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "minValue": {
          "description": "Value mapped to the low end of the colour range. Optional — when omitted the range is taken from the data.",
          "anyOf": [
            {
              "type": "number",
              "description": "Value mapped to the low end of the colour range. Optional — when omitted the range is taken from the data."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "showLabels": {
          "description": "Show the row and column labels declared in `rowLabels` / `columnLabels`. Off by default: without it those labels draw nothing, and an author who declared them reads the blank axis as their own mistake. The numeric value inside a cell is `showValues`.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Show the row and column labels declared in `rowLabels` / `columnLabels`. Off by default: without it those labels draw nothing, and an author who declared them reads the blank axis as their own mistake. The numeric value inside a cell is `showValues`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "data": {
          "description": "2D numeric array (rows × columns). Literal array or binding.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "columnLabels": {
          "description": "Horizontal axis labels",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "rowLabels": {
          "description": "Vertical axis labels",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "cellSize": {
          "description": "Cell size (logical px)",
          "anyOf": [
            {
              "type": "number",
              "default": 40,
              "description": "Cell size (logical px)"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "colorRange": {
          "description": "Gradient endpoints — the color a cell takes at the lowest value and at the highest.",
          "anyOf": [
            {
              "description": "Gradient endpoints — the color a cell takes at the lowest value and at the highest."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "showValues": {
          "description": "Render the numeric value inside each cell. On by default: every heatmap\nwritten before this property was read showed its numbers, and turning\nthem off by default would change what those documents display.\n",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Render the numeric value inside each cell. On by default: every heatmap\nwritten before this property was read showed its numbers, and turning\nthem off by default would change what those documents display.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onCellTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "data"
      ],
      "additionalProperties": true
    },
    "hero": {
      "type": "object",
      "description": "Shared-element transition wrapper. Wrap a child widget on the\nsource page AND on the destination page with the SAME `tag`; the\nruntime morphs the source widget's bounds → destination widget's\nbounds during the route transition (cover → detail magnification,\nalbum cover → expanded album view). Pairs naturally with\n`RouteTransition` styles such as `fade` or `sharedAxis`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "hero"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "tag": {
          "type": "string",
          "description": "Identifier shared between source and destination pages. Both\nsides MUST set the same tag for the morph to engage. Tags must\nbe unique within each page (a single page cannot have two heroes\nwith the same tag).\n"
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "transitionOnUserGestures": {
          "description": "When true, also morphs during gesture-driven back navigation\n(iOS swipe-from-edge). Adds slight render cost.\n",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "When true, also morphs during gesture-driven back navigation\n(iOS swipe-from-edge). Adds slight render cost.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "flightShuttleBuilder": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type",
        "tag"
      ],
      "additionalProperties": true
    },
    "icon": {
      "type": "object",
      "description": "Displays an icon. The `icon` property accepts three source forms — pick\nthe lightest option that fits the asset:\n\n1. **Named (built-in)** — `\"home\"`, `\"folder_open\"`, … from the runtime's\n   Material Icons resolver table. Zero-cost.\n2. **Codepoint** — `{codepoint, fontFamily?, fontPackage?}` for Material\n   Icons codepoints not in the name table.\n3. **Any `AssetRef`** — a network URL, `bundle://` asset, `assets/` path,\n   `data:image/svg+xml;…` inline SVG, `client://` host resource, or an\n   `{uri, origin?}` object read from an origin. Which of these a given\n   runtime resolves is declared, not assumed (`06_Runtime_Contract.md`\n   §6.12).\n\nA bare string that matches no known scheme and is not an `assets/` path\nis read as a **name**, so the named form stays the zero-ceremony default.\n\nThe optional `shader` field paints the icon with a gradient/shader fill\n(rather than a solid `color`). When both are set the shader wins.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "icon"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "icon": {
          "description": "Icon name, codepoint object, or any `AssetRef` — the same three\nforms every icon slot accepts.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Icon name, codepoint object, or any `AssetRef` — the same three\nforms every icon slot accepts.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "size": {
          "description": "Icon size. Accepts either a numeric dp value or an `AppIconSizes`\ntoken shorthand (`sm` / `md` / `lg` / `xl`) which scales with the\nactive form factor.\n",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "sizeToken": {
          "description": "Responsive icon-size token. Equivalent to passing the same string\nvia `size`; resolves through `AppIconSizes.of(context)` and tracks\nthe form-factor scale (compact / medium / desktop / embedded).\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "sm",
                "md",
                "lg",
                "xl"
              ],
              "description": "Responsive icon-size token. Equivalent to passing the same string\nvia `size`; resolves through `AppIconSizes.of(context)` and tracks\nthe form-factor scale (compact / medium / desktop / embedded).\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "color": {
          "description": "Icon color. Applied to named / codepoint / SVG forms; for raster\n`URL` form it tints when supported. Ignored when `shader` is set.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Icon color. Applied to named / codepoint / SVG forms; for raster\n`URL` form it tints when supported. Ignored when `shader` is set.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shader": {
          "description": "Gradient/shader fill (linear / radial / sweep). Painted via a\nshader mask over the icon's glyph alpha. Mutually exclusive with\n`color` — when both are present `shader` wins.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/Gradient",
              "description": "Gradient/shader fill (linear / radial / sweep). Painted via a\nshader mask over the icon's glyph alpha. Mutually exclusive with\n`color` — when both are present `shader` wins.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "icon"
      ],
      "additionalProperties": true
    },
    "iconButton": {
      "type": "object",
      "description": "Icon-only button for compact actions.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "iconButton"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "fontFamily": {
          "type": "string",
          "description": "Font family the icon glyph is taken from, for icon sets outside the default."
        },
        "disabledColor": {
          "description": "Colour used while the control is disabled.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour used while the control is disabled."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "enableFeedback": {
          "description": "Play the platform tap sound / haptic on activation.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Play the platform tap sound / haptic on activation."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "highlightColor": {
          "description": "Overlay while the surface is held down.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay while the surface is held down."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "iconSize": {
          "description": "Rendered size of the icon.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Rendered size of the icon."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "padding": {
          "description": "Space inside the surface, around its content.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space inside the surface, around its content."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "splashColor": {
          "description": "Colour of the ripple that expands from the touch point.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the ripple that expands from the touch point."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "splashRadius": {
          "description": "Radius the ripple expands to.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Radius the ripple expands to."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "icon": {
          "description": "Icon shown. Any `IconRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Icon shown. Any `IconRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "size": {
          "description": "Icon size; uses theme default if omitted.",
          "anyOf": [
            {
              "type": "number",
              "description": "Icon size; uses theme default if omitted."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "color": {
          "description": "Icon color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Icon color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "enabled": {
          "description": "Whether the button is interactive.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the button is interactive."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "alignment": {
          "description": "Where the icon sits inside the button bounds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Where the icon sits inside the button bounds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "icon"
      ],
      "additionalProperties": true
    },
    "image": {
      "type": "object",
      "description": "Displays an image from any asset source the runtime resolves — a\nnetwork URL, the active bundle, inline bytes, a host resource, or a\nresource served by an origin. See `AssetRef`; a runtime declares which\nof those it resolves (`06_Runtime_Contract.md` §6.12) and routes the\nrest to `fallback` / `fallbackUrl` / `fallbackBehavior`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "image"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "errorWidget": {
          "type": "string",
          "description": "Text shown on the error tile when nothing resolves."
        },
        "fallbackBehavior": {
          "type": "string",
          "description": "What to draw when nothing resolves: `error` (default) or `hide`."
        },
        "fallbackUrl": {
          "description": "Second source tried when the first cannot be resolved (§6.12.4).",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Second source tried when the first cannot be resolved (§6.12.4)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "src": {
          "description": "Image source. Any `AssetRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Image source. Any `AssetRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "source": {
          "description": "Legacy alias of `src`.",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Image source. Any `AssetRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundImage": {
          "description": "Legacy alias of `src`.",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Image source. Any `AssetRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Width in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "description": "Width in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Height in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "description": "Height in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "fit": {
          "description": "`cover`, `contain`, `fill`, `none`, `scaleDown`, `fitHeight`, `fitWidth`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "cover",
                "contain",
                "fill",
                "none",
                "scaleDown",
                "fitHeight",
                "fitWidth"
              ],
              "default": "contain",
              "description": "`cover`, `contain`, `fill`, `none`, `scaleDown`, `fitHeight`, `fitWidth`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "alignment": {
          "description": "Alignment within bounds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Alignment within bounds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "fallback": {
          "$ref": "#/$defs/Widget"
        },
        "loading": {
          "$ref": "#/$defs/Widget"
        },
        "placeholder": {
          "type": "string",
          "description": "Placeholder text."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "imageFilter": {
      "type": "object",
      "description": "Wraps a child with a colour / blur filter. Distinct from\n`BoxDecoration.image.colorFilter` (which colours an image\nbackground) — `imageFilter` filters the entire rendered subtree.\nUse for sepia/grayscale book pages, blur-on-hover thumbnails,\nsaturation-bumped hero shots.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "imageFilter"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "filter": {
          "description": "Filter kind:\n- `sepia` — warm-tone tint mimicking aged photograph.\n- `grayscale` — desaturate to luminance.\n- `blur` — Gaussian blur (sigma controlled by `intensity`).\n- `saturation` — multiply chroma by `intensity` (0 = grayscale, 1 = unchanged, >1 = pop).\n- `brightness` — multiply luminance.\n- `contrast` — increase / decrease contrast.\n- `invert` — colour invert (negative).\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "sepia",
                "grayscale",
                "blur",
                "saturation",
                "brightness",
                "contrast",
                "invert"
              ],
              "description": "Filter kind:\n- `sepia` — warm-tone tint mimicking aged photograph.\n- `grayscale` — desaturate to luminance.\n- `blur` — Gaussian blur (sigma controlled by `intensity`).\n- `saturation` — multiply chroma by `intensity` (0 = grayscale, 1 = unchanged, >1 = pop).\n- `brightness` — multiply luminance.\n- `contrast` — increase / decrease contrast.\n- `invert` — colour invert (negative).\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "intensity": {
          "description": "Filter strength. Range and default depend on `filter`:\n- `sepia` / `grayscale` / `invert` — `0.0` (off) .. `1.0` (full). Default `1.0`.\n- `blur` — sigma in pixels (`0.0` off, typical 4..16).\n- `saturation` / `brightness` / `contrast` — multiplier (`1.0` unchanged).\n",
          "anyOf": [
            {
              "type": "number",
              "default": 1.0,
              "description": "Filter strength. Range and default depend on `filter`:\n- `sepia` / `grayscale` / `invert` — `0.0` (off) .. `1.0` (full). Default `1.0`.\n- `blur` — sigma in pixels (`0.0` off, typical 4..16).\n- `saturation` / `brightness` / `contrast` — multiplier (`1.0` unchanged).\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type",
        "filter"
      ],
      "additionalProperties": true
    },
    "indexedStack": {
      "type": "object",
      "description": "Displays a single child selected by index. All children retain state.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "indexedStack"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "sizing": {
          "type": "string",
          "description": "How the stack sizes itself against its children: `loose`, `expand`, `passthrough`."
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "index": {
          "description": "Index of the child to display.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "alignment": {
          "description": "Alignment of the displayed child.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Alignment of the displayed child."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "children": {
          "description": "Candidate children.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "textDirection": {
          "type": "string",
          "description": "Resolution direction for `start`/`end` alignment."
        }
      },
      "required": [
        "type",
        "children"
      ],
      "additionalProperties": true
    },
    "inkWell": {
      "type": "object",
      "description": "Material-style touch feedback with ripple effect.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "inkWell"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "onHighlightChanged": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onHover": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onTapCancel": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onTapDown": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onTapUp": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "autofocus": {
          "description": "Take focus when the surface first appears.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Take focus when the surface first appears."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "canRequestFocus": {
          "description": "Whether the surface is reachable by focus traversal at all.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Whether the surface is reachable by focus traversal at all."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "customBorder": {
          "description": "Shape the ripple and highlight are clipped to.",
          "anyOf": [
            {
              "type": "object",
              "description": "Shape the ripple and highlight are clipped to.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "enableFeedback": {
          "description": "Play the platform tap sound / haptic on activation.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Play the platform tap sound / haptic on activation."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "excludeFromSemantics": {
          "description": "Keep the surface out of the accessibility tree.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Keep the surface out of the accessibility tree."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "focusColor": {
          "description": "Overlay while the surface holds focus.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay while the surface holds focus."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "highlightColor": {
          "description": "Overlay while the surface is held down.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay while the surface is held down."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "hoverColor": {
          "description": "Overlay while a pointer rests on the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay while a pointer rests on the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "overlayColor": {
          "description": "Overlay used for the focus / hover / press states together.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay used for the focus / hover / press states together."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "splashColor": {
          "description": "Colour of the ripple that expands from the touch point.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the ripple that expands from the touch point."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "splashRadius": {
          "description": "Radius the ripple expands to.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Radius the ripple expands to."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "borderRadius": {
          "description": "Ripple clip radius.",
          "anyOf": [
            {
              "type": "number",
              "description": "Ripple clip radius."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "onTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onLongPress": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onDoubleTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "intrinsicHeight": {
      "type": "object",
      "description": "Constrains a child to the intrinsic height required by its content.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "intrinsicHeight"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "intrinsicWidth": {
      "type": "object",
      "description": "Constrains a child to the intrinsic width required by its content.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "intrinsicWidth"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "stepHeight": {
          "description": "Round the intrinsic height up to a multiple of this.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Round the intrinsic height up to a multiple of this."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "stepWidth": {
          "description": "Round the intrinsic width up to a multiple of this.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Round the intrinsic width up to a multiple of this."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "kanban": {
      "type": "object",
      "description": "Cards in columns, moved between columns by dragging.\n\nComposable from `grid` + `draggable` + `dragTarget` in the sense that the\npieces exist — and that composition is where the work actually is. Drop\ntargets must be the gaps between cards rather than the cards themselves,\nthe column must auto-scroll while a card hovers near its edge, and a drop\nmust report *where* in the destination the card landed, not just that it\nmoved. Authors composing this get a board that looks right and reorders\nwrongly.\n\nThe widget owns presentation and gesture. It does **not** own the move:\n`onCardMove` reports the intent and the author's action decides, so a\nserver-rejected move is not silently already applied on screen.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "kanban"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "columns": {
          "description": "`{ key, title, items, limit?, color? }`. `limit` caps a column's card count — a drop that would exceed it is refused at the gesture, not after.",
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "description": "object",
                    "anyOf": [
                      {
                        "type": "object",
                        "description": "object",
                        "additionalProperties": true
                      },
                      {
                        "$ref": "#/$defs/Binding",
                        "description": "Binding expression resolving to this value."
                      }
                    ]
                  }
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression resolving to an array."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "itemTemplate": {
          "$ref": "#/$defs/Widget"
        },
        "itemKey": {
          "type": "string",
          "default": "id",
          "description": "Field identifying a card. Stable identity is what makes a move addressable."
        },
        "draggable": {
          "description": "Whether cards can be moved at all. False renders a read-only board.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether cards can be moved at all. False renders a read-only board."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "columnWidth": {
          "description": "Fixed column width. Omitted distributes available width.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed column width. Omitted distributes available width."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "optimistic": {
          "description": "Move the card on screen before `onCardMove` resolves. False keeps the board as the truth the server confirmed — the safer default when a move can be rejected.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Move the card on screen before `onCardMove` resolves. False keeps the board as the truth the server confirmed — the safer default when a move can be rejected."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onCardMove": {
          "$ref": "#/$defs/Action"
        },
        "onCardClick": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "columns",
        "itemTemplate"
      ],
      "additionalProperties": true
    },
    "kenBurnsImage": {
      "type": "object",
      "description": "Image with an automatic slow zoom-and-pan animation (the \"Ken\nBurns effect\"). Used for cinematic stills in book chapter\nopenings, landing pages, and slideshow frames. The image holds\nintrinsic resolution and is animated within fit-bounds — no\ncropping outside the rendered rectangle.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "kenBurnsImage"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "src": {
          "description": "Image source.",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Image source."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "duration": {
          "description": "Total animation duration in milliseconds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Total animation duration in milliseconds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "intensity": {
          "description": "Zoom amount. The image zooms from 1.0 to (1.0 + `intensity`)\nacross `duration`. Practical range: `0.05` (subtle) ..\n`0.30` (dramatic).\n",
          "anyOf": [
            {
              "type": "number",
              "default": 0.15,
              "description": "Zoom amount. The image zooms from 1.0 to (1.0 + `intensity`)\nacross `duration`. Practical range: `0.05` (subtle) ..\n`0.30` (dramatic).\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "startAlignment": {
          "description": "Pan start position within the rendered bounds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Pan start position within the rendered bounds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "endAlignment": {
          "description": "Pan end position within the rendered bounds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Pan end position within the rendered bounds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "loop": {
          "description": "When true the animation reverses and replays continuously; when false it stops at the end position.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "When true the animation reverses and replays continuously; when false it stops at the end position."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "curve": {
          "description": "Easing curve over the zoom/pan progression.",
          "anyOf": [
            {
              "$ref": "#/$defs/AnimationCurve",
              "description": "Easing curve over the zoom/pan progression."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Render width.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Render width."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "height": {
          "description": "Render height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Render height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "fit": {
          "description": "How the image scales to its render bounds before the Ken Burns animation overlays.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "cover",
                "contain",
                "fill",
                "fitWidth",
                "fitHeight"
              ],
              "default": "cover",
              "description": "How the image scales to its render bounds before the Ken Burns animation overlays."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        }
      },
      "required": [
        "type",
        "src"
      ],
      "additionalProperties": true
    },
    "layoutBuilder": {
      "type": "object",
      "description": "Runtime-only responsive container: picks a child by matching the current constraints against a breakpoint map.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "layoutBuilder"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "breakpoints": {
          "description": "Map of breakpoint name → threshold (e.g., `{ \"sm\": 480, \"md\": 768 }`).",
          "anyOf": [
            {
              "type": "object",
              "description": "Map of breakpoint name → threshold (e.g., `{ \"sm\": 480, \"md\": 768 }`).",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "layouts": {
          "description": "Map of breakpoint name → Widget; matched against `breakpoints`.",
          "anyOf": [
            {
              "type": "object",
              "description": "Map of breakpoint name → Widget; matched against `breakpoints`.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "default": {
          "$ref": "#/$defs/Widget"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "lazy": {
      "type": "object",
      "description": "Defer rendering of an expensive subtree until it enters the viewport (or until explicitly loaded).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "lazy"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "delay": {
          "description": "Milliseconds to wait before building, when `trigger` is `delay`.",
          "anyOf": [
            {
              "type": "number",
              "description": "Milliseconds to wait before building, when `trigger` is `delay`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "placeholder": {
          "$ref": "#/$defs/Widget"
        },
        "content": {
          "description": "Inline widget, or `{ source: \"ui://...\" }` to fetch a remote page fragment. Required when `children` is omitted.",
          "anyOf": [
            {
              "$ref": "#/$defs/Widget"
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "description": "Multiple deferred widgets; runtime wraps them in a linear column.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "trigger": {
          "description": "`visible` renders when the widget becomes visible to the user (typically scrolled into view); `immediate` renders on mount; `manual` renders when a `load()` signal is received.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "visible",
                "immediate",
                "manual"
              ],
              "default": "visible",
              "description": "`visible` renders when the widget becomes visible to the user (typically scrolled into view); `immediate` renders on mount; `manual` renders when a `load()` signal is received."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "onLoad": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onError": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "lightbox": {
      "type": "object",
      "description": "Full-screen modal image viewer with pinch-zoom and swipe-between-\nimages. Used as a tap target on a thumbnail image — opening the\nlightbox transitions the thumbnail into the full-screen view.\nPairs naturally with `hero` for the cover-to-detail morph.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "lightbox"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "images": {
          "description": "Image asset list. Each entry is an AssetRef.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/AssetRef"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "initialIndex": {
          "description": "Index of the image shown first.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Index of the image shown first."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "allowZoom": {
          "description": "Enable pinch-zoom (and double-tap-to-zoom). When false the lightbox shows the image at fit-bounds only.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Enable pinch-zoom (and double-tap-to-zoom). When false the lightbox shows the image at fit-bounds only."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "maxZoom": {
          "description": "Maximum zoom factor (1.0 = fit-bounds).",
          "anyOf": [
            {
              "type": "number",
              "default": 4.0,
              "description": "Maximum zoom factor (1.0 = fit-bounds)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "allowSwipe": {
          "description": "Enable swipe-between-images. When false the lightbox shows only `images[initialIndex]`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Enable swipe-between-images. When false the lightbox shows only `images[initialIndex]`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "backgroundColor": {
          "description": "Backdrop color behind the image (typically black or theme.scrim).",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Backdrop color behind the image (typically black or theme.scrim)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onIndexChanged": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onClose": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "images"
      ],
      "additionalProperties": true
    },
    "limitedBox": {
      "type": "object",
      "description": "Limits a child's size only when it would otherwise be unbounded.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "limitedBox"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "maxWidth": {
          "description": "Upper bound applied when the parent gives unbounded width. Omitted, no\nbound is applied — written as a number, so the \"infinity\" a Flutter\nimplementation uses internally is not a value a document can carry.\n",
          "anyOf": [
            {
              "type": "number",
              "description": "Upper bound applied when the parent gives unbounded width. Omitted, no\nbound is applied — written as a number, so the \"infinity\" a Flutter\nimplementation uses internally is not a value a document can carry.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "maxHeight": {
          "description": "Upper bound applied when the parent gives unbounded height. Omitted, no\nbound is applied.\n",
          "anyOf": [
            {
              "type": "number",
              "description": "Upper bound applied when the parent gives unbounded height. Omitted, no\nbound is applied.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "linear": {
      "type": "object",
      "description": "Linear (flex) layout along a single axis. Replaces separate `row` / `column` widgets — use `direction: \"horizontal\"` or `direction: \"vertical\"`.\n\nMain-axis sizing: when at least one child is `expanded` / `flexible` / `spacer` (or carries a `flex` value), `linear` expands to fill the available main-axis space so those children can distribute the remainder; otherwise it shrink-wraps its children. Authors can override this with the explicit `mainAxisSize` property.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "linear",
            "row",
            "column"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "wrap": {
          "description": "Let children flow onto another line instead of overflowing.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Let children flow onto another line instead of overflowing."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "padding": {
          "description": "Space inside the surface, around its content.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space inside the surface, around its content."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "mainAxisSize": {
          "description": "`max` fills the parent along `direction`; `min` shrink-wraps. Defaults to `max` when a flex child (`expanded`/`flexible`/`spacer`) is present, otherwise `min`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "min",
                "max"
              ],
              "description": "`max` fills the parent along `direction`; `min` shrink-wraps. Defaults to `max` when a flex child (`expanded`/`flexible`/`spacer`) is present, otherwise `min`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "direction": {
          "type": "string",
          "default": "vertical",
          "description": "`\"horizontal\"` or `\"vertical\"`."
        },
        "orientation": {
          "type": "string",
          "default": "vertical",
          "description": "Legacy alias of `direction`."
        },
        "scrollDirection": {
          "type": "string",
          "default": "vertical",
          "description": "Legacy alias of `direction`."
        },
        "alignment": {
          "description": "Cross-axis alignment: `start`, `center`, `end`, `stretch`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "start",
                "center",
                "end",
                "stretch"
              ],
              "default": "start",
              "description": "Cross-axis alignment: `start`, `center`, `end`, `stretch`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "crossAxisAlignment": {
          "description": "Legacy alias of `alignment`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "start",
                "center",
                "end",
                "stretch"
              ],
              "default": "start",
              "description": "Cross-axis alignment: `start`, `center`, `end`, `stretch`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "distribution": {
          "description": "Main-axis distribution: `start` · `center` · `end` · `spaceBetween` ·\n`spaceAround` · `spaceEvenly` (§17.1.3 spells multi-word values in\ncamelCase).\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "start",
                "center",
                "end",
                "spaceBetween",
                "spaceAround",
                "spaceEvenly",
                "space-between",
                "space-around",
                "space-evenly"
              ],
              "default": "start",
              "description": "Main-axis distribution: `start` · `center` · `end` · `spaceBetween` ·\n`spaceAround` · `spaceEvenly` (§17.1.3 spells multi-word values in\ncamelCase).\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "mainAxisAlignment": {
          "description": "Legacy alias of `distribution`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "start",
                "center",
                "end",
                "spaceBetween",
                "spaceAround",
                "spaceEvenly",
                "space-between",
                "space-around",
                "space-evenly"
              ],
              "default": "start",
              "description": "Main-axis distribution: `start` · `center` · `end` · `spaceBetween` ·\n`spaceAround` · `spaceEvenly` (§17.1.3 spells multi-word values in\ncamelCase).\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "spacing": {
          "description": "Gap between children in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Gap between children in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "gap": {
          "description": "Legacy alias of `spacing`.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Gap between children in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "itemSpacing": {
          "description": "Legacy alias of `spacing`.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Gap between children in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "children": {
          "description": "Child widgets arranged along `direction`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        }
      },
      "required": [
        "type",
        "children"
      ],
      "additionalProperties": true
    },
    "link": {
      "type": "object",
      "description": "Text that navigates. `route` goes to an internal route; `url` leaves the\napplication through `navigation.openUrl` (§4.3.3). Exactly one of the two\nis set — a link that could mean either is a link whose destination the\nauthor did not decide.\n\nComposable from `inkWell` + `text` + an action, and the composition is\nwhat most documents do today. It loses the link role (assistive technology\nannounces \"button\"), the visited/hover affordances, and — for `url` — the\nexternal-destination cue a user needs before leaving the app.\n\n`navLink` is the same widget with `activeWhen` in use: a link that knows\nwhether it points at where you already are.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "link",
            "navLink"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "label": {
          "type": "string",
          "description": "Link text. Use `child` instead when the link wraps richer content."
        },
        "route": {
          "type": "string",
          "description": "Internal route. Mutually exclusive with `url`."
        },
        "params": {
          "description": "Route parameters. Ignored when `url` is set.",
          "anyOf": [
            {
              "type": "object",
              "description": "Route parameters. Ignored when `url` is set.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "url": {
          "type": "string",
          "description": "External URL, opened per §4.3.3. Mutually exclusive with `route`."
        },
        "target": {
          "description": "Hint for `url` links only; a host with no tabs MAY ignore it.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "new",
                "same"
              ],
              "default": "new",
              "description": "Hint for `url` links only; a host with no tabs MAY ignore it."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "activeWhen": {
          "description": "Route (or expression) that marks this link as the current location. Sets the active style and `aria-current`.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "underline": {
          "description": "Underline policy.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "always",
                "hover",
                "never"
              ],
              "default": "hover",
              "description": "Underline policy."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "icon": {
          "description": "Leading icon.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Leading icon."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        },
        "onClick": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "label"
      ],
      "additionalProperties": true
    },
    "list": {
      "type": "object",
      "description": "Scrollable linear collection rendered from an array binding.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "list",
            "listView",
            "listview"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "itemBuilder": {
          "$ref": "#/$defs/Widget"
        },
        "itemCount": {
          "description": "How many items to build when the list is generated rather than listed.",
          "anyOf": [
            {
              "type": "number",
              "description": "How many items to build when the list is generated rather than listed."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "scrollCacheExtent": {
          "description": "Pixels kept built beyond the viewport on each side.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Pixels kept built beyond the viewport on each side."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "padding": {
          "description": "Space inside the surface, around its content.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space inside the surface, around its content."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "physics": {
          "type": "string",
          "description": "Scroll response: `bouncing`, `clamping`, `never`, `always`."
        },
        "shrinkWrap": {
          "description": "Size to the content instead of filling the viewport.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Size to the content instead of filling the viewport."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "virtual": {
          "description": "Render only visible items. Requires `itemHeight` — a virtualised list cannot measure items it has not built.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Render only visible items. Requires `itemHeight` — a virtualised list cannot measure items it has not built."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "itemHeight": {
          "description": "Fixed item height in logical pixels. Required when `virtual` is true.",
          "anyOf": [
            {
              "type": "number",
              "description": "Fixed item height in logical pixels. Required when `virtual` is true."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "overscan": {
          "description": "Items rendered beyond the viewport on each side, trading memory for scroll smoothness.",
          "anyOf": [
            {
              "type": "number",
              "default": 3,
              "description": "Items rendered beyond the viewport on each side, trading memory for scroll smoothness."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "items": {
          "description": "Array source. Required when `children` is omitted (dynamic list).",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "itemTemplate": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "description": "Static list of widgets. Mutually exclusive with `items` + `itemTemplate`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "spacing": {
          "description": "Gap between items.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Gap between items."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "orientation": {
          "type": "string",
          "default": "vertical",
          "description": "`vertical` or `horizontal`."
        },
        "emptyMessage": {
          "type": "string",
          "description": "Displayed when the list is empty."
        },
        "itemExtent": {
          "description": "Fixed item size for performance.",
          "anyOf": [
            {
              "type": "number",
              "description": "Fixed item size for performance."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "reverse": {
          "description": "When true, run the transition in reverse direction (for back navigation).",
          "anyOf": [
            {
              "type": "boolean",
              "description": "When true, run the transition in reverse direction (for back navigation)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "scrollDirection": {
          "type": "string",
          "description": "Scroll axis."
        },
        "template": {
          "description": "Template name to instantiate.",
          "anyOf": [
            {
              "type": "object",
              "additionalProperties": true
            },
            {
              "type": "string"
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "listItem": {
      "type": "object",
      "description": "Single row in a list with optional leading/trailing widgets. Replaces Material `listTile` naming.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "listItem",
            "listTile",
            "list-tile"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "contentPadding": {
          "description": "Space inside the row, around its content.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space inside the row, around its content."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "dense": {
          "description": "Use the compact row height.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Use the compact row height."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "isThreeLine": {
          "description": "Reserve room for a three-line row.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Reserve room for a three-line row."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "selectedTileColor": {
          "description": "Background of the row while selected.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Background of the row while selected."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tileColor": {
          "description": "Background of the row.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Background of the row."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "focusColor": {
          "description": "Overlay while the surface holds focus.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay while the surface holds focus."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "hoverColor": {
          "description": "Overlay while a pointer rests on the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay while a pointer rests on the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "iconColor": {
          "description": "Colour applied to the icon.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour applied to the icon."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "Outline of the surface, as a shape descriptor.",
          "anyOf": [
            {
              "type": "object",
              "description": "Outline of the surface, as a shape descriptor.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "title": {
          "description": "Primary text, or an inline Widget for rich titles.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/Widget"
            }
          ]
        },
        "subtitle": {
          "description": "Secondary text, or an inline Widget.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "$ref": "#/$defs/Widget"
            }
          ]
        },
        "leading": {
          "$ref": "#/$defs/Widget"
        },
        "trailing": {
          "$ref": "#/$defs/Widget"
        },
        "onTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "selected": {
          "description": "Selected state.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Selected state."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "enabled": {
          "description": "Whether the item is interactive.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the item is interactive."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onLongPress": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "textColor": {
          "description": "Default text color",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Default text color"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "lottieAnimation": {
      "type": "object",
      "description": "Embedded Lottie/JSON animation playback.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "lottieAnimation"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "fit": {
          "type": "string",
          "description": "How the animation scales to its bounds."
        },
        "onComplete": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "speed": {
          "description": "Playback rate; `1.0` is the authored speed.",
          "anyOf": [
            {
              "type": "number",
              "description": "Playback rate; `1.0` is the authored speed."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Fixed height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "src": {
          "description": "Animation source. Any `AssetRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Animation source. Any `AssetRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "source": {
          "description": "Legacy alias of `src`.",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Animation source. Any `AssetRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "autoPlay": {
          "description": "Play on mount.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Play on mount."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "loop": {
          "description": "Loop playback.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Loop playback."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "map": {
      "type": "object",
      "description": "Geographic map with markers and overlays.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "map"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "markerColor": {
          "description": "Default colour for a marker with no colour of its own.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Default colour for a marker with no colour of its own."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "showCoordinates": {
          "description": "Show the coordinate readout.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Show the coordinate readout."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showGrid": {
          "description": "Draw the coordinate grid.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Draw the coordinate grid."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "height": {
          "description": "Fixed height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "interactive": {
          "description": "Whether the surface responds to pointer input.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Whether the surface responds to pointer input."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "center": {
          "description": "Map center coordinates. Required when flat `latitude` / `longitude` are omitted.",
          "anyOf": [
            {
              "description": "Map center coordinates. Required when flat `latitude` / `longitude` are omitted."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "latitude": {
          "description": "Flat shorthand for `center.latitude` — legacy alternative to `center`.",
          "anyOf": [
            {
              "type": "number",
              "description": "Flat shorthand for `center.latitude` — legacy alternative to `center`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "longitude": {
          "description": "Flat shorthand for `center.longitude` — legacy alternative to `center`.",
          "anyOf": [
            {
              "type": "number",
              "description": "Flat shorthand for `center.longitude` — legacy alternative to `center`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "zoom": {
          "description": "Zoom level (0–22)",
          "anyOf": [
            {
              "type": "number",
              "default": 13,
              "description": "Zoom level (0–22)"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "mapType": {
          "description": "Base tile set.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "standard",
                "satellite",
                "terrain",
                "hybrid"
              ],
              "default": "standard",
              "description": "Base tile set."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "markers": {
          "description": "Point markers",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Marker"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "overlays": {
          "description": "Polygon / polyline / circle overlays",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Overlay"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "onMarkerTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onMapTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "gridColor": {
          "description": "Grid color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Grid color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "margin": {
      "type": "object",
      "description": "Applies outer margin around a single child.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "margin"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "margin": {
          "description": "Outer margin.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Outer margin."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "value": {
          "description": "Legacy alias of `margin`.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Outer margin."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "markdown": {
      "type": "object",
      "description": "Markdown renderer.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "markdown"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "text": {
          "description": "Markdown source",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "content": {
          "description": "Legacy alias of `text`.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "selectable": {
          "description": "Allow text selection",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Allow text selection"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "width": {
          "description": "Widget width",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget width"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Widget height",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget height"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "fontSize": {
          "description": "Base font size",
          "anyOf": [
            {
              "type": "number",
              "description": "Base font size"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "textColor": {
          "description": "Default text color",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Default text color"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Surface behind the rendered document",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Surface behind the rendered document"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "linkColor": {
          "description": "Hyperlink color",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Hyperlink color"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "codeBackgroundColor": {
          "description": "Code-block background",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Code-block background"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onLinkTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "mediaPlayer": {
      "type": "object",
      "description": "Audio / video player widget.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "mediaPlayer",
            "video",
            "audio"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "accentColor": {
          "description": "Colour of the progress bar and active controls.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the progress bar and active controls."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "controlsColor": {
          "description": "Colour of the transport controls.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the transport controls."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onSeek": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "source": {
          "description": "Media asset reference (audio or video file).",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Media asset reference (audio or video file)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "src": {
          "description": "Legacy alias of `source`.",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Media asset reference (audio or video file)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "mediaType": {
          "description": "Media kind. Omitted, it is inferred from the source's extension — an\n`.mp3` is audio — and anything unrecognised is treated as video.\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "audio",
                "video"
              ],
              "description": "Media kind. Omitted, it is inferred from the source's extension — an\n`.mp3` is audio — and anything unrecognised is treated as video.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "autoPlay": {
          "description": "Start playing automatically",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Start playing automatically"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "loop": {
          "description": "Loop at end",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Loop at end"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "muted": {
          "description": "Start muted",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Start muted"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "volume": {
          "description": "Volume 0.0–1.0",
          "anyOf": [
            {
              "type": "number",
              "default": 1.0,
              "description": "Volume 0.0–1.0"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "controls": {
          "description": "Show native controls",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Show native controls"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "poster": {
          "description": "Preview image rendered before playback starts (video only).",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Preview image rendered before playback starts (video only)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "waveform": {
          "description": "Audio mode only. When true, the audio's amplitude waveform is\nrendered above the transport controls, advancing with playback.\nIgnored for video sources.\n",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Audio mode only. When true, the audio's amplitude waveform is\nrendered above the transport controls, advancing with playback.\nIgnored for video sources.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "width": {
          "description": "Display width",
          "anyOf": [
            {
              "type": "number",
              "description": "Display width"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Display height",
          "anyOf": [
            {
              "type": "number",
              "description": "Display height"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "onPlay": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onPause": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onEnded": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onTimeUpdate": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onError": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "duration": {
          "description": "Animation duration in milliseconds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Animation duration in milliseconds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "title": {
          "type": "string",
          "description": "Dialog title shown at the top."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "mediaQuery": {
      "type": "object",
      "description": "Responsive widget that inspects the current layout context and picks a child based on media-query conditions. Supports a `then`/`else` expression form and a `breakpoints` map form. The binding engine exposes `{{media.width}}`, `{{media.height}}`, `{{media.orientation}}` inside `condition`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "mediaQuery"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "condition": {
          "description": "Boolean expression (string) or constraint object like `{minWidth, maxWidth}`; selects `then` when truthy.",
          "anyOf": [
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "then": {
          "$ref": "#/$defs/Widget"
        },
        "else": {
          "$ref": "#/$defs/Widget"
        },
        "breakpoints": {
          "description": "Map of breakpoint name → Widget (e.g., `{ \"sm\": ..., \"md\": ... }`).",
          "anyOf": [
            {
              "type": "object",
              "description": "Map of breakpoint name → Widget (e.g., `{ \"sm\": ..., \"md\": ... }`).",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "defaultChild": {
          "$ref": "#/$defs/Widget"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "menu": {
      "type": "object",
      "description": "Standing list of navigation destinations, optionally nested. Unlike\n`popupMenuButton` (a trigger that opens a transient list) this one is part\nof the layout — a sidebar or an inline section list.\n\n`navigationRail` covers the icon-rail form on one axis with a flat item\nset; this covers nesting, collapsing, and the expanded/collapsed sidebar\nthat admin screens are built from.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "menu"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "items": {
          "description": "`{ key, label, icon?, route?, params?, children?, enabled? }`. `children` nests one or more levels; an item with `children` is a group, not a destination.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "description": "object",
                "anyOf": [
                  {
                    "type": "object",
                    "description": "object",
                    "additionalProperties": true
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "selectedKey": {
          "description": "Two-way bound key of the active item.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "openKeys": {
          "description": "Two-way bound keys of expanded groups.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "mode": {
          "description": "`inline` expands groups in place; `vertical`/`horizontal` open them as flyouts.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "vertical",
                "horizontal",
                "inline"
              ],
              "default": "vertical",
              "description": "`inline` expands groups in place; `vertical`/`horizontal` open them as flyouts."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "collapsed": {
          "description": "Collapse to icons only. Labels move into tooltips rather than disappearing.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "onSelect": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "items"
      ],
      "additionalProperties": true
    },
    "multiSelect": {
      "type": "object",
      "description": "Dropdown selection of more than one value. Shared rows per §2.6.0;\n`binding` holds an **array** of selected values.\n\nDistinct from `checkboxGroup`, which lays every option out at once: this\nkeeps the closed/summary form of `select` and is what a long option list\nneeds. Distinct from `select`, which is single-value — the two are kept\napart rather than folded into a `multiple` flag because the bound value\nchanges shape (scalar vs array), and a flag that silently changes the\ntype of what lands in state is the kind of thing an author discovers at\nruntime.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "multiSelect"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        },
        "options": {
          "description": "`{ value, label, icon? }` entries.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "items": {
          "description": "Legacy alias of `options`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "placeholder": {
          "type": "string",
          "description": "Shown when nothing is selected."
        },
        "maxSelections": {
          "description": "Upper bound on selected values. Reaching it disables unselected rows rather than silently dropping a pick.",
          "anyOf": [
            {
              "type": "number",
              "description": "Upper bound on selected values. Reaching it disables unselected rows rather than silently dropping a pick."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "showChips": {
          "description": "Render selections as removable chips in the closed field.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Render selections as removable chips in the closed field."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "selectAll": {
          "description": "Offer a select-all / clear-all control.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Offer a select-all / clear-all control."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "searchable": {
          "description": "Filter options by typing. Filtering never adds values outside `options` — see `combobox` for free entry.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Filter options by typing. Filtering never adds values outside `options` — see `combobox` for free entry."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "navigationRail": {
      "type": "object",
      "description": "Vertical navigation rail for tablet/desktop layouts. Each item's text field is `label`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "navigationRail"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "extended": {
          "description": "Show labels beside the icons instead of under them.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Show labels beside the icons instead of under them."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "groupAlignment": {
          "description": "Where the destination group sits vertically, `-1.0` (top) to `1.0` (bottom).",
          "anyOf": [
            {
              "type": "number",
              "description": "Where the destination group sits vertically, `-1.0` (top) to `1.0` (bottom)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "labelType": {
          "type": "string",
          "description": "When labels appear: `none`, `selected`, `all`."
        },
        "minExtendedWidth": {
          "description": "Width of the rail once `extended` is set.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Width of the rail once `extended` is set."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "minWidth": {
          "description": "Width of the rail in its collapsed form.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Width of the rail in its collapsed form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "selectedIconTheme": {
          "description": "Icon size / colour for the selected destination.",
          "anyOf": [
            {
              "type": "object",
              "description": "Icon size / colour for the selected destination.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "selectedLabelTextStyle": {
          "description": "Text style for the selected label.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Text style for the selected label."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "unselectedIconTheme": {
          "description": "Icon size / colour for destinations that are not selected.",
          "anyOf": [
            {
              "type": "object",
              "description": "Icon size / colour for destinations that are not selected.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "unselectedLabelTextStyle": {
          "description": "Text style for labels that are not selected.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Text style for labels that are not selected."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "selectedIndex": {
          "description": "Currently selected item.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "items": {
          "description": "`{ icon, label, route? }` entries.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/NavItem"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "destinations": {
          "description": "Legacy alias of `items`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/NavItem"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow depth of the rail surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth of the rail surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "leading": {
          "$ref": "#/$defs/Widget"
        },
        "onSelect": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "trailing": {
          "$ref": "#/$defs/Widget"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "networkGraph": {
      "type": "object",
      "description": "Network topology graph. Same node/edge model as `graph`, with topology-oriented defaults (hierarchical layout, directed edges).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "networkGraph"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "onNodeTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "edgeColor": {
          "description": "Default colour for an edge with no colour of its own.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Default colour for an edge with no colour of its own."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "edges": {
          "description": "Edges as `{ from, to, label?, color? }`. Literal array or binding — a topology that comes from the server is bound like any other data (`heatmap.data`, `dataTable.rows`, `kanban.columns` all read this way).",
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "description": "object",
                    "anyOf": [
                      {
                        "type": "object",
                        "description": "object",
                        "additionalProperties": true
                      },
                      {
                        "$ref": "#/$defs/Binding",
                        "description": "Binding expression resolving to this value."
                      }
                    ]
                  }
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression resolving to an array."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "layout": {
          "type": "string",
          "description": "Placement algorithm: `force`, `circular`, `grid`, `tree`."
        },
        "nodeColor": {
          "description": "Default fill for a node with no colour of its own.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Default fill for a node with no colour of its own."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "nodes": {
          "description": "Nodes as `{ id, label?, color? }`. Literal array or binding.",
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "description": "object",
                    "anyOf": [
                      {
                        "type": "object",
                        "description": "object",
                        "additionalProperties": true
                      },
                      {
                        "$ref": "#/$defs/Binding",
                        "description": "Binding expression resolving to this value."
                      }
                    ]
                  }
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression resolving to an array."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "onEdgeTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "height": {
          "description": "Fixed height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "interactive": {
          "description": "Whether the surface responds to pointer input.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Whether the surface responds to pointer input."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "labelColor": {
          "description": "Colour applied to the label text.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour applied to the label text."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "numberField": {
      "type": "object",
      "description": "Specialized input for numeric values. Shared rows per §2.6.0; `value` is `number`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "numberField",
            "numberInput"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "error": {
          "type": "string",
          "description": "Error message shown under the field."
        },
        "showStepper": {
          "description": "Render increment/decrement controls. False leaves keyboard entry only.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Render increment/decrement controls. False leaves keyboard entry only."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label."
        },
        "min": {
          "description": "Minimum value.",
          "anyOf": [
            {
              "type": "number",
              "description": "Minimum value."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "max": {
          "description": "Maximum value.",
          "anyOf": [
            {
              "type": "number",
              "description": "Maximum value."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "step": {
          "description": "Increment size.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Increment size."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "decimalPlaces": {
          "description": "Decimal precision.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Decimal precision."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "prefix": {
          "type": "string",
          "description": "Leading display text (e.g., `\"$\"`)."
        },
        "suffix": {
          "type": "string",
          "description": "Trailing display text."
        },
        "thousandSeparator": {
          "description": "Thousands separator for display.",
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "string"
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "format": {
          "type": "string",
          "description": "Display format."
        },
        "helperText": {
          "type": "string",
          "description": "Helper text shown below the field."
        },
        "hint": {
          "type": "string",
          "description": "Legacy alias of `placeholder`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "numberStepper": {
      "type": "object",
      "description": "Incremental numeric input with plus/minus buttons. Shared rows per §2.6.0; `value` is `number`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "numberStepper"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "size": {
          "description": "Control size preset. Defaults to `medium`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "small",
                "medium",
                "large"
              ],
              "description": "Control size preset. Defaults to `medium`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        },
        "min": {
          "description": "Minimum value.",
          "anyOf": [
            {
              "type": "number",
              "description": "Minimum value."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "max": {
          "description": "Maximum value.",
          "anyOf": [
            {
              "type": "number",
              "description": "Maximum value."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "step": {
          "description": "Increment size.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Increment size."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "color": {
          "description": "Colour of the increment / decrement controls.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the increment / decrement controls."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "offlineFallback": {
      "type": "object",
      "description": "Swaps between an online widget and an offline fallback based on connectivity tracked by the runtime. Connectivity state may be pinned by an `isOnline` binding for tests.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "offlineFallback"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "online": {
          "$ref": "#/$defs/Widget"
        },
        "offline": {
          "$ref": "#/$defs/Widget"
        },
        "message": {
          "type": "string",
          "description": "Message shown inside the offline state."
        },
        "icon": {
          "description": "Icon rendered in the default offline state. Any `IconRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Icon rendered in the default offline state. Any `IconRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "showRetry": {
          "description": "Whether to show a retry affordance.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether to show a retry affordance."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onRetry": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "isOnline": {
          "description": "Explicit connectivity override.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "opacity": {
      "type": "object",
      "description": "Wraps a child with opacity, with optional animation.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "opacity"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "opacity": {
          "description": "Opacity in `0.0..1.0`.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "animated": {
          "description": "Animate opacity changes.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Animate opacity changes."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "duration": {
          "description": "Animation duration in milliseconds (when `animated: true`).",
          "anyOf": [
            {
              "type": "number",
              "default": 300,
              "description": "Animation duration in milliseconds (when `animated: true`)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "curve": {
          "type": "string",
          "default": "easeInOut",
          "description": "Animation curve (when `animated: true`)."
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type",
        "opacity"
      ],
      "additionalProperties": true
    },
    "otpInput": {
      "type": "object",
      "description": "Fixed-length one-time-code entry. Shared rows per §2.6.0; `binding` holds\nthe concatenated string, not per-cell values.\n\nThis exists because the composed version is reliably broken. A row of\n`textInput`s loses: pasting a full code (it lands entirely in the first\ncell), forward focus movement on entry and backward on delete, and the\nplatform's one-time-code autofill, which needs a single field declaring\nthat intent. None of the three can be recovered by the author.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "otpInput"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "length": {
          "description": "Number of cells.",
          "anyOf": [
            {
              "type": "number",
              "default": 6,
              "description": "Number of cells."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "inputType": {
          "description": "Restricts accepted characters and selects the on-screen keyboard.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "numeric",
                "alphanumeric"
              ],
              "default": "numeric",
              "description": "Restricts accepted characters and selects the on-screen keyboard."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "onComplete": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "masked": {
          "description": "Obscure entered characters.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Obscure entered characters."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "autofill": {
          "description": "Declare one-time-code autofill to the platform. A runtime without it renders normally rather than failing.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Declare one-time-code autofill to the platform. A runtime without it renders normally rather than failing."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "padding": {
      "type": "object",
      "description": "Applies padding around a single child.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "padding"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "padding": {
          "description": "Inset values.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Inset values."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type",
        "padding"
      ],
      "additionalProperties": true
    },
    "pageView": {
      "type": "object",
      "description": "Full-viewport paged scroll view. Snaps one page per swipe; ideal\nfor book pages, photo galleries, onboarding flows. Distinct from\n`carousel` (partial-viewport, peeking neighbours) — pageView is\nalways one full page. Each child is rendered as a separate page.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "pageView"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "padEnds": {
          "description": "Pad the first and last page so they can centre.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Pad the first and last page so they can centre."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "pageSnapping": {
          "description": "Snap to page boundaries when the drag ends.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Snap to page boundaries when the drag ends."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "direction": {
          "description": "Scroll axis.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "horizontal",
                "vertical"
              ],
              "default": "horizontal",
              "description": "Scroll axis."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "children": {
          "description": "One child per page.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "initialPage": {
          "description": "Index of the page rendered first.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Index of the page rendered first."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "loop": {
          "description": "When true the list wraps around — last → first.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "When true the list wraps around — last → first."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "scrollPhysics": {
          "description": "Edge / overscroll behaviour. `bouncing` mirrors iOS rubber-band;\n`clamping` is the Android-style hard stop; `neverScrollable`\ndisables user dragging (use programmatic page change only).\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "bouncing",
                "clamping",
                "neverScrollable"
              ],
              "default": "clamping",
              "description": "Edge / overscroll behaviour. `bouncing` mirrors iOS rubber-band;\n`clamping` is the Android-style hard stop; `neverScrollable`\ndisables user dragging (use programmatic page change only).\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "allowImplicitScrolling": {
          "description": "Pre-render the adjacent pages off-screen so subsequent swipes\nare instant (assistive-technology friendly). Costs memory.\n",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Pre-render the adjacent pages off-screen so subsequent swipes\nare instant (assistive-technology friendly). Costs memory.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onPageChanged": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "reverse": {
          "description": "When true, run the transition in reverse direction (for back navigation).",
          "anyOf": [
            {
              "type": "boolean",
              "description": "When true, run the transition in reverse direction (for back navigation)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "scrollDirection": {
          "type": "string",
          "description": "Scroll axis."
        }
      },
      "required": [
        "type",
        "children"
      ],
      "additionalProperties": true
    },
    "pagination": {
      "type": "object",
      "description": "Page selector for a paged collection. `binding` holds the current page\n(1-based).\n\nThe composed version is where off-by-one bugs live: first/last bounds,\nthe ellipsis window, and disabling prev/next at the ends are re-derived\nby every author. Stating it once also fixes what a screen reader hears —\n\"page 3 of 12\", not \"3\".",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "pagination"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "current": {
          "description": "Page currently shown, 1-based.",
          "anyOf": [
            {
              "type": "number",
              "description": "Page currently shown, 1-based."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "binding": {
          "type": "string",
          "description": "State path holding the current page. 1-based."
        },
        "total": {
          "description": "Total item count (not page count) — the widget derives pages from `pageSize`.",
          "anyOf": [
            {
              "type": "number",
              "description": "Total item count (not page count) — the widget derives pages from `pageSize`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "pageSize": {
          "description": "Items per page.",
          "anyOf": [
            {
              "type": "number",
              "default": 20,
              "description": "Items per page."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "siblingCount": {
          "description": "Page links shown either side of the current page before the trail is elided.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Page links shown either side of the current page before the trail is elided."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "showSizeChanger": {
          "description": "Offer a page-size selector.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Offer a page-size selector."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "pageSizeOptions": {
          "description": "Choices for the size selector. Ignored when `showSizeChanger` is false.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "string",
                    "pattern": "^\\{\\{.*\\}\\}$",
                    "description": "Binding expression."
                  },
                  {
                    "type": "object",
                    "properties": {
                      "value": {
                        "type": "number"
                      },
                      "unit": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "value"
                    ],
                    "description": "Dimension object: `{value, unit}`."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "showTotal": {
          "description": "Render a total-count summary.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Render a total-count summary."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "total"
      ],
      "additionalProperties": true
    },
    "pdfViewer": {
      "type": "object",
      "description": "Renders a PDF document with page navigation and zoom.\n\n`webView` can display a PDF where the host has a plugin for it, and that is\nthe substitution to avoid: the document then belongs to the browser, so\npage position, zoom, and search are outside the DSL's reach and outside\nthe app's theme. A viewer widget keeps them addressable.\n\n`src` is an `AssetRef`, so the same document works from a bundle, a URL, a\npicked file (`fileInput` writes a `data:` URI), or a server resource.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "pdfViewer"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "height": {
          "description": "Fixed height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "src": {
          "description": "Document source. Any `AssetRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Document source. Any `AssetRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "page": {
          "description": "Current page, 1-based. One-way — the embedded viewer reports no page change back.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "zoom": {
          "description": "Zoom factor; `1.0` is fit-width. One-way — the embedded viewer reports no zoom change back.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "showToolbar": {
          "description": "Render the built-in toolbar. False leaves navigation entirely to bindings.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Render the built-in toolbar. False leaves navigation entirely to bindings."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showPageNav": {
          "description": "Page stepper within the toolbar.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Page stepper within the toolbar."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showZoom": {
          "description": "Zoom controls within the toolbar.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Zoom controls within the toolbar."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "fit": {
          "description": "Initial fit.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "width",
                "height",
                "page"
              ],
              "default": "width",
              "description": "Initial fit."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "onLoad": {
          "$ref": "#/$defs/Action"
        },
        "onPageChange": {
          "$ref": "#/$defs/Action"
        },
        "onError": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "src"
      ],
      "additionalProperties": true
    },
    "permissionPrompt": {
      "type": "object",
      "description": "Presents a prompt for one or more client capabilities (e.g., clipboard, filesystem). See [`07_Security.md`](07_Security.md) for the permission vocabulary.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "permissionPrompt"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "permissions": {
          "description": "Permission identifiers (e.g., `[\"client.clipboard\"]`). Required when `permissionType` is omitted.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "permissionType": {
          "type": "string",
          "description": "Single-permission shorthand for `permissions: [permissionType]`."
        },
        "style": {
          "description": "`inline`, `dialog`, `banner`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "inline",
                "dialog",
                "banner"
              ],
              "default": "inline",
              "description": "`inline`, `dialog`, `banner`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "title": {
          "type": "string",
          "description": "Prompt title."
        },
        "description": {
          "type": "string",
          "description": "Prompt body."
        },
        "icon": {
          "description": "Icon shown alongside the prompt. Any `IconRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Icon shown alongside the prompt. Any `IconRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "allowPartial": {
          "description": "Allow the user to grant a subset.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Allow the user to grant a subset."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onAllow": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onDeny": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "placeholder": {
      "type": "object",
      "description": "Empty placeholder widget with optional dimensions and color.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "placeholder",
            "skeleton"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "fallbackWidth": {
          "description": "Width when unconstrained.",
          "anyOf": [
            {
              "type": "number",
              "description": "Width when unconstrained."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "fallbackHeight": {
          "description": "Height when unconstrained.",
          "anyOf": [
            {
              "type": "number",
              "description": "Height when unconstrained."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "color": {
          "description": "Fill color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Fill color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "strokeWidth": {
          "description": "Line stroke width.",
          "anyOf": [
            {
              "type": "number",
              "description": "Line stroke width."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "popover": {
      "type": "object",
      "description": "Transient surface anchored to a trigger. `child` is the trigger; `content`\nis what appears.\n\nDistinct from `tooltip` (text only, no focusable content) and from\n`customDialog` (modal, centred, takes the whole screen's attention). A\npopover keeps the page usable behind it and is positioned against its\nanchor, which is the part composition cannot supply: flipping to the other\nside when it would overflow the viewport, and returning focus to the\ntrigger on dismiss.\n\n`hoverCard` is the same widget with `trigger: \"hover\"`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "popover",
            "hoverCard"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "content": {
          "$ref": "#/$defs/Widget"
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "open": {
          "description": "Two-way open state. Bind it to drive the popover from actions instead of the trigger.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "trigger": {
          "description": "`manual` means only `open` controls it.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "tap",
                "hover",
                "focus",
                "manual"
              ],
              "default": "tap",
              "description": "`manual` means only `open` controls it."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "placement": {
          "description": "Preferred side. `auto` picks the side with room; any value flips when it would overflow.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "top",
                "bottom",
                "start",
                "end",
                "auto"
              ],
              "default": "auto",
              "description": "Preferred side. `auto` picks the side with room; any value flips when it would overflow."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "openDelay": {
          "description": "Milliseconds before opening. Chiefly for `hover`.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Milliseconds before opening. Chiefly for `hover`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "closeDelay": {
          "description": "Milliseconds before closing after the trigger is released.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Milliseconds before closing after the trigger is released."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "dismissOnOutside": {
          "description": "Close when the user interacts outside the surface.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Close when the user interacts outside the surface."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        },
        "onOpen": {
          "$ref": "#/$defs/Action"
        },
        "onClose": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "content"
      ],
      "additionalProperties": true
    },
    "popupMenuButton": {
      "type": "object",
      "description": "Button that reveals a popup menu of options.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "popupMenuButton",
            "dropdownMenu"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "onCanceled": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onOpened": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "iconSize": {
          "description": "Rendered size of the icon.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Rendered size of the icon."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "offset": {
          "description": "Displacement from the anchor, as `{ dx, dy }`.",
          "anyOf": [
            {
              "type": "object",
              "description": "Displacement from the anchor, as `{ dx, dy }`.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "padding": {
          "description": "Space inside the surface, around its content.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space inside the surface, around its content."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shadowColor": {
          "description": "Colour of the drop shadow cast by the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the drop shadow cast by the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "Outline of the surface, as a shape descriptor.",
          "anyOf": [
            {
              "type": "object",
              "description": "Outline of the surface, as a shape descriptor.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "splashRadius": {
          "description": "Radius the ripple expands to.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Radius the ripple expands to."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "surfaceTintColor": {
          "description": "Elevation tint applied over the surface colour (M3).",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Elevation tint applied over the surface colour (M3)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "icon": {
          "description": "Trigger icon. Any `IconRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Trigger icon. Any `IconRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "items": {
          "description": "`{ value, label, icon?, enabled? }` entries.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/MenuItem"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "onSelect": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "color": {
          "description": "Background of the open menu surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Background of the open menu surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow depth of the open menu.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth of the open menu."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "items"
      ],
      "additionalProperties": true
    },
    "positioned": {
      "type": "object",
      "description": "Positions a child within a `stack` using offsets.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "positioned"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "height": {
          "description": "Fixed height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "bottom": {
          "description": "Distance from the parent stack's bottom edge.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Distance from the parent stack's bottom edge."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "left": {
          "description": "Distance from the parent stack's left edge.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Distance from the parent stack's left edge."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "right": {
          "description": "Distance from the parent stack's right edge.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Distance from the parent stack's right edge."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "top": {
          "description": "Distance from the parent stack's top edge.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Distance from the parent stack's top edge."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "progressBar": {
      "type": "object",
      "description": "Progress indicator (linear or circular).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "progressBar",
            "linearProgressIndicator",
            "circularProgressIndicator",
            "loadingIndicator",
            "loading-indicator",
            "progress-bar",
            "progress"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "size": {
          "description": "Rendered size.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Rendered size."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "strokeWidth": {
          "description": "Thickness of the drawn stroke.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Thickness of the drawn stroke."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "value": {
          "description": "Progress in `0.0..1.0`; omit for indeterminate.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "indicatorType": {
          "type": "string",
          "default": "circular",
          "description": "`linear` or `circular`. The widget type can name the shape instead:\n`linearProgressIndicator` and `circularProgressIndicator` are linear and\ncircular respectively, and this property overrides either. Circular is\nthe default because a bar has no intrinsic width — a bare `progressBar`\ninside a row asserts at layout time rather than rendering.\n"
        },
        "color": {
          "description": "Foreground color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Foreground color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Track color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Track color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "qrCode": {
      "type": "object",
      "description": "Renders a QR code for `value`.\n\nCannot be composed — the module grid is computed, not laid out. The\ndefinition is small in exchange, and it pairs with the scanning side the\nplatform already has: a code produced here is the same artefact §8.9 reads\non arrival.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "qrCode"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "value": {
          "description": "Encoded payload. A URL, a `ui://` route, or an entry token.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "size": {
          "description": "Edge length in logical pixels. The grid is square.",
          "anyOf": [
            {
              "type": "number",
              "default": 200,
              "description": "Edge length in logical pixels. The grid is square."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "errorCorrection": {
          "description": "Error-correction level: `low` (7%), `medium` (15%), `quartile` (25%),\n`high` (30%).\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "low",
                "medium",
                "quartile",
                "high",
                "L",
                "M",
                "Q",
                "H"
              ],
              "default": "medium",
              "description": "Error-correction level: `low` (7%), `medium` (15%), `quartile` (25%),\n`high` (30%).\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "foregroundColor": {
          "description": "Module color. Contrast against `backgroundColor` must stay high enough to scan; a runtime SHOULD refuse to render below it rather than emit an unreadable code.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Module color. Contrast against `backgroundColor` must stay high enough to scan; a runtime SHOULD refuse to render below it rather than emit an unreadable code."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Quiet-zone and gap color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Quiet-zone and gap color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "margin": {
          "description": "Include the quiet zone. Omitting it breaks scanning against busy backgrounds.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Include the quiet zone. Omitting it breaks scanning against busy backgrounds."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "logo": {
          "description": "Image centred over the code. Requires a higher `errorCorrection` to stay scannable.",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Image centred over the code. Requires a higher `errorCorrection` to stay scannable."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "value"
      ],
      "additionalProperties": true
    },
    "radio": {
      "type": "object",
      "description": "Single radio button. The group's selected value is bound via `groupValue` (or the enclosing `radioGroup`'s `binding`).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "radio"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "activeColor": {
          "description": "Colour of the selected / active portion.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the selected / active portion."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "focusColor": {
          "description": "Overlay while the surface holds focus.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay while the surface holds focus."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "hoverColor": {
          "description": "Overlay while a pointer rests on the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay while a pointer rests on the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "splashRadius": {
          "description": "Radius the ripple expands to.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Radius the ripple expands to."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "value": {
          "description": "This button's value.",
          "anyOf": [
            {
              "description": "This button's value."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "groupValue": {
          "description": "Currently selected value in the group.",
          "anyOf": [
            {
              "description": "any",
              "anyOf": [
                {
                  "description": "any"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Optional label."
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "fillColor": {
          "description": "Area fill color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Area fill color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "value",
        "groupValue"
      ],
      "additionalProperties": true
    },
    "radioGroup": {
      "type": "object",
      "description": "Single-selection radio group. Shared rows per §2.6.0.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "radioGroup"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        },
        "options": {
          "description": "`{ value, label }` entries.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "items": {
          "description": "Legacy alias of `options`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "orientation": {
          "type": "string",
          "default": "vertical",
          "description": "`vertical` or `horizontal`."
        },
        "direction": {
          "type": "string",
          "description": "`horizontal` or `vertical`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "rangeSlider": {
      "type": "object",
      "description": "Range selection with two thumbs. Shared rows per §2.6.0; `value` is an object `{ start, end }`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "rangeSlider"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "labels": {
          "description": "Labels shown above the two thumbs.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "onChangeEnd": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onChangeStart": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "activeColor": {
          "description": "Colour of the selected / active portion.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the selected / active portion."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "inactiveColor": {
          "description": "Colour of the unselected / inactive portion.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the unselected / inactive portion."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "min": {
          "description": "Minimum value.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Minimum value."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "max": {
          "description": "Maximum value.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Maximum value."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "divisions": {
          "description": "Number of discrete steps.",
          "anyOf": [
            {
              "type": "number",
              "description": "Number of discrete steps."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "rating": {
      "type": "object",
      "description": "Discrete rating control (e.g., star rating). Shared rows per §2.6.0; `value` is `number`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "rating"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "allowHalf": {
          "description": "Allow half-unit ratings.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Allow half-unit ratings."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "emptyColor": {
          "description": "Colour of the unfilled portion.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the unfilled portion."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "readOnly": {
          "description": "Display only — the value cannot be changed by touch.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Display only — the value cannot be changed by touch."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "size": {
          "description": "Rendered size.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Rendered size."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "max": {
          "description": "Maximum rating value.",
          "anyOf": [
            {
              "type": "number",
              "default": 5,
              "description": "Maximum rating value."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "icon": {
          "description": "Icon for each unit. Any `IconRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Icon for each unit. Any `IconRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "color": {
          "description": "Icon color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Icon color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "resizable": {
      "type": "object",
      "description": "A box the user can resize by dragging its edges or corners.\n\nDistinct from `splitter`, which divides a fixed area between siblings.\nThis changes one widget's own size against whatever surrounds it.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "resizable"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "width": {
          "description": "Two-way bound width. Bind it to persist the size.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "height": {
          "description": "Two-way bound height.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "minWidth": {
          "description": "Lower bound on width.",
          "anyOf": [
            {
              "type": "number",
              "description": "Lower bound on width."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "maxWidth": {
          "description": "Upper bound on width.",
          "anyOf": [
            {
              "type": "number",
              "description": "Upper bound on width."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "minHeight": {
          "description": "Lower bound on height.",
          "anyOf": [
            {
              "type": "number",
              "description": "Lower bound on height."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "maxHeight": {
          "description": "Upper bound on height.",
          "anyOf": [
            {
              "type": "number",
              "description": "Upper bound on height."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "handles": {
          "description": "Draggable edges/corners: `top`, `bottom`, `start`, `end`, `topStart`, `topEnd`, `bottomStart`, `bottomEnd`. Omitted means `[bottomEnd]`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "keepAspectRatio": {
          "description": "Constrain to the starting ratio.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Constrain to the starting ratio."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        },
        "onResize": {
          "$ref": "#/$defs/Action"
        },
        "onResizeEnd": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "richText": {
      "type": "object",
      "description": "Multi-span styled text — paragraph composition for long-form content\n(articles, book chapters, captions with inline emphasis or icons).\nEach `Span` is either a styled text run or an inline widget; the\nparagraph-level `style` provides the base TextStyle that individual\nspan styles layer on top of.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "richText"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "textScaleFactor": {
          "description": "Multiplier applied to the text size.",
          "anyOf": [
            {
              "type": "number",
              "description": "Multiplier applied to the text size."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "spans": {
          "description": "Inline span list. Each entry is a `Span` (TextSpan or\nWidgetSpan). See the `Span` $def for the two shapes.\n",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Span"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "style": {
          "description": "Paragraph base style applied to every span unless overridden.\nUse this for a uniform `lineHeight` / `letterSpacing` /\n`fontFamily` / `color` across the paragraph; per-span styles\nlayer on top via `merge` semantics.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Paragraph base style applied to every span unless overridden.\nUse this for a uniform `lineHeight` / `letterSpacing` /\n`fontFamily` / `color` across the paragraph; per-span styles\nlayer on top via `merge` semantics.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "dropCap": {
          "description": "Render the first character of the first `TextSpan` as an\nenlarged drop cap that the paragraph flows around. Mutually\nexclusive with `maxLines`.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/DropCap",
              "description": "Render the first character of the first `TextSpan` as an\nenlarged drop cap that the paragraph flows around. Mutually\nexclusive with `maxLines`.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "textAlign": {
          "description": "Alignment for the composed paragraph.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "start",
                "end",
                "left",
                "right",
                "center",
                "justify"
              ],
              "default": "start",
              "description": "Alignment for the composed paragraph."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "textDirection": {
          "description": "Resolution direction for `start`/`end` alignment.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "ltr",
                "rtl"
              ],
              "description": "Resolution direction for `start`/`end` alignment."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "maxLines": {
          "description": "Maximum rendered lines. Mutually exclusive with `dropCap`.",
          "anyOf": [
            {
              "type": "number",
              "description": "Maximum rendered lines. Mutually exclusive with `dropCap`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "overflow": {
          "description": "Overflow behaviour when content exceeds `maxLines`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "clip",
                "ellipsis",
                "fade",
                "visible"
              ],
              "default": "clip",
              "description": "Overflow behaviour when content exceeds `maxLines`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "softWrap": {
          "description": "When false, the paragraph never wraps and overflow rules apply at the right edge.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "When false, the paragraph never wraps and overflow rules apply at the right edge."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        }
      },
      "required": [
        "type",
        "spans"
      ],
      "additionalProperties": true
    },
    "richTextEditor": {
      "type": "object",
      "description": "Formatted text entry. Shared rows per §2.6.0.\n\n**The bound value is HTML**, a subset stated in `format` below. That choice\nis the whole reason this widget needed a decision rather than a definition:\nan editor's value format is a contract every consumer of the document\ninherits, and a proprietary delta model would make the content unreadable\nto anything but the editor that produced it. HTML is what `richText` and\n`markdown` already sit beside, and a host that cannot edit can still\ndisplay it.\n\nA runtime MUST sanitise on the way in and on the way out — §7.5 applies to\nthis value exactly as it applies to any other author-supplied markup. The\neditor is not a licence to inject.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "richTextEditor"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "format": {
          "description": "Value format. `html` is limited to inline marks (`strong`, `em`, `u`, `s`, `code`, `a`), block elements (`p`, `h1`-`h6`, `ul`, `ol`, `li`, `blockquote`, `pre`), and `img` whose `src` is an `AssetRef`. Anything else is stripped, not escaped.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "html",
                "markdown"
              ],
              "default": "html",
              "description": "Value format. `html` is limited to inline marks (`strong`, `em`, `u`, `s`, `code`, `a`), block elements (`p`, `h1`-`h6`, `ul`, `ol`, `li`, `blockquote`, `pre`), and `img` whose `src` is an `AssetRef`. Anything else is stripped, not escaped."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "toolbar": {
          "description": "Controls to show, e.g. `[bold, italic, link, bulletList, heading]`. Omitted means the runtime's default set. A control absent here MUST also be unreachable by keyboard shortcut, or the toolbar lies about what the document can contain.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "placeholder": {
          "type": "string",
          "description": "Shown while empty."
        },
        "minHeight": {
          "description": "Minimum editor height in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "description": "Minimum editor height in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "maxLength": {
          "description": "Character ceiling, counted on the text content rather than the markup.",
          "anyOf": [
            {
              "type": "number",
              "description": "Character ceiling, counted on the text content rather than the markup."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "rive": {
      "type": "object",
      "description": "Plays a Rive animation (`.riv` asset) — the alternative to\n`lottieAnimation` when the asset was authored in Rive. State-machine\ninputs are driven via `inputs` so the same asset reacts to host\nstate changes (toggle, scrub, trigger). Distinct from Lottie:\nRive supports interactive state machines, vector deformation, and\nsmaller binary footprints; Lottie is After Effects-based and\nbetter suited to time-driven motion graphics.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "rive"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "src": {
          "description": "Rive asset path (`bundle://...riv`, `https://...riv`, `assets/...riv`).",
          "anyOf": [
            {
              "$ref": "#/$defs/AssetRef",
              "description": "Rive asset path (`bundle://...riv`, `https://...riv`, `assets/...riv`)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "artboard": {
          "type": "string",
          "description": "Named artboard to render. Defaults to the file's primary artboard."
        },
        "animation": {
          "type": "string",
          "description": "Named timeline animation. Mutually exclusive with `stateMachine`."
        },
        "stateMachine": {
          "type": "string",
          "description": "Named state machine. Mutually exclusive with `animation`."
        },
        "inputs": {
          "description": "Map of state-machine input name → value. `boolean` / `number` /\n`trigger` (set to `true` to fire) — type matches the input's\ndeclared type in the Rive file.\n",
          "anyOf": [
            {
              "type": "object",
              "description": "Map of state-machine input name → value. `boolean` / `number` /\n`trigger` (set to `true` to fire) — type matches the input's\ndeclared type in the Rive file.\n",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "fit": {
          "description": "How the artboard scales within its render bounds.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "fill",
                "contain",
                "cover",
                "fitWidth",
                "fitHeight",
                "none",
                "scaleDown"
              ],
              "default": "contain",
              "description": "How the artboard scales within its render bounds."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "alignment": {
          "description": "Alignment of the artboard within its render bounds.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Alignment of the artboard within its render bounds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Render width in logical pixels.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Render width in logical pixels."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "height": {
          "description": "Render height in logical pixels.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Render height in logical pixels."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "src"
      ],
      "additionalProperties": true
    },
    "safeArea": {
      "type": "object",
      "description": "Insets children so they avoid system UI overlaps (notch, status bar, home indicator).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "safeArea"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "maintainBottomViewPadding": {
          "description": "Keep the bottom inset while the keyboard is up.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Keep the bottom inset while the keyboard is up."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "minimum": {
          "description": "Padding applied even where the system intrusion is smaller.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Padding applied even where the system intrusion is smaller."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "bottom": {
          "description": "Distance from the parent stack's bottom edge.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Distance from the parent stack's bottom edge."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "left": {
          "description": "Distance from the parent stack's left edge.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Distance from the parent stack's left edge."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "right": {
          "description": "Distance from the parent stack's right edge.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Distance from the parent stack's right edge."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "top": {
          "description": "Distance from the parent stack's top edge.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Distance from the parent stack's top edge."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "scrollAnimated": {
      "type": "object",
      "description": "Animation driven by scroll position rather than time. Wraps a\nchild and reads from an ancestor scroll view's offset. Each\n`binding` maps a scroll-offset window to a property tween — when\nscroll passes through `[fromOffset, toOffset]` the property\ninterpolates between `fromValue` and `toValue` linearly (or via\n`curve`).\n\nCommon uses: parallax masthead (image translates 50% scroll\nspeed), reveal-on-scroll (fade in as element enters viewport),\nshrinking title (font scales as user scrolls down).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "scrollAnimated"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "bindings": {
          "description": "Per-property scroll bindings. Each entry is\n`{property, fromOffset, toOffset, fromValue, toValue, curve?}`:\n- `property` — `opacity` / `scale` / `translateX` /\n  `translateY` / `rotate`. Other properties may be wired by\n  runtime extensions.\n- `fromOffset` / `toOffset` — scroll-position window in\n  logical pixels.\n- `fromValue` / `toValue` — interpolated target values for the\n  property at the window endpoints.\n- `curve` — optional easing across the window. Defaults to\n  `linear`.\n",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "description": "object",
                "anyOf": [
                  {
                    "type": "object",
                    "description": "object",
                    "additionalProperties": true
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type",
        "bindings"
      ],
      "additionalProperties": true
    },
    "scrollBar": {
      "type": "object",
      "description": "Wraps a scrollable child with a visible scroll bar.\n\nThe runtime MUST provide a shared scroll controller to both the scroll bar and its scrollable child. When `thumbVisibility` or `trackVisibility` is `true`, the controller is required to drive the visibility; omitting it is a runtime error. Authors SHOULD NOT manage the controller manually — the runtime injects it transparently so the child scrollable (`scrollView`, `singleChildScrollView`, `list`, `grid`, `pageView`) binds to the same position.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "scrollBar"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "thumbVisibility": {
          "description": "Whether the scroll thumb is always visible.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Whether the scroll thumb is always visible."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "trackVisibility": {
          "description": "Whether the scroll track is always visible.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Whether the scroll track is always visible."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "thickness": {
          "description": "Scroll bar thickness.",
          "anyOf": [
            {
              "type": "number",
              "description": "Scroll bar thickness."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "radius": {
          "description": "Scroll bar corner radius.",
          "anyOf": [
            {
              "type": "number",
              "description": "Scroll bar corner radius."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "description": "Multiple widgets; runtime wraps them in a linear column. Mutually exclusive with `child`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "scrollView": {
      "type": "object",
      "description": "Scrollable viewport. Two layout modes — pick one per instance:\n\n- **Linear mode** — `child` (single widget) or `children` (wrapped\n  in an implicit linear column along `direction`). Simplest for\n  everyday scrolling content.\n- **Sliver mode** — `slivers` array of `Sliver` primitives. Use\n  for collapsing app bars, sticky section headers, parallax\n  mastheads, or mixing list/grid sections in one viewport.\n\nThe three input slots (`child`, `children`, `slivers`) are\nmutually exclusive.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "scrollView",
            "scrollArea"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "primary": {
          "description": "Whether this is the primary scroll view of the page.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Whether this is the primary scroll view of the page."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "direction": {
          "description": "Scroll axis.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "vertical",
                "horizontal"
              ],
              "default": "vertical",
              "description": "Scroll axis."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "padding": {
          "description": "Inner padding around the scrollable content.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Inner padding around the scrollable content."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "scrollPhysics": {
          "description": "Edge / overscroll behaviour.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "bouncing",
                "clamping",
                "neverScrollable"
              ],
              "default": "clamping",
              "description": "Edge / overscroll behaviour."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "description": "Multiple widgets wrapped in an implicit linear column along `direction`. Mutually exclusive with `child` and `slivers`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "slivers": {
          "description": "Sliver entries (sliverAppBar / sliverPersistentHeader /\nsliverList / sliverGrid / sliverFixedExtentList). Mutually\nexclusive with `child` and `children`.\n",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Sliver"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "reverse": {
          "description": "When true, run the transition in reverse direction (for back navigation).",
          "anyOf": [
            {
              "type": "boolean",
              "description": "When true, run the transition in reverse direction (for back navigation)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "scrollDirection": {
          "type": "string",
          "description": "Scroll axis."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "segmentedControl": {
      "type": "object",
      "description": "Segmented selection, styled as tabs or buttons. Shared rows per §2.6.0.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "segmentedControl"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        },
        "options": {
          "description": "`{ value, label, icon? }` entries.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "segments": {
          "description": "Legacy alias of `options`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "variant": {
          "description": "`segmented`, `tabs`, `buttons`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "segmented",
                "tabs",
                "buttons"
              ],
              "default": "segmented",
              "description": "`segmented`, `tabs`, `buttons`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "select": {
      "type": "object",
      "description": "Single-value dropdown selection. Shared rows per §2.6.0.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "select",
            "dropdown"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "disabledHint": {
          "type": "string",
          "description": "Text shown in place of the value while the control is disabled."
        },
        "isExpanded": {
          "description": "Let the control fill the available width.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Let the control fill the available width."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "itemHeight": {
          "description": "Height of one option row.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Height of one option row."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "iconSize": {
          "description": "Rendered size of the icon.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Rendered size of the icon."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "style": {
          "description": "Text style applied to the label.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Text style applied to the label."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "options": {
          "description": "`{ value, label, icon? }` entries.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "items": {
          "description": "Legacy alias of `options`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "placeholder": {
          "type": "string",
          "description": "Placeholder shown when nothing is selected."
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow depth of the open option list.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth of the open option list."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "hint": {
          "type": "string",
          "description": "Legacy alias of `placeholder`."
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "signature": {
      "type": "object",
      "description": "Signature capture pad.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "signature"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "onSignatureStart": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "borderWidth": {
          "description": "Thickness of the outline.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Thickness of the outline."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "binding": {
          "type": "string",
          "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
          "description": "Target binding for signature data (base64 PNG or SVG path). Required when capturing is needed; may be omitted for pure display."
        },
        "penColor": {
          "description": "Stroke color",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Stroke color"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "color": {
          "description": "Legacy alias of `penColor`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Stroke color"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "penWidth": {
          "description": "Stroke width",
          "anyOf": [
            {
              "type": "number",
              "default": 2.0,
              "description": "Stroke width"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "strokeWidth": {
          "description": "Legacy alias of `penWidth`.",
          "anyOf": [
            {
              "type": "number",
              "default": 2.0,
              "description": "Stroke width"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "width": {
          "description": "Widget width",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget width"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Widget height",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget height"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "backgroundColor": {
          "description": "Pad background",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Pad background"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "borderColor": {
          "description": "Pad border",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Pad border"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "showClearButton": {
          "description": "Show a clear-signature button",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Show a clear-signature button"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showGuide": {
          "description": "Show a signing guide line.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Show a signing guide line."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onSignatureEnd": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onClear": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "simpleDialog": {
      "type": "object",
      "description": "Dialog presenting a list of options.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "simpleDialog"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "contentPadding": {
          "description": "Space around the options.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space around the options."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "titlePadding": {
          "description": "Space around the title.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space around the title."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "Outline of the surface, as a shape descriptor.",
          "anyOf": [
            {
              "type": "object",
              "description": "Outline of the surface, as a shape descriptor.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "title": {
          "type": "string",
          "description": "Dialog title."
        },
        "options": {
          "description": "`{ value, label, icon? }` entries. Required when `children` is omitted.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Option"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "children": {
          "description": "Custom option widgets. Mutually exclusive with `options`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "onSelect": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onClose": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow depth of the dialog surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth of the dialog surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "singleChildScrollView": {
      "type": "object",
      "description": "Lightweight scrollable wrapper for a single child.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "singleChildScrollView"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "physics": {
          "type": "string",
          "description": "Scroll response: `bouncing`, `clamping`, `never`, `always`."
        },
        "primary": {
          "description": "Whether this is the primary scroll view of the page.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Whether this is the primary scroll view of the page."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "direction": {
          "type": "string",
          "default": "vertical",
          "description": "Scroll direction."
        },
        "padding": {
          "description": "Inner padding.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Inner padding."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "description": "Multiple widgets; runtime wraps them in a linear column along `direction`. Mutually exclusive with `child`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "reverse": {
          "description": "When true, run the transition in reverse direction (for back navigation).",
          "anyOf": [
            {
              "type": "boolean",
              "description": "When true, run the transition in reverse direction (for back navigation)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "scrollDirection": {
          "type": "string",
          "description": "Scroll axis."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "sizedBox": {
      "type": "object",
      "description": "Fixed-size box. Commonly used as a spacer with an explicit size.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "sizedBox"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "width": {
          "description": "Width in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "description": "Width in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Height in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "description": "Height in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "slider": {
      "type": "object",
      "description": "Continuous single-value selection. Shared rows per §2.6.0; `value` is `number`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "slider"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "Current numeric value (overrides shared contract to register `values` legacy alias).",
          "anyOf": [
            {
              "type": "number",
              "description": "Current numeric value (overrides shared contract to register `values` legacy alias)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "values": {
          "description": "Legacy alias of `value`.",
          "anyOf": [
            {
              "type": "number",
              "description": "Current numeric value (overrides shared contract to register `values` legacy alias)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onChangeEnd": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onChangeStart": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "thumbColor": {
          "description": "Colour of the thumb.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the thumb."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "activeColor": {
          "description": "Colour of the selected / active portion.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the selected / active portion."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "inactiveColor": {
          "description": "Colour of the unselected / inactive portion.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the unselected / inactive portion."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        },
        "min": {
          "description": "Minimum value.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Minimum value."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "max": {
          "description": "Maximum value.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Maximum value."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "divisions": {
          "description": "Number of discrete steps.",
          "anyOf": [
            {
              "type": "number",
              "description": "Number of discrete steps."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "snackBar": {
      "type": "object",
      "description": "Transient notification at the bottom of the screen.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "snackBar",
            "toast"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "behavior": {
          "type": "string",
          "description": "Placement: `fixed` (attached to the bottom) or `floating`."
        },
        "closeIconColor": {
          "description": "Colour of the dismiss control.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of the dismiss control."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "dismissDirection": {
          "type": "string",
          "description": "Swipe direction that dismisses it: `up`, `down`, `startToEnd`, `endToStart`, `horizontal`."
        },
        "showCloseIcon": {
          "description": "Show the dismiss control.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Show the dismiss control."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "margin": {
          "description": "Space outside the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space outside the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "padding": {
          "description": "Space inside the surface, around its content.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space inside the surface, around its content."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "shape": {
          "description": "Outline of the surface, as a shape descriptor.",
          "anyOf": [
            {
              "type": "object",
              "description": "Outline of the surface, as a shape descriptor.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "content": {
          "type": "string",
          "description": "Message text."
        },
        "duration": {
          "description": "Display duration in milliseconds.",
          "anyOf": [
            {
              "type": "number",
              "default": 4000,
              "description": "Display duration in milliseconds."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "action": {
          "description": "`{ label, onTap }` optional action.",
          "anyOf": [
            {
              "$ref": "#/$defs/SnackBarAction",
              "description": "`{ label, onTap }` optional action."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onClose": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "elevation": {
          "description": "Shadow depth of the bar.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Shadow depth of the bar."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "textColor": {
          "description": "Default text color",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Default text color"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Measured from real documents (2026-08-04): number, `{value, unit}`, a dimension string or a binding."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "content"
      ],
      "additionalProperties": true
    },
    "spacer": {
      "type": "object",
      "description": "Flexible empty space inside a `linear` parent.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "spacer"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "flex": {
          "description": "Proportion of available space to occupy.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Proportion of available space to occupy."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "splitter": {
      "type": "object",
      "description": "Panes separated by draggable gutters.\n\nDistinct from `resizable`, which resizes one box against the layout around\nit. This divides a fixed area between siblings: dragging a gutter takes\nspace from one pane and gives it to the next, so the total never changes.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "splitter"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "children": {
          "description": "Panes, in order. Gutters appear between adjacent panes.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "orientation": {
          "description": "Direction panes are laid along.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "horizontal",
                "vertical"
              ],
              "default": "horizontal",
              "description": "Direction panes are laid along."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "sizes": {
          "description": "Pane sizes as fractions summing to 1. Two-way bindable so a layout can be restored. Omitted distributes evenly.",
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "number"
                      },
                      {
                        "type": "string",
                        "pattern": "^\\{\\{.*\\}\\}$",
                        "description": "Binding expression."
                      },
                      {
                        "type": "object",
                        "properties": {
                          "value": {
                            "type": "number"
                          },
                          "unit": {
                            "type": "string"
                          }
                        },
                        "required": [
                          "value"
                        ],
                        "description": "Dimension object: `{value, unit}`."
                      }
                    ]
                  }
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression resolving to an array."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "minSizes": {
          "description": "Per-pane minimum fractions. A drag stops at them rather than collapsing a pane the author did not mark collapsible.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "type": "number"
                  },
                  {
                    "type": "string",
                    "pattern": "^\\{\\{.*\\}\\}$",
                    "description": "Binding expression."
                  },
                  {
                    "type": "object",
                    "properties": {
                      "value": {
                        "type": "number"
                      },
                      "unit": {
                        "type": "string"
                      }
                    },
                    "required": [
                      "value"
                    ],
                    "description": "Dimension object: `{value, unit}`."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "gutterSize": {
          "description": "Gutter thickness in logical pixels. Keep it large enough to hit with a finger where touch is possible.",
          "anyOf": [
            {
              "type": "number",
              "default": 8,
              "description": "Gutter thickness in logical pixels. Keep it large enough to hit with a finger where touch is possible."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "collapsible": {
          "description": "Per-pane: whether dragging past its minimum collapses it entirely.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "type": "boolean"
                  },
                  {
                    "type": "string",
                    "pattern": "^\\{\\{.*\\}\\}$",
                    "description": "Binding expression."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "onDragEnd": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "children"
      ],
      "additionalProperties": true
    },
    "spreadsheet": {
      "type": "object",
      "description": "Editable grid of cells addressed by row and column, with optional formulas.\n\nDistinct from `dataTable`, which presents records: a table's unit is a row\nwith typed fields, a spreadsheet's unit is a cell with a coordinate. That\ndifference is why one cannot be a mode of the other — selection, editing,\nand paste all address different things.\n\n**Formulas are evaluated by the runtime's expression engine under §7.1**,\nnot by a spreadsheet language of the DSL's own. A runtime that cannot meet\nthat sandbox MUST render formulas as their last computed value rather than\nevaluating them loosely: a cell that runs arbitrary text is the one place\nthis widget could become an injection surface.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "spreadsheet"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "data": {
          "description": "Rows of cell values. A cell is a scalar, or `{ value, formula?, format?, style? }` when it carries more than a value.",
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "anyOf": [
                      {
                        "type": "array"
                      },
                      {
                        "$ref": "#/$defs/Binding",
                        "description": "Binding expression resolving to this value."
                      }
                    ]
                  }
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression resolving to an array."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "columns": {
          "description": "`{ key?, label?, width?, type?, readOnly? }` per column. Omitted derives columns from the widest row and labels them A, B, C…",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "description": "object",
                "anyOf": [
                  {
                    "type": "object",
                    "description": "object",
                    "additionalProperties": true
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "rowHeaders": {
          "description": "Show the row-number gutter.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Show the row-number gutter."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "columnHeaders": {
          "description": "Show the column-letter header.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Show the column-letter header."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "editable": {
          "description": "Allow cell editing.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Allow cell editing."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "formulas": {
          "description": "Evaluate `formula` fields. Off by default — a document that does not need computation should not carry an evaluator.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Evaluate `formula` fields. Off by default — a document that does not need computation should not carry an evaluator."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "frozenRows": {
          "description": "Rows pinned at the top while scrolling.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Rows pinned at the top while scrolling."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "frozenColumns": {
          "description": "Columns pinned at the start while scrolling.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Columns pinned at the start while scrolling."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "onChange": {
          "$ref": "#/$defs/Action"
        },
        "onCellSelect": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type",
        "data"
      ],
      "additionalProperties": true
    },
    "stack": {
      "type": "object",
      "description": "Overlapping children. Non-positioned children align per `alignment`; positioned children use explicit offsets (see `positioned`).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "stack"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "alignment": {
          "description": "Alignment of non-positioned children.",
          "anyOf": [
            {
              "$ref": "#/$defs/Alignment",
              "description": "Alignment of non-positioned children."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "fit": {
          "description": "Sizing of non-positioned children: `loose`, `expand`, `passthrough`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "loose",
                "expand",
                "passthrough"
              ],
              "default": "loose",
              "description": "Sizing of non-positioned children: `loose`, `expand`, `passthrough`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "children": {
          "description": "Stacked children (rendered in order; later children render on top).",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "textDirection": {
          "type": "string",
          "description": "Resolution direction for `start`/`end` alignment."
        }
      },
      "required": [
        "type",
        "children"
      ],
      "additionalProperties": true
    },
    "staggeredGrid": {
      "type": "object",
      "description": "Pinterest-style masonry layout — items keep their intrinsic height\nand are packed by column without uniform row alignment. Use for\nalbum / book / portfolio shelves where covers have different\naspect ratios. Distinct from `grid` (uniform row alignment) and\n`carousel` (single-axis scrolling).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "staggeredGrid"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "items": {
          "description": "Array source. Required when `children` is omitted.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "itemTemplate": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "description": "Static cells. Mutually exclusive with `items` + `itemTemplate`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "columns": {
          "description": "Column count. The object form is a responsive override keyed by form factor (§14.1.1) — `{compact, medium, expanded, large, extraLarge, default}`.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "mainAxisSpacing": {
          "description": "Gap along the scroll axis (between consecutive items in a column).",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Gap along the scroll axis (between consecutive items in a column)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "crossAxisSpacing": {
          "description": "Gap across the scroll axis (between columns).",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Gap across the scroll axis (between columns)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "padding": {
          "description": "Inner padding around the grid.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Inner padding around the grid."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "scrollDirection": {
          "description": "Scroll axis.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "vertical",
                "horizontal"
              ],
              "default": "vertical",
              "description": "Scroll axis."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        }
      },
      "required": [
        "type",
        "columns"
      ],
      "additionalProperties": true
    },
    "stepper": {
      "type": "object",
      "description": "Step-by-step wizard. Shared `binding` / `value` / `enabled` / `onChange` per §2.6.0 — the bound value is the **active step index** (integer). `binding` and `currentStep` are aliases: when `binding` is set, it takes effect; otherwise `currentStep` is read as a one-way property.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "stepper",
            "steps"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "margin": {
          "description": "Space outside the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space outside the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "physics": {
          "type": "string",
          "description": "Scroll response: `bouncing`, `clamping`, `never`, `always`."
        },
        "steps": {
          "description": "Each step: `{ title, subtitle?, state?, content, isActive? }`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Step"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "currentStep": {
          "description": "One-way legacy property. Use §2.6.0 `binding` for two-way behavior.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "stepperType": {
          "type": "string",
          "default": "vertical",
          "description": "`vertical` or `horizontal`."
        },
        "onStepTapped": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onStepContinue": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onStepCancel": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "steps"
      ],
      "additionalProperties": true
    },
    "tabBar": {
      "type": "object",
      "description": "Horizontal tab selector. Each tab's text field is `label`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "tabBar"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "indicator": {
          "description": "Decoration drawn as the selection indicator, replacing the default underline.",
          "anyOf": [
            {
              "type": "object",
              "description": "Decoration drawn as the selection indicator, replacing the default underline.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "indicatorPadding": {
          "description": "Space between the indicator and the tab bounds.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space between the indicator and the tab bounds."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "indicatorSize": {
          "type": "string",
          "description": "What the indicator spans: `tab` (the whole tab) or `label` (the text only)."
        },
        "indicatorWeight": {
          "description": "Thickness of the default underline indicator.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Thickness of the default underline indicator."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "isScrollable": {
          "description": "Let the tabs scroll horizontally instead of sharing the width.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Let the tabs scroll horizontally instead of sharing the width."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "labelPadding": {
          "description": "Space around each tab label.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space around each tab label."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "mouseCursor": {
          "type": "string",
          "description": "Pointer cursor shown over a tab."
        },
        "unselectedLabelColor": {
          "description": "Colour of labels that are not selected.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour of labels that are not selected."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "unselectedLabelStyle": {
          "description": "Text style for labels that are not selected.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Text style for labels that are not selected."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "enableFeedback": {
          "description": "Play the platform tap sound / haptic on activation.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Play the platform tap sound / haptic on activation."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "labelColor": {
          "description": "Colour applied to the label text.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Colour applied to the label text."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "overlayColor": {
          "description": "Overlay used for the focus / hover / press states together.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Overlay used for the focus / hover / press states together."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "padding": {
          "description": "Space inside the surface, around its content.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space inside the surface, around its content."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "physics": {
          "type": "string",
          "description": "Scroll response: `bouncing`, `clamping`, `never`, `always`."
        },
        "selectedIndex": {
          "description": "Currently selected index. Defaults to 0 when omitted.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "tabs": {
          "description": "`{ label, icon? }` entries.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/Tab"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "indicatorColor": {
          "description": "Selection indicator pill color (rail / drawer / tabs).",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Selection indicator pill color (rail / drawer / tabs)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "labelStyle": {
          "description": "Default text style for item labels (selected and unselected). Per-item override via NavItem.style.labelStyle.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Default text style for item labels (selected and unselected). Per-item override via NavItem.style.labelStyle."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "tabs"
      ],
      "additionalProperties": true
    },
    "tabBarView": {
      "type": "object",
      "description": "Content area that displays widgets corresponding to the currently selected tab.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "tabBarView"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "dragStartBehavior": {
          "type": "string",
          "description": "When a drag is recognised: `start` or `down`."
        },
        "physics": {
          "type": "string",
          "description": "Scroll response: `bouncing`, `clamping`, `never`, `always`."
        },
        "selectedIndex": {
          "description": "Displayed index (usually bound to the same state as a `tabBar`).",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "children": {
          "description": "One child per tab.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        }
      },
      "required": [
        "type",
        "children"
      ],
      "additionalProperties": true
    },
    "table": {
      "type": "object",
      "description": "Layout table for arranging widgets in rows and columns. Not data-bound; each cell is a widget.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "table"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "textBaseline": {
          "type": "string",
          "description": "Baseline used to align cells in a baseline-aligned row."
        },
        "rows": {
          "description": "Row definitions — each carries the widgets for its cells.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/TableRow"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "border": {
          "description": "Optional cell border",
          "anyOf": [
            {
              "description": "Optional cell border"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "defaultColumnWidth": {
          "description": "`flex`, `intrinsic`, or a fixed number (logical px).",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            }
          ]
        },
        "defaultVerticalAlignment": {
          "description": "Cell vertical alignment.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "top",
                "middle",
                "bottom",
                "baseline"
              ],
              "default": "middle",
              "description": "Cell vertical alignment."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "columnWidths": {
          "description": "Map `columnIndex` → width override",
          "anyOf": [
            {
              "type": "object",
              "description": "Map `columnIndex` → width override",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "textDirection": {
          "type": "string",
          "description": "Resolution direction for `start`/`end` alignment."
        }
      },
      "required": [
        "type",
        "rows"
      ],
      "additionalProperties": true
    },
    "terminal": {
      "type": "object",
      "description": "ANSI-capable terminal emulator.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "terminal"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "lines": {
          "description": "Output lines (ANSI-escaped strings accepted). Declared `binding`\nbefore, which rejected a literal list of lines.\n",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "prompt": {
          "type": "string",
          "default": "$ ",
          "description": "Input prompt prefix, including its trailing space."
        },
        "showInput": {
          "description": "Render an input line",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Render an input line"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "maxLines": {
          "description": "Scrollback retention cap",
          "anyOf": [
            {
              "type": "number",
              "description": "Scrollback retention cap"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "width": {
          "description": "Widget width",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget width"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Widget height",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget height"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "fontSize": {
          "description": "Font size (logical px)",
          "anyOf": [
            {
              "type": "number",
              "description": "Font size (logical px)"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "backgroundColor": {
          "description": "Terminal background",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Terminal background"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "textColor": {
          "description": "Default text color",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Default text color"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "promptColor": {
          "description": "Prompt color",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Prompt color"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onCommand": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "theme": {
          "type": "string",
          "description": "Editor color theme."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "text": {
      "type": "object",
      "description": "Renders a string. The canonical content field is `text`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "text",
            "label"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "textTransform": {
          "type": "string",
          "description": "Case transform applied before rendering: `uppercase`, `lowercase`, `capitalize`, `none`."
        },
        "ariaLabel": {
          "type": "string",
          "description": "Label announced by a screen reader in place of the content."
        },
        "semanticsLabel": {
          "type": "string",
          "description": "Label announced by a screen reader in place of the content."
        },
        "softWrap": {
          "description": "Whether the text breaks at soft line boundaries.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Whether the text breaks at soft line boundaries."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "textScaleFactor": {
          "description": "Multiplier applied to the text size.",
          "anyOf": [
            {
              "type": "number",
              "description": "Multiplier applied to the text size."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "text": {
          "type": "string",
          "description": "Text content. Supports binding expressions."
        },
        "content": {
          "type": "string",
          "description": "Legacy alias of `text`."
        },
        "variant": {
          "description": "M3 typography role. The runtime resolves it through\n`theme.typography.<variant>` (see `05_Theme.md` § 5.4) and uses the\nresult as the base TextStyle. Inline `style` (if present) overrides\nindividual fields on top of the resolved role.\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "displayLarge",
                "displayMedium",
                "displaySmall",
                "headlineLarge",
                "headlineMedium",
                "headlineSmall",
                "titleLarge",
                "titleMedium",
                "titleSmall",
                "bodyLarge",
                "bodyMedium",
                "bodySmall",
                "labelLarge",
                "labelMedium",
                "labelSmall"
              ],
              "description": "M3 typography role. The runtime resolves it through\n`theme.typography.<variant>` (see `05_Theme.md` § 5.4) and uses the\nresult as the base TextStyle. Inline `style` (if present) overrides\nindividual fields on top of the resolved role.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "style": {
          "description": "Inline `TextStyle` overrides (color, fontWeight, fontSize,\ndecoration, shader, shadows, fontFeatures, …). Layered on top of\n`variant` when both are set. A binding expression\n`\"{{theme.typography.<role>}}\"` is also accepted and resolves to\nthe same role's typography object — equivalent to writing\n`variant: \"<role>\"`.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Inline `TextStyle` overrides (color, fontWeight, fontSize,\ndecoration, shader, shadows, fontFeatures, …). Layered on top of\n`variant` when both are set. A binding expression\n`\"{{theme.typography.<role>}}\"` is also accepted and resolves to\nthe same role's typography object — equivalent to writing\n`variant: \"<role>\"`.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "dropCap": {
          "description": "Render the first character of `text` as an enlarged drop cap that\nthe surrounding lines flow around (book / magazine ornament).\nMutually exclusive with `maxLines`.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/DropCap",
              "description": "Render the first character of `text` as an enlarged drop cap that\nthe surrounding lines flow around (book / magazine ornament).\nMutually exclusive with `maxLines`.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "maxLines": {
          "description": "Maximum rendered lines.",
          "anyOf": [
            {
              "type": "number",
              "description": "Maximum rendered lines."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "overflow": {
          "description": "`clip`, `ellipsis`, `fade`, `visible`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "clip",
                "ellipsis",
                "fade",
                "visible"
              ],
              "default": "clip",
              "description": "`clip`, `ellipsis`, `fade`, `visible`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "textAlign": {
          "description": "`start`, `center`, `end`, `justify` follow the text\ndirection (`start` is right-hand in an RTL locale). `left` and `right`\nare absolute and do not — they are the way to pin a column of numbers\nto one side whatever the locale, so they are values, not aliases.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "start",
                "center",
                "end",
                "justify",
                "left",
                "right"
              ],
              "default": "start",
              "description": "`start`, `center`, `end`, `justify` follow the text\ndirection (`start` is right-hand in an RTL locale). `left` and `right`\nare absolute and do not — they are the way to pin a column of numbers\nto one side whatever the locale, so they are values, not aliases."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "textDirection": {
          "type": "string",
          "description": "Resolution direction for `start`/`end` alignment."
        },
        "value": {
          "description": "Measured from real documents (2026-08-04): any literal or binding — the §2.6.0 input contract shape.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "textInput": {
      "type": "object",
      "description": "Single-line (or multi-line when `maxLines > 1`) text entry. Follows the\nshared input contract (spec §2.6.0): when `binding` is set, the runtime\nreads the current value from the state path and writes user input back\nwithout requiring an explicit `onChange`. Explicit `value` + `onChange`\noverride the shorthand.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "textInput",
            "textField",
            "textfield",
            "textFormField",
            "text-form-field"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the input value."
        },
        "value": {
          "description": "One-way initial/display value when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "enabled": {
          "description": "Whether the field accepts user input.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the field accepts user input."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "$ref": "#/$defs/Action"
        },
        "debounce": {
          "description": "Milliseconds to wait after typing stops before `onChange` fires.",
          "anyOf": [
            {
              "type": "number",
              "description": "Milliseconds to wait after typing stops before `onChange` fires."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "textInputAction": {
          "type": "string",
          "description": "Keyboard action key: `done`, `next`, `search`, `send`, `go`, `newline`."
        },
        "errorText": {
          "type": "string",
          "description": "Error message shown under the field. Overrides validation output."
        },
        "style": {
          "description": "Text style applied to the label.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Text style applied to the label."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label shown above or floating inside the field."
        },
        "placeholder": {
          "type": "string",
          "description": "Placeholder shown when the field is empty."
        },
        "hint": {
          "type": "string",
          "description": "Legacy alias of `placeholder`."
        },
        "helperText": {
          "type": "string",
          "description": "Helper text shown below the field."
        },
        "prefixIcon": {
          "type": "string",
          "description": "Icon displayed at the start of the field."
        },
        "suffixIcon": {
          "type": "string",
          "description": "Icon displayed at the end of the field."
        },
        "obscureText": {
          "description": "Hide input characters (passwords).",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Hide input characters (passwords)."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "readOnly": {
          "description": "Whether the field is read-only.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Whether the field is read-only."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "maxLines": {
          "description": "Maximum number of visible lines.",
          "anyOf": [
            {
              "type": "integer",
              "default": 1,
              "description": "Maximum number of visible lines."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "maxLength": {
          "description": "Maximum character length enforced at input time.",
          "anyOf": [
            {
              "type": "integer",
              "description": "Maximum character length enforced at input time."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "inputType": {
          "description": "Virtual keyboard hint / input semantics.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "text",
                "number",
                "email",
                "phone",
                "url",
                "search",
                "multiline"
              ],
              "default": "text",
              "description": "Virtual keyboard hint / input semantics."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "showToggle": {
          "description": "Offer a reveal control alongside an obscured field. Ignored unless `obscureText` is true.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Offer a reveal control alongside an obscured field. Ignored unless `obscureText` is true."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "defaultCountry": {
          "type": "string",
          "description": "ISO 3166-1 alpha-2 country seeding the dialling code. Ignored unless `inputType` is `phone`."
        },
        "validation": {
          "description": "Input constraints; see `../../07_Security.md` §7.2.1.",
          "anyOf": [
            {
              "description": "Input constraints; see `../../07_Security.md` §7.2.1.",
              "oneOf": [
                {
                  "type": "object"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "object"
                  }
                }
              ]
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "error": {
          "description": "Error state: a message to show, or `true` to mark the field with `errorText`.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "onChanged": {
          "$ref": "#/$defs/Action",
          "description": "Legacy alias of `onChange`."
        },
        "change": {
          "$ref": "#/$defs/Action",
          "description": "Legacy alias of `onChange`."
        },
        "onSubmit": {
          "$ref": "#/$defs/Action"
        },
        "submit": {
          "$ref": "#/$defs/Action",
          "description": "Legacy alias of `onSubmit`."
        },
        "onFocus": {
          "$ref": "#/$defs/Action"
        },
        "onBlur": {
          "$ref": "#/$defs/Action"
        },
        "blur": {
          "$ref": "#/$defs/Action",
          "description": "Legacy alias of `onBlur`."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "timeField": {
      "type": "object",
      "description": "Time input. Shared rows per §2.6.0; `value` is a time string (e.g., `\"14:30\"`).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "timeField"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "errorText": {
          "type": "string",
          "description": "Error message shown under the field. Overrides validation output."
        },
        "label": {
          "type": "string",
          "description": "Field label."
        },
        "format": {
          "type": "string",
          "default": "HH:mm",
          "description": "Display format."
        },
        "use24HourFormat": {
          "description": "24-hour clock. The default follows what this field has drawn since it\nshipped; `timePicker` defaults the other way, and unifying them would\nchange every screen that never set the property.\n",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "24-hour clock. The default follows what this field has drawn since it\nshipped; `timePicker` defaults the other way, and unifying them would\nchange every screen that never set the property.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "mode": {
          "description": "`spinner`, `input`, `dial`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "spinner",
                "input",
                "dial"
              ],
              "default": "spinner",
              "description": "`spinner`, `input`, `dial`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "timePicker": {
      "type": "object",
      "description": "Standalone time picker surface. Shared rows per §2.6.0; `value` is a time string.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "timePicker"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "initialTime": {
          "type": "string",
          "description": "Time the picker opens on when the bound value is empty (`HH:mm`)."
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        },
        "use24HourFormat": {
          "description": "24-hour clock. The default follows what this picker has drawn since it\nshipped; `timeField` defaults the other way — see its note.\n",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "24-hour clock. The default follows what this picker has drawn since it\nshipped; `timeField` defaults the other way — see its note.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "icon": {
          "description": "Icon shown. Any `IconRef` form.",
          "anyOf": [
            {
              "$ref": "#/$defs/IconRef",
              "description": "Icon shown. Any `IconRef` form."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "timeFormat": {
          "type": "string",
          "description": "Display pattern for the time portion. Display only."
        },
        "variant": {
          "description": "`elevated`, `filled`, `outlined`, `text`, `icon`.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "elevated",
                "filled",
                "outlined",
                "text",
                "icon"
              ],
              "description": "`elevated`, `filled`, `outlined`, `text`, `icon`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "timeline": {
      "type": "object",
      "description": "Chronological timeline of events.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "timeline"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "nodeSize": {
          "description": "Diameter of the node marker on the line.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Diameter of the node marker on the line."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "lineWidth": {
          "description": "Thickness of the connecting line.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Thickness of the connecting line."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "spacing": {
          "description": "Gap between adjacent items.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Gap between adjacent items."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "items": {
          "description": "Timeline entries",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "anyOf": [
                  {
                    "$ref": "#/$defs/TimelineItem"
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "orientation": {
          "description": "Layout axis.",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "vertical",
                "horizontal"
              ],
              "default": "vertical",
              "description": "Layout axis."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        },
        "itemTemplate": {
          "$ref": "#/$defs/Widget"
        },
        "lineColor": {
          "description": "Line / bar color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Line / bar color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "items"
      ],
      "additionalProperties": true
    },
    "toggle": {
      "type": "object",
      "description": "Binary on/off control. Shared `binding` / `value` / `enabled` / `onChange` per §2.6.0; `value` is `boolean`.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "toggle",
            "switch"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "change": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "label": {
          "type": "string",
          "description": "Field label shown beside or above the control."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "tooltip": {
      "type": "object",
      "description": "Wrapper that reveals a tooltip on hover or long-press.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "tooltip"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "preferBelow": {
          "description": "Prefer to place the tooltip under the anchor.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Prefer to place the tooltip under the anchor."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "richMessage": {
          "description": "Message as styled spans, when plain text is not enough.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "description": "object",
                "anyOf": [
                  {
                    "type": "object",
                    "description": "object",
                    "additionalProperties": true
                  },
                  {
                    "$ref": "#/$defs/Binding",
                    "description": "Binding expression resolving to this value."
                  }
                ]
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "showDuration": {
          "description": "How long the tooltip stays up, in milliseconds.",
          "anyOf": [
            {
              "type": "number",
              "description": "How long the tooltip stays up, in milliseconds."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "textStyle": {
          "description": "Text style of the tooltip message.",
          "anyOf": [
            {
              "$ref": "#/$defs/TextStyle",
              "description": "Text style of the tooltip message."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "triggerMode": {
          "type": "string",
          "description": "What raises it: `tap`, `longPress`, `manual`."
        },
        "verticalOffset": {
          "description": "Distance between the anchor and the tooltip.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Distance between the anchor and the tooltip."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "enableFeedback": {
          "description": "Play the platform tap sound / haptic on activation.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Play the platform tap sound / haptic on activation."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "excludeFromSemantics": {
          "description": "Keep the surface out of the accessibility tree.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Keep the surface out of the accessibility tree."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "height": {
          "description": "Fixed height.",
          "anyOf": [
            {
              "$ref": "#/$defs/Dimension",
              "description": "Fixed height."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "margin": {
          "description": "Space outside the surface.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space outside the surface."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "padding": {
          "description": "Space inside the surface, around its content.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Space inside the surface, around its content."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "message": {
          "type": "string",
          "description": "Tooltip text."
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "decoration": {
          "description": "Animated decoration (color/gradient/border/shadow/image).",
          "anyOf": [
            {
              "$ref": "#/$defs/BoxDecoration",
              "description": "Animated decoration (color/gradient/border/shadow/image)."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "textAlign": {
          "type": "string",
          "description": "Alignment for the composed paragraph."
        },
        "waitDuration": {
          "description": "Measured from real documents (2026-08-04): milliseconds before the tooltip appears.",
          "anyOf": [
            {
              "type": "number",
              "description": "Measured from real documents (2026-08-04): milliseconds before the tooltip appears."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type",
        "message"
      ],
      "additionalProperties": true
    },
    "transform": {
      "type": "object",
      "description": "Geometric transformation (rotate, scale, translate) applied to a child.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "transform"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "rotate": {
          "description": "Rotation in radians.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Rotation in radians."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "scale": {
          "description": "Uniform scale, or `{ x, y }` non-uniform scale.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "translate": {
          "description": "`{ x, y }` offset in logical pixels.",
          "anyOf": [
            {
              "type": "object",
              "description": "`{ x, y }` offset in logical pixels.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "origin": {
          "description": "Transform origin as fraction.",
          "anyOf": [
            {
              "type": "object",
              "description": "Transform origin as fraction.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "animated": {
          "description": "Animate transform changes.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Animate transform changes."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "duration": {
          "description": "Animation duration in milliseconds.",
          "anyOf": [
            {
              "type": "number",
              "default": 300,
              "description": "Animation duration in milliseconds."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "curve": {
          "type": "string",
          "default": "easeInOut",
          "description": "Animation curve."
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Widget"
          },
          "description": "Multiple widgets; runtime wraps them into an implicit column. Alternative to `child` — mutually exclusive."
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "tree": {
      "type": "object",
      "description": "Hierarchical tree view with expandable nodes.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "tree",
            "treeView"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "checkable": {
          "description": "Render a checkbox per node. Checked keys bind through `checkedKeys`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Render a checkbox per node. Checked keys bind through `checkedKeys`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "checkedKeys": {
          "description": "Two-way bound checked node keys.",
          "anyOf": [
            {
              "oneOf": [
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression resolving to an array."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "draggable": {
          "description": "Allow reordering/reparenting by drag. Reports through `onDrop`; the widget does not mutate the tree itself.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Allow reordering/reparenting by drag. Reports through `onDrop`; the widget does not mutate the tree itself."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "data": {
          "description": "Hierarchical data; each node MAY carry a `children` array. Literal array or binding.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "childrenKey": {
          "type": "string",
          "default": "children",
          "description": "Field name of the children array"
        },
        "indentation": {
          "description": "Per-depth horizontal indentation in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "default": 24,
              "description": "Per-depth horizontal indentation in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "itemPadding": {
          "description": "Padding applied inside every tree row (expandable and leaf). Defaults to `{vertical: 4, right: 8}`. Adjust the vertical component to change row height / density.",
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeInsets",
              "description": "Padding applied inside every tree row (expandable and leaf). Defaults to `{vertical: 4, right: 8}`. Adjust the vertical component to change row height / density."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "itemTemplate": {
          "$ref": "#/$defs/Widget"
        },
        "expandable": {
          "description": "Allow expand/collapse",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Allow expand/collapse"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "initiallyExpanded": {
          "description": "Expand all nodes on mount",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Expand all nodes on mount"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "selectable": {
          "description": "Whether a node can be selected by tap.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Whether a node can be selected by tap."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "showLines": {
          "description": "Draw connector lines between parent and leaf nodes.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Draw connector lines between parent and leaf nodes."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "selectedColor": {
          "description": "Background color of the currently selected row.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Background color of the currently selected row."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "lineColor": {
          "description": "Color of connector lines when `showLines` is true.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Color of connector lines when `showLines` is true."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "width": {
          "description": "Fixed widget width in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "description": "Fixed widget width in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Fixed widget height in logical pixels.",
          "anyOf": [
            {
              "type": "number",
              "description": "Fixed widget height in logical pixels."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "onDrop": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onNodeTap": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onSelect": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onExpand": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onCollapse": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "data"
      ],
      "additionalProperties": true
    },
    "use": {
      "type": "object",
      "description": "Instantiates a named template (see §9.6).",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "use"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "template": {
          "type": "string",
          "description": "Template name to instantiate."
        },
        "params": {
          "description": "Parameter values; merged with the template's declared defaults.",
          "anyOf": [
            {
              "type": "object",
              "description": "Parameter values; merged with the template's declared defaults.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "slots": {
          "description": "Widget subtrees keyed by slot name.",
          "anyOf": [
            {
              "type": "object",
              "description": "Widget subtrees keyed by slot name.",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type",
        "template"
      ],
      "additionalProperties": true
    },
    "verticalDivider": {
      "type": "object",
      "description": "Vertical separator, typically used in horizontal layouts.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "verticalDivider"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "width": {
          "description": "Total width including spacing.",
          "anyOf": [
            {
              "type": "number",
              "description": "Total width including spacing."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "thickness": {
          "description": "Line thickness.",
          "anyOf": [
            {
              "type": "number",
              "default": 1,
              "description": "Line thickness."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "color": {
          "description": "Line color.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Line color."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "indent": {
          "description": "Top indent.",
          "anyOf": [
            {
              "type": "number",
              "description": "Top indent."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "endIndent": {
          "description": "Bottom indent.",
          "anyOf": [
            {
              "type": "number",
              "description": "Bottom indent."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "view": {
      "type": "object",
      "description": "Embeds a definition sourced from anywhere — including another MCP origin — at\nthis point in the tree. The consumer side of composition: `11_Bundle_Metadata.md`\n§11.9 `dashboard` declares how an app presents itself *when embedded*; `view`\nis how an app *embeds*. See [`01_Core_Concepts.md`](01_Core_Concepts.md) §1.9\nfor `DefinitionSource` and the origin-scope rule.\n\nWhen `source` names an origin, that origin becomes the ambient origin for the\nembedded subtree: its `tool` calls, resource reads, subscriptions, state,\nstorage, and permissions all scope to it (§1.9.5, §7.10, §8.8). This is what\nlets a definition authored with no knowledge of any embedder render unmodified\nand still reach its own server.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "view"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "source": {
          "description": "What to render — an inline definition, a `ui://` URI on the current origin,\na qualified `{ $ref, from }` reference to another origin, or a binding to a\ndefinition held in state. See §1.9.1.\n",
          "anyOf": [
            {
              "$ref": "#/$defs/DefinitionSource",
              "description": "What to render — an inline definition, a `ui://` URI on the current origin,\na qualified `{ $ref, from }` reference to another origin, or a binding to a\ndefinition held in state. See §1.9.1.\n"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "props": {
          "description": "Values merged into the embedded definition's initial state. The ONLY channel\nfrom embedder to embedded — an embedded view never implicitly reads the\nembedder's state (§7.10). One-way: mutations inside the embedded scope do not\npropagate back.\n",
          "anyOf": [
            {
              "type": "object",
              "description": "Values merged into the embedded definition's initial state. The ONLY channel\nfrom embedder to embedded — an embedded view never implicitly reads the\nembedder's state (§7.10). One-way: mutations inside the embedded scope do not\npropagate back.\n",
              "additionalProperties": true
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "fallback": {
          "$ref": "#/$defs/Widget"
        },
        "loading": {
          "$ref": "#/$defs/Widget"
        },
        "onError": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "theme": {
          "description": "`inherit` (default) renders the embedded definition under the embedder's\ntheme so a composed screen looks like one product. `own` honours the\nembedded definition's own theme — appropriate when embedding a\nfull third-party application rather than a summary tile.\n",
          "anyOf": [
            {
              "type": "string",
              "enum": [
                "inherit",
                "own"
              ],
              "default": "inherit",
              "description": "`inherit` (default) renders the embedded definition under the embedder's\ntheme so a composed screen looks like one product. `own` honours the\nembedded definition's own theme — appropriate when embedding a\nfull third-party application rather than a summary tile.\n"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to one of the declared values."
            }
          ]
        }
      },
      "required": [
        "type",
        "source"
      ],
      "additionalProperties": true
    },
    "visibility": {
      "type": "object",
      "description": "Shows or hides a child with optional state preservation and replacement content.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "visibility"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "maintainAnimation": {
          "description": "Keep animations running while hidden.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Keep animations running while hidden."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "maintainInteractivity": {
          "description": "Keep the subtree interactive while hidden.",
          "anyOf": [
            {
              "type": "boolean",
              "description": "Keep the subtree interactive while hidden."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "visible": {
          "description": "Whether the child is visible. Defaults to `true` when omitted.",
          "anyOf": [
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$|^[a-zA-Z_][a-zA-Z0-9_.\\[\\]]*$",
              "description": "State path (`\"form.email\"`) or expression (`\"{{form.email}}\"`)."
            }
          ]
        },
        "maintainSize": {
          "description": "Keep space allocated when hidden.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Keep space allocated when hidden."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "maintainState": {
          "description": "Preserve `child` state when hidden. Does not affect `replacement`, which is built fresh each time it is shown.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Preserve `child` state when hidden. Does not affect `replacement`, which is built fresh each time it is shown."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "replacement": {
          "$ref": "#/$defs/Widget"
        },
        "child": {
          "$ref": "#/$defs/Widget"
        },
        "children": {
          "description": "Multiple widgets; runtime wraps them in a linear column. Mutually exclusive with `child`.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "voiceInput": {
      "type": "object",
      "description": "Speech captured from the microphone, transcribed into `binding`.\n\n**Client Profile, not Core** — and the line is worth stating because\n`fileInput` sits on the other side of it. Picking a file is one act of\nchoosing, and the choosing is the consent. A microphone is a continuous\ncapture of the room, including whoever else is in it, so it belongs behind\nthe permission system (§8.4) with a grant the user can see and revoke.\n\nThere is no composition that reaches it: microphone access is a host\ncapability, so without this widget a document cannot express the intent at\nall.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "voiceInput"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "binding": {
          "type": "string",
          "description": "State path receiving the transcript."
        },
        "value": {
          "description": "One-way initial/display value (§2.6.0). Literal or `{{path}}`; used when `binding` is not set.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "anyOf": [
                {
                  "type": "number"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                },
                {
                  "type": "object",
                  "properties": {
                    "value": {
                      "type": "number"
                    },
                    "unit": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "value"
                  ],
                  "description": "Dimension object: `{value, unit}`."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "string",
                  "pattern": "^\\{\\{.*\\}\\}$",
                  "description": "Binding expression."
                }
              ]
            },
            {
              "description": "object",
              "anyOf": [
                {
                  "type": "object",
                  "description": "object",
                  "additionalProperties": true
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            },
            {
              "anyOf": [
                {
                  "type": "array"
                },
                {
                  "$ref": "#/$defs/Binding",
                  "description": "Binding expression resolving to this value."
                }
              ]
            }
          ]
        },
        "enabled": {
          "description": "Whether the widget accepts user input (§2.6.0). Default `true`.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Whether the widget accepts user input (§2.6.0). Default `true`."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onChange": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "language": {
          "type": "string",
          "description": "BCP 47 tag. Omitted uses the host locale."
        },
        "continuous": {
          "description": "Keep listening after a pause instead of stopping at the first result.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Keep listening after a pause instead of stopping at the first result."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "interimResults": {
          "description": "Write partial transcripts as they arrive. The binding then changes many times per utterance — bind a separate path if the final text is what the document acts on.",
          "anyOf": [
            {
              "type": "boolean",
              "default": false,
              "description": "Write partial transcripts as they arrive. The binding then changes many times per utterance — bind a separate path if the final text is what the document acts on."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "maxDuration": {
          "description": "Seconds before capture stops on its own. A microphone with no ceiling is a microphone left on.",
          "anyOf": [
            {
              "type": "number",
              "description": "Seconds before capture stops on its own. A microphone with no ceiling is a microphone left on."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "showWaveform": {
          "description": "Render live input level. This is the user's only continuous evidence that capture is running, so a runtime SHOULD keep some indicator even when this is false.",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Render live input level. This is the user's only continuous evidence that capture is running, so a runtime SHOULD keep some indicator even when this is false."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "onStart": {
          "$ref": "#/$defs/Action"
        },
        "onResult": {
          "$ref": "#/$defs/Action"
        },
        "onEnd": {
          "$ref": "#/$defs/Action"
        },
        "onError": {
          "$ref": "#/$defs/Action"
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "webView": {
      "type": "object",
      "description": "Embedded web view.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "webView"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "url": {
          "type": "string",
          "default": "null (if html set)",
          "description": "URL to load"
        },
        "html": {
          "type": "string",
          "default": "null (if url set)",
          "description": "Raw HTML content — alternative to `url`"
        },
        "allowNavigation": {
          "description": "Allow link navigation within the view",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Allow link navigation within the view"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "enableJavaScript": {
          "description": "Enable JavaScript",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Enable JavaScript"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "enableZoom": {
          "description": "Allow pinch-to-zoom",
          "anyOf": [
            {
              "type": "boolean",
              "default": true,
              "description": "Allow pinch-to-zoom"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            }
          ]
        },
        "width": {
          "description": "Widget width",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget width"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "height": {
          "description": "Widget height",
          "anyOf": [
            {
              "type": "number",
              "description": "Widget height"
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "onPageStarted": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onPageFinished": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "onError": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "backgroundColor": {
          "description": "Legacy alias of `color`.",
          "anyOf": [
            {
              "$ref": "#/$defs/Color",
              "description": "Legacy alias of `color`."
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        }
      },
      "required": [
        "type"
      ],
      "additionalProperties": true
    },
    "wrap": {
      "type": "object",
      "description": "Flow layout that wraps children to the next line when they exceed available width/height.",
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "wrap"
          ]
        },
        "click": {
          "anyOf": [
            {
              "$ref": "#/$defs/Action"
            },
            {
              "$ref": "#/$defs/Binding",
              "description": "Binding expression resolving to this value."
            }
          ]
        },
        "tooltip": {
          "type": "string",
          "description": "Hover / long-press tooltip text. Runtime wraps the widget in a\n`Tooltip` surface.\n"
        },
        "runAlignment": {
          "type": "string",
          "description": "How the runs (lines) are aligned along the cross axis."
        },
        "verticalDirection": {
          "type": "string",
          "description": "Order children are laid out vertically: `down` or `up`."
        },
        "clipBehavior": {
          "type": "string",
          "description": "How content crossing the bounds is clipped."
        },
        "direction": {
          "type": "string",
          "default": "horizontal",
          "description": "Primary flow direction."
        },
        "spacing": {
          "description": "Gap between children on the same run.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Gap between children on the same run."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "runSpacing": {
          "description": "Gap between runs.",
          "anyOf": [
            {
              "type": "number",
              "default": 0,
              "description": "Gap between runs."
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression."
            },
            {
              "type": "object",
              "properties": {
                "value": {
                  "type": "number"
                },
                "unit": {
                  "type": "string"
                }
              },
              "required": [
                "value"
              ],
              "description": "Dimension object: `{value, unit}`."
            }
          ]
        },
        "alignment": {
          "type": "string",
          "default": "start",
          "description": "Alignment within a run."
        },
        "children": {
          "description": "Children to wrap.",
          "oneOf": [
            {
              "type": "array",
              "items": {
                "$ref": "#/$defs/Widget"
              }
            },
            {
              "type": "string",
              "pattern": "^\\{\\{.*\\}\\}$",
              "description": "Binding expression resolving to an array."
            }
          ]
        },
        "crossAxisAlignment": {
          "type": "string",
          "description": "Legacy alias of `alignment`."
        },
        "textDirection": {
          "type": "string",
          "description": "Resolution direction for `start`/`end` alignment."
        }
      },
      "required": [
        "type",
        "children"
      ],
      "additionalProperties": true
    },
    "Widget": {
      "anyOf": [
        {
          "$ref": "#/$defs/accessibleWrapper"
        },
        {
          "$ref": "#/$defs/accordion"
        },
        {
          "$ref": "#/$defs/alertDialog"
        },
        {
          "$ref": "#/$defs/align"
        },
        {
          "$ref": "#/$defs/animatedAlign"
        },
        {
          "$ref": "#/$defs/animatedContainer"
        },
        {
          "$ref": "#/$defs/animatedDefaultTextStyle"
        },
        {
          "$ref": "#/$defs/animatedOpacity"
        },
        {
          "$ref": "#/$defs/animatedPositioned"
        },
        {
          "$ref": "#/$defs/aspectRatio"
        },
        {
          "$ref": "#/$defs/avatar"
        },
        {
          "$ref": "#/$defs/badge"
        },
        {
          "$ref": "#/$defs/banner"
        },
        {
          "$ref": "#/$defs/barcode"
        },
        {
          "$ref": "#/$defs/baseline"
        },
        {
          "$ref": "#/$defs/bottomNavigation"
        },
        {
          "$ref": "#/$defs/bottomSheet"
        },
        {
          "$ref": "#/$defs/box"
        },
        {
          "$ref": "#/$defs/breadcrumb"
        },
        {
          "$ref": "#/$defs/button"
        },
        {
          "$ref": "#/$defs/calendar"
        },
        {
          "$ref": "#/$defs/canvas"
        },
        {
          "$ref": "#/$defs/card"
        },
        {
          "$ref": "#/$defs/carousel"
        },
        {
          "$ref": "#/$defs/center"
        },
        {
          "$ref": "#/$defs/chart"
        },
        {
          "$ref": "#/$defs/checkbox"
        },
        {
          "$ref": "#/$defs/checkboxGroup"
        },
        {
          "$ref": "#/$defs/chip"
        },
        {
          "$ref": "#/$defs/clipOval"
        },
        {
          "$ref": "#/$defs/clipRRect"
        },
        {
          "$ref": "#/$defs/codeEditor"
        },
        {
          "$ref": "#/$defs/colorPicker"
        },
        {
          "$ref": "#/$defs/combobox"
        },
        {
          "$ref": "#/$defs/conditional"
        },
        {
          "$ref": "#/$defs/contextMenu"
        },
        {
          "$ref": "#/$defs/customDialog"
        },
        {
          "$ref": "#/$defs/dashboard"
        },
        {
          "$ref": "#/$defs/dataTable"
        },
        {
          "$ref": "#/$defs/dateField"
        },
        {
          "$ref": "#/$defs/datePicker"
        },
        {
          "$ref": "#/$defs/dateRangePicker"
        },
        {
          "$ref": "#/$defs/dateTimePicker"
        },
        {
          "$ref": "#/$defs/decoration"
        },
        {
          "$ref": "#/$defs/diffViewer"
        },
        {
          "$ref": "#/$defs/divider"
        },
        {
          "$ref": "#/$defs/dragTarget"
        },
        {
          "$ref": "#/$defs/draggable"
        },
        {
          "$ref": "#/$defs/drawer"
        },
        {
          "$ref": "#/$defs/errorBoundary"
        },
        {
          "$ref": "#/$defs/errorRecovery"
        },
        {
          "$ref": "#/$defs/expanded"
        },
        {
          "$ref": "#/$defs/fileExplorer"
        },
        {
          "$ref": "#/$defs/fileInput"
        },
        {
          "$ref": "#/$defs/fittedBox"
        },
        {
          "$ref": "#/$defs/flexible"
        },
        {
          "$ref": "#/$defs/floatingActionButton"
        },
        {
          "$ref": "#/$defs/flow"
        },
        {
          "$ref": "#/$defs/form"
        },
        {
          "$ref": "#/$defs/fractionallySized"
        },
        {
          "$ref": "#/$defs/gantt"
        },
        {
          "$ref": "#/$defs/gauge"
        },
        {
          "$ref": "#/$defs/gestureDetector"
        },
        {
          "$ref": "#/$defs/graph"
        },
        {
          "$ref": "#/$defs/grid"
        },
        {
          "$ref": "#/$defs/headerBar"
        },
        {
          "$ref": "#/$defs/heatmap"
        },
        {
          "$ref": "#/$defs/hero"
        },
        {
          "$ref": "#/$defs/icon"
        },
        {
          "$ref": "#/$defs/iconButton"
        },
        {
          "$ref": "#/$defs/image"
        },
        {
          "$ref": "#/$defs/imageFilter"
        },
        {
          "$ref": "#/$defs/indexedStack"
        },
        {
          "$ref": "#/$defs/inkWell"
        },
        {
          "$ref": "#/$defs/intrinsicHeight"
        },
        {
          "$ref": "#/$defs/intrinsicWidth"
        },
        {
          "$ref": "#/$defs/kanban"
        },
        {
          "$ref": "#/$defs/kenBurnsImage"
        },
        {
          "$ref": "#/$defs/layoutBuilder"
        },
        {
          "$ref": "#/$defs/lazy"
        },
        {
          "$ref": "#/$defs/lightbox"
        },
        {
          "$ref": "#/$defs/limitedBox"
        },
        {
          "$ref": "#/$defs/linear"
        },
        {
          "$ref": "#/$defs/link"
        },
        {
          "$ref": "#/$defs/list"
        },
        {
          "$ref": "#/$defs/listItem"
        },
        {
          "$ref": "#/$defs/lottieAnimation"
        },
        {
          "$ref": "#/$defs/map"
        },
        {
          "$ref": "#/$defs/margin"
        },
        {
          "$ref": "#/$defs/markdown"
        },
        {
          "$ref": "#/$defs/mediaPlayer"
        },
        {
          "$ref": "#/$defs/mediaQuery"
        },
        {
          "$ref": "#/$defs/menu"
        },
        {
          "$ref": "#/$defs/multiSelect"
        },
        {
          "$ref": "#/$defs/navigationRail"
        },
        {
          "$ref": "#/$defs/networkGraph"
        },
        {
          "$ref": "#/$defs/numberField"
        },
        {
          "$ref": "#/$defs/numberStepper"
        },
        {
          "$ref": "#/$defs/offlineFallback"
        },
        {
          "$ref": "#/$defs/opacity"
        },
        {
          "$ref": "#/$defs/otpInput"
        },
        {
          "$ref": "#/$defs/padding"
        },
        {
          "$ref": "#/$defs/pageView"
        },
        {
          "$ref": "#/$defs/pagination"
        },
        {
          "$ref": "#/$defs/pdfViewer"
        },
        {
          "$ref": "#/$defs/permissionPrompt"
        },
        {
          "$ref": "#/$defs/placeholder"
        },
        {
          "$ref": "#/$defs/popover"
        },
        {
          "$ref": "#/$defs/popupMenuButton"
        },
        {
          "$ref": "#/$defs/positioned"
        },
        {
          "$ref": "#/$defs/progressBar"
        },
        {
          "$ref": "#/$defs/qrCode"
        },
        {
          "$ref": "#/$defs/radio"
        },
        {
          "$ref": "#/$defs/radioGroup"
        },
        {
          "$ref": "#/$defs/rangeSlider"
        },
        {
          "$ref": "#/$defs/rating"
        },
        {
          "$ref": "#/$defs/resizable"
        },
        {
          "$ref": "#/$defs/richText"
        },
        {
          "$ref": "#/$defs/richTextEditor"
        },
        {
          "$ref": "#/$defs/rive"
        },
        {
          "$ref": "#/$defs/safeArea"
        },
        {
          "$ref": "#/$defs/scrollAnimated"
        },
        {
          "$ref": "#/$defs/scrollBar"
        },
        {
          "$ref": "#/$defs/scrollView"
        },
        {
          "$ref": "#/$defs/segmentedControl"
        },
        {
          "$ref": "#/$defs/select"
        },
        {
          "$ref": "#/$defs/signature"
        },
        {
          "$ref": "#/$defs/simpleDialog"
        },
        {
          "$ref": "#/$defs/singleChildScrollView"
        },
        {
          "$ref": "#/$defs/sizedBox"
        },
        {
          "$ref": "#/$defs/slider"
        },
        {
          "$ref": "#/$defs/snackBar"
        },
        {
          "$ref": "#/$defs/spacer"
        },
        {
          "$ref": "#/$defs/splitter"
        },
        {
          "$ref": "#/$defs/spreadsheet"
        },
        {
          "$ref": "#/$defs/stack"
        },
        {
          "$ref": "#/$defs/staggeredGrid"
        },
        {
          "$ref": "#/$defs/stepper"
        },
        {
          "$ref": "#/$defs/tabBar"
        },
        {
          "$ref": "#/$defs/tabBarView"
        },
        {
          "$ref": "#/$defs/table"
        },
        {
          "$ref": "#/$defs/terminal"
        },
        {
          "$ref": "#/$defs/text"
        },
        {
          "$ref": "#/$defs/textInput"
        },
        {
          "$ref": "#/$defs/timeField"
        },
        {
          "$ref": "#/$defs/timePicker"
        },
        {
          "$ref": "#/$defs/timeline"
        },
        {
          "$ref": "#/$defs/toggle"
        },
        {
          "$ref": "#/$defs/tooltip"
        },
        {
          "$ref": "#/$defs/transform"
        },
        {
          "$ref": "#/$defs/tree"
        },
        {
          "$ref": "#/$defs/use"
        },
        {
          "$ref": "#/$defs/verticalDivider"
        },
        {
          "$ref": "#/$defs/view"
        },
        {
          "$ref": "#/$defs/visibility"
        },
        {
          "$ref": "#/$defs/voiceInput"
        },
        {
          "$ref": "#/$defs/webView"
        },
        {
          "$ref": "#/$defs/wrap"
        }
      ]
    },
    "ResponsiveValue": {
      "type": "object",
      "description": "Per-form-factor property override. Keys are FormFactor labels (`compact` / `medium` / `expanded` / `large` / `extraLarge` / `embedded`) plus an optional `default`. Values match the property's normal type. See `14_Responsive_Events.md` § 14.2.",
      "properties": {
        "compact": {},
        "medium": {},
        "expanded": {},
        "large": {},
        "extraLarge": {},
        "embedded": {},
        "default": {}
      },
      "additionalProperties": false
    },
    "Action": {
      "description": "An action definition per `04_Actions.md`; a list runs in order (§4.7).",
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "animation",
                "batch",
                "cancel",
                "channel",
                "channel.restart",
                "channel.send",
                "channel.start",
                "channel.stop",
                "channel.toggle",
                "client.clipboard",
                "client.exec",
                "client.getSystemInfo",
                "client.httpRequest",
                "client.listFiles",
                "client.notification",
                "client.readFile",
                "client.saveFile",
                "client.selectFile",
                "client.storage.get",
                "client.storage.remove",
                "client.storage.set",
                "client.writeFile",
                "conditional",
                "dialog",
                "event",
                "identity",
                "identity.promote",
                "identity.release",
                "location",
                "media.pause",
                "media.play",
                "media.seek",
                "media.toggle",
                "navigation",
                "notification",
                "parallel",
                "payment",
                "permission",
                "permission.revoke",
                "resource",
                "sequence",
                "sound.play",
                "sound.stop",
                "state",
                "submit",
                "tool"
              ]
            }
          },
          "required": [
            "type"
          ],
          "additionalProperties": true
        },
        {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "enum": [
                  "animation",
                  "batch",
                  "cancel",
                  "channel",
                  "channel.restart",
                  "channel.send",
                  "channel.start",
                  "channel.stop",
                  "channel.toggle",
                  "client.clipboard",
                  "client.exec",
                  "client.getSystemInfo",
                  "client.httpRequest",
                  "client.listFiles",
                  "client.notification",
                  "client.readFile",
                  "client.saveFile",
                  "client.selectFile",
                  "client.storage.get",
                  "client.storage.remove",
                  "client.storage.set",
                  "client.writeFile",
                  "conditional",
                  "dialog",
                  "event",
                  "identity",
                  "identity.promote",
                  "identity.release",
                  "location",
                  "media.pause",
                  "media.play",
                  "media.seek",
                  "media.toggle",
                  "navigation",
                  "notification",
                  "parallel",
                  "payment",
                  "permission",
                  "permission.revoke",
                  "resource",
                  "sequence",
                  "sound.play",
                  "sound.stop",
                  "state",
                  "submit",
                  "tool"
                ]
              }
            },
            "required": [
              "type"
            ],
            "additionalProperties": true
          }
        },
        {
          "type": "string",
          "pattern": "^\\{\\{.*\\}\\}$",
          "description": "Binding resolving to an action or a list of them."
        }
      ]
    }
  },
  "$ref": "#/$defs/Widget"
}
