type: signature
category: advanced
profile: Advanced
since: v1.0
description: |
  Signature capture pad.
properties:
  onSignatureStart:
    type: Action
    description: "Fired when the first stroke begins."
  borderWidth:
    type: Dimension
    description: "Thickness of the outline."
  binding:
    type: "binding"
    description: "Target binding for signature data (base64 PNG or SVG path). Required when capturing is needed; may be omitted for pure display."
  penColor:
    type: Color
    default: "#000000"
    description: "Stroke color"
    aliases:
      - color
  penWidth:
    type: "number"
    default: 2.0
    description: "Stroke width"
    aliases:
      - strokeWidth
  width:
    type: "number"
    default: null
    description: "Widget width"
  height:
    type: "number"
    default: null
    description: "Widget height"
  backgroundColor:
    type: Color
    default: null
    description: "Pad background"
  borderColor:
    type: Color
    default: null
    description: "Pad border"
  showClearButton:
    type: "boolean"
    default: true
    description: "Show a clear-signature button"
  showGuide:
    type: "boolean"
    default: true
    description: "Show a signing guide line."
  onSignatureEnd:
    type: "Action"
    default: null
    description: "Fired when a stroke completes"
  onClear:
    type: "Action"
    default: null
    description: "Fired when the signature is cleared"
# examples migrated verbatim from prose JSON fences.
examples:
  - name: prose_example_0
    dsl: |
      {
        "type": "signature",
        "binding": "{{signatureData}}",
        "penColor": "#000000",
        "penWidth": 2.0,
        "width": 400,
        "height": 200,
        "backgroundColor": "#FFFFFF",
        "borderColor": "#E0E0E0",
        "showClearButton": true,
        "showGuide": true,
        "onSignatureEnd": {
          "type": "state", "action": "set",
          "binding": "signatureData", "value": "{{event.value}}"
        },
        "onClear": {
          "type": "state", "action": "set",
          "binding": "signatureData", "value": null
        }
      }
