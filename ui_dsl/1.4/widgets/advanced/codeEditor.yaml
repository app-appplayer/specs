type: codeEditor
category: advanced
profile: Advanced
aliases: [code]
since: v1.0
description: |
  Syntax-highlighted code editor.
properties:
  lineNumberColor:
    type: Color
    description: "Colour of the line-number gutter."
  copyable:
    type: boolean
    default: false
    description: "Offer a copy-to-clipboard control. Useful with `readOnly`."
  expandAll:
    type: boolean
    default: false
    description: "Expand every collapsible region on load. Chiefly for structured languages such as JSON."
  code:
    type: "string | binding"
    description: "Code content — literal string or binding. Required unless `binding` supplies the content."
  language:
    type: "string"
    enum: ["plaintext", "javascript", "typescript", "dart", "python", "java", "kotlin", "swift", "go", "rust", "c", "cpp", "csharp", "ruby", "php", "sql", "json", "yaml", "xml", "html", "css", "markdown", "shell"]
    default: "plaintext"
    description: "Syntax highlighting language."
  theme:
    type: "string"
    enum: ["vsLight", "vsDark", "monokai", "solarizedLight", "solarizedDark", "github", "dracula"]
    legacyValues: ["light", "dark"]
    default: "vsDark"
    description: "Editor color theme."
  readOnly:
    type: "boolean"
    default: false
    description: "Disable editing (syntax highlighting still applies)"
  showLineNumbers:
    type: "boolean"
    default: true
    description: "Show gutter line numbers"
  fontSize:
    type: "number"
    default: null
    description: "Font size (logical px)"
  lineHeight:
    type: "number"
    default: null
    description: "Line height multiplier"
  tabSize:
    type: "number"
    default: 2
    description: "Tab width in spaces"
  width:
    type: "number"
    default: null
    description: "Widget width"
  height:
    type: "number"
    default: null
    description: "Widget height"
  backgroundColor:
    type: Color
    default: null
    description: "Override theme background"
  textColor:
    type: Color
    default: null
    description: "Override theme default text color"
  onChange:
    type: "Action"
    default: null
    description: "Fired on code change; `event.value` is current code"
  binding:
    type: "string"
    description: "State path bound two-way to the widget value (§2.6.0). Runtime reads/writes this path without requiring an explicit onChange."
  change:
    type: Action
    description: "Legacy alias of `onChange`."
# examples migrated verbatim from prose JSON fences.
examples:
  - name: prose_example_0
    dsl: |
      {
        "type": "codeEditor",
        "code": "{{sourceCode}}",
        "language": "javascript",
        "theme": "vsDark",
        "readOnly": false,
        "showLineNumbers": true,
        "fontSize": 14,
        "lineHeight": 1.5,
        "tabSize": 2,
        "width": 600,
        "height": 400,
        "onChange": {
          "type": "state", "action": "set",
          "binding": "sourceCode", "value": "{{event.value}}"
        }
      }
