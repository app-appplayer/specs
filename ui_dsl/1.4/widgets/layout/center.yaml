type: center
category: layout
profile: Core
description: |
  Centers a single child within available space.
properties:
  heightFactor:
    type: "number"
    description: "Size to this multiple of the child height."
  widthFactor:
    type: "number"
    description: "Size to this multiple of the child width."
  child:
    type: "Widget"
    required: true
    description: "Centered widget."
# examples migrated verbatim from prose JSON fences.
examples:
  - name: prose_example_0
    dsl: |
      { "type": "center", "child": { "type": "text", "text": "Centered" } }
